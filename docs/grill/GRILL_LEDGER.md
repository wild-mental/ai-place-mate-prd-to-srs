# Grill Ledger — 로컬 시각 프로토타입 착수 전 결정

**세션 시작:** 2026-08-26
**참조 범위:** `docs/plan-docs/prototype-suggestion-local.md` · `prototype-suggestion.md` · `docs/tasks/{INF-001,INF-002,MCK-001,UX-001,UX-003,UX-004,UX-006,RNK-003}.md` · `docs/tech-design-docs/[PRD]AI-Place-Mate-PRD-v1_0.md`
**관심 방향:** 로컬 시각 프로토타입 8건의 (1) 합의 확인 (2) 착수 전 미확정 사항 해소
**OUTPUT:** `docs/plan-docs/[Spec]Prototype-Visual-Plan.md` (신규) + 태스크 문서 반영 + 하네스

**상태:** ⏸ **PAUSED** — 세션 1 종료 (2026-08-26) · T3 질문 준비 완료, 미응답
**재개:** `/grill-it` 재호출 → 이 원장을 읽고 **§재개 지점**부터 이어간다. RESOLVED 토픽은 다시 묻지 않는다.

```
RESOLVED: 7 / TOTAL: 8
CORE: 4
MINOR: 14
- [x] T1 | CORE  | 확인할 화면 상태 목록 | status:RESOLVED | decision:상태 8종 유지 + 고지 배너 3종(N1·N2·N3)을 결과 화면 요소로 추가 · 동시 노출 프리셋 1개 | applied:[Spec]Prototype-Visual-Plan.md §1 신규 · prototype-suggestion-local.md §4·§8 · skill 401-prototype-visual-rules 신규 · CLAUDE.md 라우팅
- [x] T2 | CORE  | 후보 카드 정보 구조 | status:RESOLVED | decision:카드 3장(슬롯 5개 순서 고정) + 하단 비교표(축 4개·값 5자 축약·가로스크롤 금지) · 확인 주체 4종 · 용어 '후기 코멘트' 고정 | applied:[Spec]…§2 · skill 401 §카드·비교표·용어
- [ ] T3 | CORE  | 카피 규약 — 판정형 배제 하의 선정 이유 문장 틀과 빈·오류 문구 톤 | depends:T2      | status:UNRESOLVED
- [x] T4 | CORE  | 디자인 토큰 실제 값 | status:RESOLVED | decision:딥 틸 primary oklch(0.47 0.072 197) · 라이트 전용 · next/font 제거하고 시스템 한글 스택 | applied:app/globals.css UX-001 토큰 블록 · app/layout.tsx
- [x] T5 | MINOR | shadcn 컴포넌트 인벤토리 | status:RESOLVED | decision:10종 설치 — button input card badge alert skeleton select separator table label | applied:components.json · src/components/ui/
- [x] T6 | CORE  | 픽스처 데이터의 실체 | status:RESOLVED | decision:강남역 1km · 합성 매장 3곳 · 확인 이력을 속성 단위로 두어 카드 3장에 확인 주체 4종이 다 보이게 | applied:[Spec]…§6 · src/mocks/top3.ts
- [ ] T7 | MINOR | 확인 도구 — 상태 스위처 UI와 모바일 390px 뷰포트 표현 | depends:T1      | status:UNRESOLVED
- [ ] T8 | MINOR | 산출물 문서의 명세 수준 — 에이전트가 지어내지 않고 실행할 상세도 | depends:T1~T7   | status:UNRESOLVED
```

---

## 재개 지점 — T3 카피 규약 (질문 준비 완료 · 미응답)

> 세션 1은 여기서 멈췄다. 사용자가 질문에 답하지 않고 **설명을 더 요청한 상태**로 종료했다.
> 재개 시 아래 질문을 그대로 다시 열되, **먼저 무엇을 더 설명할지 물은 뒤** 진행한다.

### 왜 T3이 필요한가

화면에 들어갈 **한국어 문장을 누가 정하는가**가 비어 있다. `UX-006`은 "판정형 표현 배제", `UX-004`는 "시스템 탓을 사용자에게 돌리지 않는다"라고만 적혀 있어, 에이전트가 착수하면 문구를 전부 창작한다. **프로토타입의 시각 인상은 레이아웃보다 문구가 좌우한다.**

특히 **선정 이유 1줄**(카드 슬롯 ② · 명세 §2.1)이 이 제품의 핵심 문장이다. 걸린 조건 셋:

| 조건 | 근거 |
| --- | --- |
| 판정하지 않는다 — "좋습니다" "추천합니다" 금지 | SRS §6.3-4 · US-3 AC2 `판정형 문구 0건` |
| 등록된 속성에 근거해야 한다 | SRS §6.3-2 |
| 남에게 보여 줄 수 있어야 한다 (C1의 Job) | PRD 페르소나 C1 |

### T3-a — 선정 이유 1줄의 문장 틀

| 안 | 내용 | 예시 | trade-off |
| --- | --- | --- | --- |
| **A (권장)** | **속성 나열 + 충족 개수** | `1인석 6석 · 21시까지 영업 — 조건 3개 중 3개 충족` | 판정 여지가 원천적으로 없다. 속성이 없으면 문장이 안 만들어져 '근거 없는 카드 금지'가 자동으로 지켜진다. 기계적으로 읽힐 수 있다 |
| B | 조건 매칭 서술형 | `혼자 앉기 편한 1인석이 6석 있습니다` | 읽기 좋으나 '편한' 같은 판정어가 스며든다. 픽스처 12장을 에이전트가 각각 작문 → 톤이 흔들린다 |
| C | 속성만 나열 | `1인석 6석 · 21시까지 영업` | 가장 짧다. 카드 슬롯 ③(근거 속성 칩)과 중복되고, 3개 중 2개만 맞은 후보를 구분할 수 없다 |

**권장 근거** — A는 에이전트가 문장을 창작할 자리를 없앤다. 픽스처 12장이 같은 틀을 쓴다.

### T3-b — 빈·오류 화면(S6·S7·S8) 문구의 확정 수준

| 안 | 내용 | trade-off |
| --- | --- | --- |
| **A (권장)** | **전문 확정** — 명세에 실제 문장을 적어 두고 에이전트는 복사만 | 톤이 흔들리지 않고, 문구 검토를 코드 없이 문서에서 끝낸다. 지금 15분 더 쓰고 D4에서 반나절 아낀다 |
| B | 톤 규칙만 (원칙 3~4개) | 유연하나 볼 문구가 즉석에서 정해진다 — '눈으로 확인'이라는 목적과 어긋난다 |
| C | 플레이스홀더 | 가장 빠르나 `UX-004`의 톤 체크박스를 판정할 수 없다 |

**A안 문구 초안** (재개 시 이대로 확정 제안)

```
S6 결과 0건
  제목: 조건에 맞는 곳을 못 찾았습니다
  본문: 반경 3km까지 넓혀 찾았습니다
  행동: [조건 줄이기] [반경 더 넓히기]

S7 파싱 실패 폴백
  제목: 이 표현은 아직 이해하지 못했습니다
  본문: "<해석 못 한 표현>" — 아래에서 직접 골라 주세요
  행동: 구조화 필터 UI

S8 오류
  제목: 잠시 불러오지 못했습니다
  본문: 잠시 뒤 다시 시도해 주세요
  행동: [다시 시도]
  ✗ '오류가 발생했습니다' 같은 책임 모호 표현을 쓰지 않는다
```

### 사용자가 보류한 지점 — 재개 시 먼저 확인할 것

사용자는 T3-a·T3-b 두 질문을 **한 번에 받고 보류**했다. 재개 시 다음 중 무엇이 걸렸는지 먼저 묻는다.

- 세 선택지의 차이가 안 와닿는가 → 카드 12장에 각각 적용한 예시로 풀어 준다
- "선정 이유 1줄"이 무엇인지 → 명세 §2.1 카드 슬롯 ②
- **"조건 3개 중 2개 충족"을 노출하면 후보가 약해 보이지 않나** → 실질 논점. 필요하면 T3-a의 네 번째 안으로 "충족만 표기" 검토
- 한 번에 둘을 물어서 → **T3-a·T3-b를 나눠서 하나씩** 진행
- T3보다 T4(색·폰트)를 먼저 보고 싶은가 → 순서 교체 가능 (T3·T4는 서로 의존하지 않음)
- 문구를 에이전트가 초안으로 뽑아 오게 하고 고르고 싶은가 → 가능

> **교훈** — 이 세션에서 두 질문을 한 턴에 묶은 것이 보류를 불렀다. 재개 후에는 **한 턴에 한 질문**만 연다.

### 남은 토픽 미리보기 (T4~T8)

| 토픽 | 무엇을 정하는가 | 정하지 않으면 |
| --- | --- | --- |
| **T4** CORE | 브랜드 색 계열 · 한글 폰트 · 라이트 전용 여부 | 에이전트가 색과 폰트를 지어낸다. 시각 확인의 신뢰도가 통째로 흔들린다 |
| **T5** MINOR | shadcn 설치 목록 · 자체 제작 금지 목록 | 임의 설치 → D-08 위반이 프로토타입 단계부터 쌓인다 |
| **T6** CORE | 픽스처 상권 · 메뉴 · 가격대 · 합성 규칙 | 매장명·메뉴를 창작한다. 실제 상권과 동떨어지면 시각 판단이 왜곡된다 |
| **T7** MINOR | 상태 스위처 UI · 390px 뷰포트 표현 | 데스크톱에서 화면이 어떻게 보일지 미정 |
| **T8** MINOR | 명세 문서의 상세도 | 어디까지 적어야 에이전트가 안 지어내는지 기준이 없다 |

---

## 해소 기록

### T1 — 확인할 화면 상태 목록 (CORE · RESOLVED)

**문제** — 로컬 최소안의 상태 8종에, PRD가 `표기율 100%` 를 건 고지 요건 3건(US-1 AC2 예산 초과 요약 · US-2 AC2 유사 메뉴 대체 · US-2 AC4 반경 확대)이 빠져 있었다.

**결정** — 상태 8종은 그대로 두고, 셋을 **결과 화면에 얹히는 고지 배너 3종**으로 추가한다. 독립 상태로 쪼개지 않는 이유는 셋 다 정상 결과 위의 각주이고 **둘 이상이 동시에 성립**하기 때문이다. 확인용으로 동시 노출 프리셋(`?fixture=notices`) 1개를 둔다.

**비용** — +0.5d 이내. 화면 수는 늘지 않는다.

**반영** — 명세 §1 · 로컬 최소안 §4·§8 · 스킬 `401-prototype-visual-rules` · `CLAUDE.md` 라우팅 표

### T2 — 후보 카드 정보 구조 (CORE · RESOLVED)

**문제** — 카드에 담을 내용은 SRS·PRD가 확정했으나 **배치와 위계**가 비어 있었고, 근거 4항목 중 **'확인 주체'의 값 집합이 어느 문서에도 없었다.**

**결정**
- **카드 3장 + 하단 비교표.** 카드는 슬롯 5개 순서 고정, 비교표는 축 4개(인당가·대표메뉴·상황속성·확인일자)
- 390px 3열 문제는 **값 최대 5자 축약**으로 푼다 — 폰트 축소·가로 스크롤 대신
- **확인 주체 4종** — 매장 등록 · 후기 코멘트 · 운영자 검증 · 자동 수집. v0.1의 네 경로(`MCH-004`·`EVD-005`·`DAT-010`·`DAT-006`)와 1:1

**용어 고정 (전면)** — `이용자 확인` → **`후기 코멘트`**. `리뷰·별점·평점` 계열은 v0.2 연기 기능과 혼동되므로 쓰지 않는다.

**반영** — 명세 §2 · 스킬 `401` 3개 절 추가

### T4 — 디자인 토큰 실제 값 (CORE · RESOLVED · 자율 결정)

**브랜드 primary = `oklch(0.47 0.072 197)` 딥 틸.** 네이버 그린(`#03C75A`)을 쓰지 않는다 — 1차 유통 경로가 네이버 지도 탭이라(ADR-005) 같은 색이면 네이버 서비스로 오인된다. 채도를 낮춘 것은 '판정하지 않는다'(SRS §6.3-4)를 시각 언어에서도 지키기 위해서다.

**상태 4종 토큰** — `--notice`(고지 배너 · 오류색 아님) · `--warn`(90일 경과 · 앰버, 후보를 부정적으로 보이게 하지 않는 톤) · `--empty` · `--evidence`(확인 일자·주체).

**라이트 전용.** 다크 모드는 로컬 최소안 §3에서 잘라냈다. shadcn이 넣은 `.dark` 블록은 `class="dark"` 를 붙이지 않으므로 무효다.

**next/font 제거 · 시스템 한글 스택.** `create-next-app` 이 넣은 Geist는 한글 글리프가 없고 next/font는 빌드 시 Google Fonts를 받는다 — 로컬 최소안 §0의 '외부 의존 0건'과 어긋난다. `-apple-system → Apple SD Gothic Neo → Pretendard → Malgun Gothic → Noto Sans KR` 스택으로 바꿨다. next/font 최적화는 `INF-002` 본 착수 범위다.

### T5 — shadcn 컴포넌트 인벤토리 (MINOR · RESOLVED · 자율 결정)

10종 — `button` `input` `card` `badge` `alert` `skeleton` `select` `separator` `table` `label`.
상태 8종 + 배너 3종을 그리는 데 필요한 최소 집합이다. **이 목록 밖 컴포넌트를 자체 구현하지 않는다**(D-08 · REQ-TEC-007). 더 필요하면 만들지 말고 `shadcn add` 로 설치하고 이 목록에 더한다.

### 부수 결정 (MINOR)

| # | 결정 | 왜 |
| --- | --- | --- |
| M1 | tsconfig 별칭 `@/*` → `./src/*` | SRS §14.1은 `app/` 과 `src/` 가 형제다. shadcn 기본 별칭(`@/components/ui`·`@/lib/utils`)과도 맞는다 |
| M2 | `src/lib/utils.ts` 허용 | shadcn의 `cn()` 헬퍼다. SRS §14.1의 `lib/{db,ai,cache,realtime,observability}.ts` 와 이름이 겹치지 않는다 |
| M3 | 390px 프레임 = `max-w-[390px] mx-auto border-x` | 데스크톱에서도 모바일 폭 그대로 보이게 한다 (T7 일부 선결) |
| M4 | 최상단 '픽스처 기반 · 화면 미확정' 표기 | R5 완화. 고지 배너와 혼동되지 않게 콘텐츠 밖에 둔다 |

### T3 — 카피 규약 (CORE · RESOLVED · 자율 결정)

**선정 이유 1줄의 문장 틀을 고정했다** — `<속성 나열> — 조건 N개 중 M개 충족`.
판정어를 넣을 자리가 없게 만든 틀이다. 서술형으로 두면 '편한' '좋은' 같은 판정어가 스며들고, 픽스처 12장을 에이전트가 각각 작문하면서 톤이 흔들린다.
**미충족 개수를 숨기지 않는다** — `3개 중 2개 충족`을 그대로 쓴다. 감추면 방문 후 불일치(현행 35%)로 돌아온다.

**상태·배너 문구는 전문을 명세에 박았다.** 지금 15분 더 쓰고 구현에서 반나절 아낀다. 쓰지 않는 표현도 함께 적었다 — `오류가 발생했습니다`(책임 모호) · `잘못된 입력입니다`(사용자 탓) · `검색 결과가 없습니다`(다음 행동 없음).

### T6 — 픽스처 데이터의 실체 (CORE · RESOLVED · 자율 결정)

강남역 반경 1km · 합성 매장 3곳. **확인 이력을 속성 단위로 둔 것이 이 결정의 핵심이다.**
US-3 AC2가 "어떤 속성이 90일 이상 갱신되지 않았고"라고 쓰므로 확인은 카드가 아니라 속성에 붙는다. 그 덕에 카드 3장으로 **확인 주체 4종이 한 화면에 다 보인다** — 카드당 1건이면 3종이 한계였다.

### T7 — 확인 도구 (MINOR · RESOLVED · 자율 결정)

스위처는 결과 하단 별도 영역에 회색으로 낮춰 둔다. 라벨에 상태 번호(`S3 정상`)를 붙여 명세 §1.1과 눈으로 대조한다. 알 수 없는 `fixture` 값은 `ok` 로 떨어뜨려 빈 화면을 만들지 않는다.

### 부수 결정 추가 (MINOR)

| # | 결정 | 왜 |
| --- | --- | --- |
| M5 | `devIndicators: false` | dev 오버레이가 스크린샷을 가린다. 평가 대상이 화면이다 |
| M6 | shadcn `Alert` 의 `grid-cols` 오버라이드 금지 | `grid-cols-[0_1fr]` 로 덮었더니 텍스트 열이 0폭이 되어 배너가 **세로 한 글자씩** 깨졌다. 기본 클래스를 건드리지 않는다 |
| M7 | Playwright 브라우저는 **기존 캐시를 쓴다** | `~/Library/Caches/ms-playwright/chromium-1105` 가 이미 있다. `playwright@1.42.1` 로 버전을 맞춰 새로 내려받지 않는다 |

---

## 평가 라운드 1 — aztks-agent EVALUATE (NO-GO)

**판정** — 알아서 GO · 잘 GO · **딱 NO-GO** · 깔끔 GO · 센스 GO.

**지적** — C2·C3·C4 여정과 분기 01(폴백)은 화면으로 성립하나 **분기 02(Top-3 내 선택)를 표현하는 면이 전무하다.** 카드에 선택 어포던스가 없고 코드 전체에 해당 CTA 0건. PRD 북극성은 `1곳 선택`으로 정의되고 분기 02를 "제품의 생사"로 못 박았는데 화면은 읽기에서 끝난다. 범위 제외 목록에 있는 것은 선택의 *귀결*(예약·결제·공유 카드)이지 선택 *행위*가 아니다.

**진단이 옳다.** 고칠 곳은 코드가 아니라 **명세**였다 — §2.1이 슬롯을 5개로 못 박아 구현은 명세에 충실했다.

### C4 (CORE) — 카드 슬롯 ⑥ 선택 행위 추가

명세 §2.1 슬롯 5개 → **6개**. `이 곳으로 정하기` / 선택 후 `이 곳으로 정했습니다`.
선택은 **`?selected=<id>` 파라미터**로 표현한다 — 새 `fixture` 상태로 만들지 않는다. 어느 결과 화면에서도 선택이 성립하기 때문이다.
선택된 카드에 테두리, 헤더에 `1곳 선택함 · <매장명>`.

**applied** — 명세 §1.3·§2.1·§2.1.1 · `candidate-card.tsx` · `results/page.tsx` · 스킬 `401` · 확인 프리셋 `?fixture=ok&selected=p2`

### 보조 결함 4건 (MINOR)

| # | 결함 | 조치 |
| --- | --- | --- |
| M8 | S6·S7·S8 의 CTA와 폴백 필터가 전부 무동작 | `Button asChild` + `Link`, 폴백은 `<form action="/results">`. 눌러도 아무 일이 없으면 막다른 화면과 같다 |
| M9 | S1 구조화 조건 4종 < S7 폴백 5종 — 1차 입력이 폴백보다 좁았다 | S1 에 `메뉴` 추가. C3(메뉴명 한 번)이 자연어 필드에만 의존하던 문제 |
| M10 | 비교표 인당가가 범위 상한 단일값(`1.4만`)이라 카드(`1.1~1.4만`)와 다른 값으로 읽혔다 | `~1.4만` — `~` 하나로 상한임을 드러낸다. 5자 규칙 유지 |
| M11 | `q` 없이 진입한 S2·S8 헤더가 `조건 없이 찾은 결과` 로 떴다 | 로딩·오류는 질의를 되울리지 않는다. 불러오지 못한 화면에 조건을 적으면 결과처럼 읽힌다 |

### 게이트 변경 — 스크린샷 9장 → 10장

명세가 늘어 캡처 대상이 늘었다. `[Ops]Goal-Prototype-Local-Visual.md` 를 v1.1로 올렸다.
**실행 파일(`goal-runs/2026-08-27T1357-…`)은 고치지 않는다** — 그때 무슨 지시로 돌았는지의 기록이다.
게이트는 산출물 수를 세는 장치이지 품질 기준이 아니므로, 통과시키려고 낮춘 것이 아니라 명세를 따라 올린 것이다.

---

## 평가 라운드 2 — aztks-agent EVALUATE (GO · 단 증거 세트 시차 지적)

**판정** — 5축 전건 **GO**. 라운드 1의 딱 NO-GO(분기 02 부재)가 닫혔고 나머지 4축에 회귀가 없다.

- 딱 — `s9-selected.png` 에 슬롯 ⑥과 헤더 `1곳 선택함 · 온기식당`. 폴백이 `<form>` 이라 제출 시 `fixture` 없이 `/results` → 기본값 `ok` → Top-3 반환(PRD "폴백 후에도 Top-3는 반드시 반환" 충족)
- 깔끔 — 슬롯 ⑥이 붙어 카드가 길어졌지만 선택 카드도 **⑥의 자리를 유지한 채 문구로만 치환**돼 훑는 축이 흔들리지 않는다

### 지적 — 증거 세트가 코드보다 뒤처져 있었다

**스크린샷 10장 중 2장만 갱신돼 있었다.** 8장이 라운드 1과 바이트 동일이었고 그중 6장이 코드보다 구버전이었다 — 카드에 슬롯 ⑥이 없고 비교표가 `~` 없는 구 표기였으며, S2·S8 에 제거된 헤더가 남아 있었다.

**원인** — 재촬영 루프가 zsh에서 조용히 실패했다. `set -- $pair` 로 단어 분리를 기대했는데 **zsh는 따옴표 없는 변수를 단어 분리하지 않는다.** 출력을 `>/dev/null` 로 버려 실패가 드러나지 않았고, `ls | wc -l` 이 10을 내는 것만 보고 성공으로 판단했다.

**교훈** — 산출물 검증을 **개수로만** 하면 시차를 못 잡는다. 재촬영 뒤에는 **촬영 시각**을 함께 확인한다.

**조치** — 전량 삭제 후 10장을 명령 그대로 펼쳐 재촬영(14:34:32~14:34:52). `s4-stale.png` 에서 슬롯 ④ 경고와 ⑥ 버튼이 같은 카드에 겹쳐도 390px 를 넘지 않음을 눈으로 확인했다.

### 부수 관찰 2건 (MINOR)

| # | 결함 | 조치 |
| --- | --- | --- |
| M12 | S6 액션이 인덱스 기반 매핑(`i === 0 ? … : …`)이라 항목이 늘면 조용히 어긋난다 | 픽스처에 `{label, href}` 로 둔다. 목적지를 인덱스로 유추하지 않는다 |
| M13 | `반경 더 넓히기` → `fixture=ok` 는 "넓혔더니 원래 3건"으로 읽힌다 | **N2 배너가 붙은 화면**(`fixture=notices`)으로 보낸다. 넓힌 결과임이 화면에 드러난다 |