# -*- coding: utf-8 -*-
"""태스크별 이슈 명세의 실질 내용.

기계적 부분(제목·라벨·References 앵커·의존성·DoD 공통)은 `gen_task_docs.py` 가
`tasks_data.py` 에서 파생한다. 이 파일은 사람이 판단해야 하는 것만 담는다.

필드
  purpose     : 이 태스크가 없으면 무엇이 성립하지 않는지 한 문장
  breakdown   : 코딩 에이전트가 순차 실행 가능한 체크리스트
  scenarios   : GWT. fail=True 가 실패 흐름. 스토리마다 실패 1건 이상 필수
  constraints : (라벨, 내용). 수치는 SRS §4.2에서 그대로 인용
  sdd/seq     : 설계 문서 앵커 · 시퀀스 다이어그램 (없으면 생략)
  dod_extra   : 태스크 고유 완료 조건
  external    : 외부 블로커 (없으면 "없음")
"""

SPECS = {}

# ══════════════════════════════════════════════════════════════════
# S-1 기반 · 제약 게이트
# ══════════════════════════════════════════════════════════════════

SPECS["INF-001"] = dict(
    purpose="Next.js 앱과 Vercel 배포 경로, 환경 변수, Cron 스케줄을 한 번에 세워 "
            "나머지 58개 태스크가 올라설 바닥을 만든다. 이것이 없으면 아무것도 배포되지 않는다.",
    sdd="12-컴포넌트-다이어그램--시스템-내부-구조", seq="해당 없음",
    breakdown=[
        "`create-next-app` 으로 App Router 프로젝트 생성 (TypeScript · Node.js 런타임 기본)",
        "`src/` · `app/` 디렉터리 구조를 SRS §14.1대로 배치 (`app/actions` · `app/api` · `src/modules` · `src/lib`)",
        "Vercel 프로젝트 연결 및 Git 저장소 연동 — **Git Push만으로 배포되는 경로 확립** (C-TEC-007)",
        "`vercel.ts` 작성 — `crons` 5종 등록 (`freshness` 04:00 · `close-rooms` 매 1분 · `aggregate` 03:00 · `purge` 05:00 · `noshow` 매 10분)",
        "환경 변수 11종을 Vercel에 등록 (SRS §6.5 표 그대로). `SUPABASE_SERVICE_ROLE_KEY` 는 서버 전용으로 격리",
        "`scripts/check-env.mjs` 작성 — 필수 환경 변수 누락 시 **빌드 실패**",
        "프리뷰·프로덕션 배포를 각각 1회 성공시켜 경로를 실증",
        "단위 테스트 — `check-env.mjs` 가 누락 변수를 정확히 지목하는지",
    ],
    scenarios=[
        dict(title="Git Push만으로 배포된다",
             given="Vercel 프로젝트가 저장소에 연결된 상태에서",
             when="main 브랜치에 커밋을 푸시하면",
             then="외부 CI 설정 없이 프로덕션 배포가 자동 실행된다. 저장소에 CI 워크플로 파일이 존재하지 않아야 한다.",
             slo="워크플로 파일 0개 (REQ-TEC-011)"),
        dict(title="Cron 5종이 스케줄대로 호출된다",
             given="`vercel.ts` 의 `crons` 가 배포된 상태에서",
             when="각 스케줄 시각이 지나면",
             then="해당 Route Handler가 호출되고 실행 로그가 남는다. 경로는 SRS §6.1의 Cron 표와 정확히 일치해야 한다."),
        dict(fail=True, title="필수 환경 변수가 없으면 배포가 차단된다",
             given="`GOOGLE_GENERATIVE_AI_API_KEY` 가 등록되지 않은 상태에서",
             when="빌드를 실행하면",
             then="`check-env.mjs` 가 누락 변수명을 출력하고 **빌드가 실패**한다. 변수 없이 배포된 산출물이 존재해서는 안 된다.",
             slo="누락 상태 배포 0건"),
    ],
    constraints=[
        ("기술 제약", "**C-TEC-001** 단일 Next.js 앱. **C-TEC-007** 배포·인프라를 Vercel로 단일화하고 외부 CI를 두지 않는다. "
                   "**REQ-TEC-013** Cron 엔드포인트는 `CRON_SECRET` 으로 인증한다."),
        ("런타임", "기본 **Node.js 런타임**을 사용한다. Prisma와 Node API 전체가 필요하므로 Edge 런타임을 쓰지 않는다 (SRS §3.1)."),
        ("보안", "`SUPABASE_SERVICE_ROLE_KEY` 는 **클라이언트에 노출 금지**. `NEXT_PUBLIC_` 접두어를 붙이지 않는다 (SRS §6.5)."),
        ("되돌림 비용", "낮음 — 다만 디렉터리 구조 변경은 이후 전 태스크의 경로에 영향을 주므로 S-1에서 확정한다."),
    ],
    dod_extra=["`vercel.ts` 의 Cron 경로가 SRS §6.1 표와 1:1 일치하는가?",
               "환경 변수 11종이 전부 등록되었고 누락 시 빌드가 실패하는가?"],
    external="**DEP-T1** Vercel 계정 및 프로젝트 생성 권한",
)

SPECS["INF-002"] = dict(
    purpose="Tailwind와 shadcn/ui를 설치해 AI가 일관된 디자인 코드를 생성하도록 강제하고, "
            "1차 유통 경로인 네이버 지도 탭 진입을 성립시킨다.",
    sdd="11-컨텍스트-다이어그램--시스템의-경계", seq="해당 없음",
    breakdown=[
        "Tailwind CSS 설치 및 `UX-001` 이 정의한 디자인 토큰을 테마로 등록",
        "`shadcn/ui` 초기화 — `src/components/ui/` 에 설치. `UX-002` 의 컴포넌트 인벤토리 목록만 추가",
        "`next/font` 로 폰트 최적화 설정 (LCP 예산에 직결 · REQ-NF-004)",
        "네이버 지도 탭 임베드 진입 경로 구성 — 프레임 정책 헤더, 진입 파라미터 처리",
        "독립 모바일 웹 진입 경로를 **같은 스프린트에서 병행** 구성 (ADR-005)",
        "ESLint 규칙 추가 — shadcn/ui에 존재하는 컴포넌트의 자체 구현 차단 (REQ-TEC-007)",
        "단위 테스트 — 임의 CSS 파일 추가 시 린트가 경고하는지",
    ],
    scenarios=[
        dict(title="디자인 토큰이 Tailwind 테마로 동작한다",
             given="`UX-001` 의 토큰이 테마에 등록된 상태에서",
             when="컴포넌트에서 토큰 기반 유틸리티 클래스를 사용하면",
             then="정의된 색·간격·타이포가 적용되고, 하드코딩된 색상값이 코드에 남아 있지 않다.",
             slo="하드코딩 색상값 0건"),
        dict(title="두 진입 경로가 모두 동작한다",
             given="네이버 지도 탭과 독립 모바일 웹이 배포된 상태에서",
             when="각 경로로 진입하면",
             then="동일한 앱이 렌더되고 진입 경로가 계측 이벤트의 `entry_point` 속성으로 구분된다."),
        dict(fail=True, title="shadcn/ui에 있는 컴포넌트를 자체 구현하면 차단된다",
             given="인벤토리에 등재된 컴포넌트가 있고",
             when="같은 역할의 컴포넌트를 직접 작성하면",
             then="ESLint가 오류를 내고 **빌드가 실패**한다. 중복 구현이 머지되지 않아야 한다.",
             slo="중복 구현 0건 (D-08)"),
    ],
    constraints=[
        ("기술 제약", "**C-TEC-004** UI·스타일링은 Tailwind + shadcn/ui로 한정한다. **D-08** shadcn/ui에 존재하는 컴포넌트를 다시 만들지 않는다."),
        ("성능", "**REQ-NF-004** 모바일 초기 렌더 LCP ≤ 2.5s (4G 기준). 클라이언트 번들을 최소화하고 Server Component를 우선한다."),
        ("유통 제약", "**LIM-T05 · ADR-005** 네이버 지도 탭의 노출·정책 결정권이 당사에 없다. 독립 웹 경로를 항상 병행 유지한다."),
    ],
    dod_extra=["shadcn/ui 컴포넌트 인벤토리(`UX-002`)와 실제 설치 목록이 일치하는가?"],
    external="네이버 지도 플랫폼 탭 노출 승인 (**DEP-T4** 계열 · 미승인 시 독립 웹 경로만으로 진행)",
)

SPECS["INF-003"] = dict(
    purpose="Prisma와 Supabase를 연결하고 커넥션 풀러를 고정해, 서버리스 환경에서 "
            "커넥션 고갈로 5xx가 터지는 구조적 위험(R-T2)을 착수 전에 막는다.",
    sdd="11-컨텍스트-다이어그램--시스템의-경계", seq="해당 없음",
    breakdown=[
        "로컬 Supabase 구성 — `supabase init` · `supabase start` 로 운영과 동일한 스택 기동",
        "Prisma 설치 및 `prisma/schema.prisma` 의 `datasource` 블록 작성",
        "`DATABASE_URL` 을 **Supavisor 트랜잭션 모드(:6543)** 로, `DIRECT_URL` 을 직결(:5432)로 설정 (ADR-T04)",
        "`src/lib/db.ts` Prisma 싱글턴 작성 — 개발 모드 HMR 대비 `globalThis` 캐싱",
        "마이그레이션 승인 절차 수립 — `prisma migrate deploy` 를 **빌드 단계에 넣지 않고** 배포 전 수동 승인으로 분리",
        "로컬·운영 스키마 드리프트 검사 스크립트 작성 (`prisma migrate diff`)",
        "단위 테스트 — 싱글턴이 재사용되는지(인스턴스 수 1), 원시 커넥션 생성이 없는지",
    ],
    scenarios=[
        dict(title="커넥션이 풀러를 경유한다",
             given="`DATABASE_URL` 이 Supavisor 트랜잭션 모드로 설정된 상태에서",
             when="애플리케이션이 DB에 접근하면",
             then="포트 6543을 통해 연결되고 `pgbouncer=true` 파라미터가 적용된다. 직결(:5432)은 마이그레이션 전용이어야 한다.",
             slo="런타임 직결 사용 0건"),
        dict(title="로컬과 운영의 스키마가 일치한다",
             given="`supabase start` 로 로컬 환경이 기동된 상태에서",
             when="마이그레이션을 적용하고 드리프트를 검사하면",
             then="로컬과 운영의 스키마 차이가 0이어야 한다.",
             slo="드리프트 0건 (REQ-TEC-006)"),
        dict(fail=True, title="커넥션 환경 변수가 하나라도 없으면 빌드가 실패한다",
             given="`DIRECT_URL` 이 등록되지 않은 상태에서",
             when="빌드를 실행하면",
             then="`verify-constraints` 가 누락을 지목하고 빌드가 실패한다. 마이그레이션이 불가능한 상태로 배포되어서는 안 된다.",
             slo="REQ-TEC-005 위반 배포 0건"),
        dict(fail=True, title="마이그레이션이 자동 적용되지 않는다",
             given="스키마 변경이 포함된 커밋이 푸시된 상태에서",
             when="Vercel 빌드가 실행되면",
             then="빌드는 성공하되 **스키마 변경이 자동 적용되지 않는다.** 적용은 별도 승인 단계에서만 일어난다.",
             slo="빌드 중 migrate deploy 실행 0회"),
    ],
    constraints=[
        ("기술 제약", "**C-TEC-003** Prisma + Supabase. **REQ-TEC-004** DB 접근은 `src/lib/db.ts` 싱글턴만 허용. "
                   "**REQ-TEC-005** `DATABASE_URL`(:6543) / `DIRECT_URL`(:5432) 분리 필수."),
        ("처리량", "**REQ-NF-003** Phase 1 300 RPS. 풀 사용률 80% 초과 시 알림(`REL-001`)이 뜨며, 3,000 RPS는 컴퓨트 상향이 전제다."),
        ("안정성", "마이그레이션을 빌드에 넣지 않는다 — 롤백이 어려운 스키마 변경의 자동 적용을 막기 위함 (SRS §14.4)."),
    ],
    dod_extra=["`supabase start` 로컬 환경과 운영 마이그레이션이 동일한가? (REQ-TEC-006)"],
    external="**DEP-T2** Supabase 프로젝트(운영) 및 로컬 CLI 설치",
)

SPECS["INF-008"] = dict(
    purpose="모든 요청이 지나는 단일 지점에서 경로 태깅과 인증 훅을 걸어, "
            "관측(`REL-001`)과 인가가 각 핸들러에 흩어지지 않게 한다.",
    sdd="12-컴포넌트-다이어그램--시스템-내부-구조", seq="해당 없음",
    breakdown=[
        "`proxy.ts` 작성 (Next.js 16 규약 — `proxy` 함수 + `proxyConfig`)",
        "요청 태깅 — 요청 ID 발급, 진입 경로(`entry_point`) 판별, 응답 헤더에 상관 ID 부착",
        "인증 훅 — 가맹 콘솔(`/(merchant)`) 경로에 세션 검사 배치",
        "`matcher` 설정 — 정적 자산과 계측 엔드포인트를 제외해 불필요한 실행을 막음",
        "관측 연동 지점 노출 — `REL-001` 이 읽을 태그 규약 정의",
        "단위 테스트 — 보호 경로 미인증 접근이 차단되는지, 상관 ID가 응답에 실리는지",
    ],
    scenarios=[
        dict(title="모든 요청에 상관 ID가 부여된다",
             given="`proxy.ts` 가 배포된 상태에서",
             when="임의의 요청이 들어오면",
             then="요청 ID가 발급되고 응답 헤더와 로그에 동일한 값이 남아 추적이 가능하다."),
        dict(fail=True, title="미인증 상태로 가맹 콘솔에 접근할 수 없다",
             given="세션이 없는 상태에서",
             when="`/(merchant)` 경로에 접근하면",
             then="핸들러 실행 전에 차단되고 로그인으로 유도된다. 콘솔 데이터가 응답에 포함되어서는 안 된다.",
             slo="비인가 노출 0건 (REQ-NF-012)"),
    ],
    constraints=[
        ("성능", "프록시는 모든 요청 경로에 있으므로 **REQ-NF-001a**(결정론 경로 p95 ≤ 1,000ms) 예산을 잠식하지 않아야 한다. 동기 I/O를 넣지 않는다."),
        ("기술 제약", "**C-TEC-002** 서버 로직은 RSC · Server Action · Route Handler로만 구현한다. 프록시는 라우팅 계층이며 비즈니스 로직을 두지 않는다."),
        ("보안", "**REQ-NF-012** 내부 조회는 감사 로그 대상이다. 프록시가 붙인 상관 ID가 `audit_logs` 와 연결되어야 한다."),
    ],
    external="없음",
)

SPECS["TEC-001"] = dict(
    purpose="제약을 선언이 아니라 **빌드 실패로 드러나는 장치**로 만든다. 이 게이트가 먼저 작동해야 "
            "이후 스프린트의 위반이 즉시 잡히고, 없으면 위반이 누적된 뒤에 발견된다.",
    sdd="12-컴포넌트-다이어그램--시스템-내부-구조", seq="해당 없음",
    breakdown=[
        "`src/modules/` 8개 디렉터리 스캐폴딩 — indexing · query · evidence · ranking · agentroom · merchant · reservation · analytics",
        "각 모듈에 `index.ts` 공개 표면 작성 — 외부에 노출할 것만 export (REQ-TEC-002)",
        "ESLint `no-restricted-imports` 규칙 — 모듈 내부 파일의 교차 임포트 차단, 자기 자신 `fetch` 금지",
        "`scripts/verify-constraints.mjs` 작성 — REQ-TEC-001·003·004·005·008·011·015 검사",
        "  · 배포 산출물 1개 · 독립 서버 프로세스 부재 (REQ-TEC-001)",
        "  · DB 접근이 `src/lib/db.ts` 경유인지 (REQ-TEC-004) · 커넥션 환경 변수 분리 (REQ-TEC-005)",
        "  · AI 호출이 `src/lib/ai.ts` 경유인지 (REQ-TEC-008) · 외부 CI 워크플로 부재 (REQ-TEC-011)",
        "  · Redis 등 외부 캐시 의존성 부재 (REQ-TEC-015)",
        "`package.json` build 스크립트에 게이트 편입 — `prisma generate && tsc --noEmit && eslint && verify-constraints && next build`",
        "통합 지점 검증 — 의도적 위반을 넣어 빌드가 실제로 실패하는지 (`TST-007` 이 이를 상시 검증)",
    ],
    scenarios=[
        dict(title="모듈 경계를 지킨 임포트는 통과한다",
             given="모듈이 `index.ts` 로만 공개 표면을 노출한 상태에서",
             when="다른 모듈이 그 `index.ts` 를 임포트하면",
             then="ESLint 경고 없이 빌드가 성공한다."),
        dict(fail=True, title="모듈 내부 파일 직접 임포트가 차단된다",
             given="`src/modules/query/parser.ts` 가 있고",
             when="`src/modules/ranking` 이 그 파일을 직접 임포트하면",
             then="ESLint가 오류를 내고 **빌드가 실패**한다. 경계를 넘은 결합이 머지되어서는 안 된다.",
             slo="경계 위반 머지 0건 (ADR-T01)"),
        dict(fail=True, title="제약 위반이 빌드를 막는다",
             given="게이트가 build 스크립트에 편입된 상태에서",
             when="REQ-TEC 위반(예: 원시 DB 커넥션 생성)을 포함한 커밋을 푸시하면",
             then="Vercel 빌드가 실패하고 **이전 버전이 유지**된다. 위반 상태가 프로덕션에 도달하지 않아야 한다.",
             slo="위반 배포 0건 (REQ-TEC-012 · ADR-T08)"),
    ],
    constraints=[
        ("기술 제약", "**C-TEC-007 · D-06** 외부 CI를 두지 않으므로 품질 게이트는 **빌드 명령 안**에 있어야 한다. 빌드 실패 = 배포 차단으로 동일한 효과를 낸다."),
        ("성능", "게이트는 빌드 시간을 늘린다(**LIM-T07**). 검사 스크립트는 파일 스캔 위주로 작성하고 네트워크 호출을 넣지 않는다."),
        ("되돌림 비용", "낮음 — 린트 규칙과 스크립트 변경 수준. 다만 게이트를 늦게 붙이면 그동안 쌓인 위반을 한꺼번에 걷어내야 한다."),
    ],
    dod_extra=["의도적 위반 3종(경계·DB·AI)을 넣었을 때 전부 빌드가 실패하는가?"],
    external="없음",
)

SPECS["TEC-006"] = dict(
    purpose="비동기 처리를 `after()` 와 DB 큐 테이블로만 하도록 강제한다. 외부 메시지 브로커가 "
            "슬쩍 들어오면 C-TEC-001의 단일 스택 전제가 무너진다.",
    seq="해당 없음",
    breakdown=[
        "`verify-constraints.mjs` 에 검사 추가 — `package.json` 의존성에 메시지 브로커 클라이언트가 없는지 (kafkajs · @aws-sdk/client-sqs · bullmq · amqplib 등)",
        "허용 수단 명문화 — 응답 이후 처리는 `after()`, 지연·재시도가 필요하면 **DB 큐 테이블**",
        "DB 큐 테이블 규약 정의 — 상태 컬럼(`pending`/`processing`/`done`/`failed`) · 재시도 횟수 · 잠금 방식 · Cron 소비자 패턴",
        "기존 큐 성격 테이블(`ReverifyTask`)이 규약을 따르는지 점검 (`DAT-010` 과 교차)",
        "단위 테스트 — 금지 의존성을 추가했을 때 검사가 실패하는지",
    ],
    scenarios=[
        dict(title="허용된 수단은 통과한다",
             given="비동기 처리가 `after()` 또는 DB 큐 테이블로 구현된 상태에서",
             when="빌드를 실행하면",
             then="제약 검사를 통과한다."),
        dict(fail=True, title="외부 브로커 의존성이 빌드를 막는다",
             given="`package.json` 에 메시지 브로커 클라이언트를 추가한 상태에서",
             when="빌드를 실행하면",
             then="`verify-constraints` 가 해당 패키지명을 지목하고 **빌드가 실패**한다.",
             slo="브로커 의존성 0개 (REQ-TEC-014 · D-04)"),
    ],
    constraints=[
        ("기술 제약", "**D-04** Kafka·SQS를 도입하지 않는다. **REQ-TEC-014** 응답 이후 처리는 `after()` 또는 DB 큐 테이블로만 구현한다."),
        ("대가", "큐가 없어 대량 유입 시 DB 쓰기 부하가 발생한다(SRS §15-10). 부하는 `REL-003`(풀 사용률)에서 감시한다."),
    ],
    external="없음",
)

SPECS["CTR-001"] = dict(
    purpose="백엔드와 프론트엔드가 공유하는 **단일 기준점**을 만든다. 계약이 각 기능 태스크 안에 "
            "흩어지면 두 태스크가 같은 계약을 다르게 구현해도 탐지되지 않는다.",
    sdd="41-query-service--조건을-해석하는-부품", seq="SD-01 조건 검색 → Top-3 반환 · SD-07 제안 선택 → 예약 승계 → 선결제",
    breakdown=[
        "`src/contracts/` 생성 및 모듈 공개 표면 등록 (TEC-001 규약 준수)",
        "공통 결과 타입 정의 — `ActionResult<T> = { ok: true; data: T } | { ok: false; code: ErrorCode; message: string }`. **예외를 던져 실패를 표현하지 않는다**",
        "`ErrorCode` 를 **타입 자리만** 선언 — 값 목록은 `CTR-002` 가 채운다 (순환 의존 방지)",
        "Server Action 9종의 입력 Zod 스키마 작성 — SRS §6.1 표의 `입력` 열을 필드 단위로 전개",
        "9종의 출력 타입 작성 — `createAgentRoom` 은 **소환 0곳 미개시를 정상 응답**으로 표현할 수 있어야 한다",
        "**`ConditionSet` Zod 스키마 정의** — 결정론 파서의 반환 타입이자 LLM 구조화 출력(`Output.object`)의 검증 대상. 두 경로가 **동일 스키마**를 쓴다",
        "인가 가드 시그니처 정의 — 각 액션이 첫 줄에서 호출할 `assertActor(...)` 계약",
        "계약 스냅샷 테스트 작성 — 스키마 변경 시 diff가 드러나게",
        "단위 테스트 — 9종 유효/무효 입력, 개인정보 필드 부재 검증",
    ],
    scenarios=[
        dict(title="유효 입력이 타입 안전하게 통과한다",
             given="`submitQuery` 의 입력 스키마가 정의된 상태에서",
             when="자연어 1줄·지역·인원을 담은 유효 입력을 파싱하면",
             then="`z.infer` 로 추출한 타입과 런타임 결과가 일치하고 반환은 `ActionResult<...>` 다.",
             slo="tsc 경고 0건"),
        dict(title="소환 0곳이 실패가 아닌 정상 응답이다",
             given="`createAgentRoom` 의 출력 계약이 정의된 상태에서",
             when="적합 매장이 0곳인 결과를 표현하면",
             then="`ok: true` 이면서 소환 수 0과 미개시 사유를 담을 수 있다. 오류로 표현하면 '빈 화면 금지'(§6.3-6)를 구현할 수 없다."),
        dict(fail=True, title="무효 입력이 스택 노출 없이 거부된다",
             given="`requestPayment` 에 `reservationId` 가 누락된 입력이 주어지고",
             when="입력 검증이 실행되면",
             then="예외를 던지지 않고 `{ ok: false, code, message }` 를 반환한다. 응답에 스택·내부 경로·SQL이 포함되어서는 안 된다."),
        dict(fail=True, title="개인 식별 정보 필드가 계약에 없다",
             given="9종 입력 스키마 전체가 정의된 상태에서",
             when="스키마 키 집합을 검사하면",
             then="참석자 연락처·개인 취향·비고에 해당하는 필드가 0개여야 한다.",
             slo="발견 시 테스트 실패 (REQ-NF-010)"),
    ],
    constraints=[
        ("성능", "입력 검증은 요청 경로 맨 앞에 있으므로 **REQ-NF-001a**(p95 ≤ 1,000ms) 예산을 잠식하지 않아야 한다. 스키마 컴파일은 모듈 로드 시 1회만."),
        ("안정성", "모든 실패를 `ActionResult` 로 표현해 5xx로 새지 않게 한다. **REQ-NF-006** 5xx ≤ 0.3% — 검증 실패는 5xx가 아니다."),
        ("보안", "**REQ-NF-010** 개인 제약 정보는 계약에 필드를 두지 않는다. **SRS §6.1 규약 ②** Server Action은 공개 엔드포인트와 동등하므로 인가를 첫 줄에서 확인한다."),
        ("기술 제약", "**C-TEC-002** · **REQ-TEC-002** 계약은 `src/contracts/index.ts` 로만 노출. **REQ-TEC-010** LLM 응답은 이 스키마 검증을 통과해야만 사용한다."),
    ],
    dod_extra=["9종이 SRS §6.1 Server Actions 표와 1:1 일치하는가? (누락 0 · 임의 추가 0)"],
    external="없음",
)

SPECS["CTR-002"] = dict(
    purpose="외부에서 들어오는 모든 경로(PG 웹훅·계측·Cron·공유 카드)의 요청·응답·인증 규약과 "
            "시스템 공통 **에러 코드 체계**를 한자리에 고정한다. 이들은 외부 노출 지점이라 계약이 곧 공격면 정의다.",
    sdd="45-reservation--payment-service", seq="SD-08 취소·환불 / 노쇼 판정·정산 · SD-10 계측 이벤트 수집 → 집계 → 알림",
    breakdown=[
        "`src/contracts/routes.ts` — 8종의 경로·메서드·인증 방식을 단일 테이블로 선언",
        "인증 방식 4종의 가드 계약 — `public`(share-cards) · `sessionToken`(events) · `cronSecret`(cron 5종) · `signedWebhook`(pg)",
        "`CRON_SECRET` 검증 헬퍼 — 외부 호출은 거부되어야 한다 (REQ-TEC-013)",
        "PG 웹훅 서명 검증 계약 — 서명 불일치 시 **본문을 파싱하지 않고** 즉시 거부",
        "**멱등 키 계약** — 웹훅에서 멱등 키를 추출하는 규칙과 `Payment.idempotencyKey` 매핑",
        "**`ErrorCode` 값 목록 정의** — `CTR-001` 이 만든 타입 자리를 채운다. SRS에 근거가 흩어져 있어 **여기서 처음 체계화**한다",
        "에러 코드 5분류 — ① 입력 검증 실패 ② 인증·인가 실패 ③ 근거 미충족(400) ④ 외부 시스템 실패 ⑤ **폴백 신호**",
        "HTTP 상태 매핑표 작성 — 무엇이 4xx이고 무엇이 5xx인지. **REQ-NF-006 오류율 집계 기준**이 된다",
        "Cron 5종 공통 응답 계약 — 처리 건수·소요 시간·마지막 실행 시각 (`REL-001` 이 읽는다)",
        "단위 테스트 — 인증 실패 4종과 멱등 키 중복이 전부 거부되는지",
    ],
    scenarios=[
        dict(title="Cron 엔드포인트가 정상 호출을 처리한다",
             given="`CRON_SECRET` 이 설정되고 계약이 정의된 상태에서",
             when="Vercel Cron이 올바른 시크릿과 함께 호출하면",
             then="처리 건수·소요 시간·마지막 실행 시각을 담은 응답을 반환한다. 5종이 동일한 형태여야 한다."),
        dict(title="폴백 신호가 오류로 집계되지 않는다",
             given="에러 코드 체계가 정의된 상태에서",
             when="파싱 실패·결과 0건·제안 0건 신호를 분류하면",
             then="셋 다 **폴백 신호**로 분류되고 5xx가 아니다. `REL-007` 의 오류율 집계에서 제외된다 (§6.3-6)."),
        dict(fail=True, title="서명 없는 웹훅이 본문 파싱 전에 거부된다",
             given="`PG_WEBHOOK_SECRET` 이 설정된 상태에서",
             when="서명이 없거나 불일치하는 요청이 도착하면",
             then="**본문을 파싱하기 전에** 거부한다. 응답에 내부 상태·스택이 포함되지 않아야 하고 결제 오류율 예산에 5xx로 집계되지 않아야 한다.",
             slo="결제 API 5xx ≤ 0.1%"),
        dict(fail=True, title="외부에서 Cron 엔드포인트를 실행할 수 없다",
             given="`/api/cron/purge` 가 배포된 상태에서",
             when="`CRON_SECRET` 없이 외부에서 호출하면",
             then="인증 거부로 응답하고 **개인정보 파기 로직이 실행되지 않는다**.",
             slo="무단 실행 0건 (REQ-TEC-013 · REQ-NF-010)"),
        dict(fail=True, title="근거 4항목이 없는 공유 카드는 만들어지지 않는다",
             given="근거 4항목 중 하나가 누락된 후보 id가 주어지고",
             when="`/api/share-cards/[id]` 를 호출하면",
             then="이미지를 생성하지 않고 **400**을 반환한다. 빈 이미지나 플레이스홀더를 반환해서는 안 된다."),
    ],
    constraints=[
        ("성능", "`/api/share-cards/[id]` 는 GET 캐시가 가능한 유일한 경로다. 캐시 헤더 계약이 **REQ-FUNC-005** 생성 p95 ≤ 3,000ms 달성 수단이다."),
        ("안정성", "**REQ-NF-006** 5xx ≤ 0.3% · 결제 ≤ 0.1%. 인증·검증 실패는 4xx이며 5xx로 집계되지 않아야 한다."),
        ("보안", "**REQ-NF-011** 웹훅은 서명 검증 후에만 처리하고 카드 정보를 수신·보관하지 않는다. **REQ-TEC-013** Cron은 외부 호출을 거부한다."),
        ("설계 원칙", "**폴백은 오류가 아니다.** 파싱 실패·결과 0건·제안 0건은 열화된 정상 흐름이므로 오류율 예산에 집계되면 안 된다."),
    ],
    dod_extra=["8종이 SRS §6.1 Route Handlers 표와 1:1 일치하는가?",
               "에러 코드 → HTTP 상태 매핑표가 작성되었고 `REL-007` 이 이를 집계 기준으로 쓸 수 있는가?"],
    external="**DEP-T4** PG 계약 — 서명 알고리즘·멱등 키 필드명이 확정되어야 웹훅 계약을 최종 고정할 수 있다. 미확정 시 해당 부분만 잠정 정의하고 `RSV-003` 착수 전까지 확정한다.",
)

SPECS["CTR-005"] = dict(
    purpose="계측 이벤트 20종의 이름·발생 시점·필수 속성을 확정한다. 이벤트 스펙이 코드보다 먼저 "
            "정해지지 않으면 각 기능이 제각각 이벤트를 만들어 지표를 집계할 수 없다.",
    sdd="46-analytics-service--계측", seq="SD-10 계측 이벤트 수집 → 집계 → 알림",
    breakdown=[
        "SRS §10.2.2의 이벤트 20종을 Zod 스키마로 정의 — 조건 입력 5 · 후보 소비·결정 7 · 방문 후 3 · 공급 측 6",
        "공통 봉투(envelope) 정의 — `event_id`(UUID v4) · `name` · `session_id` · `occurred_at` · `received_at` · `traffic_type`",
        "이벤트별 필수 속성 명세 — 예: `top3_rendered` 는 `candidate_count` · `latency_ms` · **`parse_path`** · `radius_expanded` · `substitute_used`",
        "`parse_path` 열거값 확정 (`cache` / `deterministic` / `llm`) — REQ-NF-001a·001b 분리 측정의 전제",
        "`incomplete` 플래그 규약 — 필수 속성 결측 시 폐기하지 않고 표기해 적재",
        "계약 문서화 — `CTR-002` 의 `/api/events` 페이로드 스키마와 정합 확인",
        "계약 테스트 작성 — `TST-008` 이 이 스키마로 상시 검증",
        "단위 테스트 — 20종 전부 유효 페이로드 통과·필수 속성 누락 탐지",
    ],
    scenarios=[
        dict(title="이벤트 20종이 스키마로 검증된다",
             given="이벤트 계약이 정의된 상태에서",
             when="각 이벤트의 유효 페이로드를 검증하면",
             then="20종 전부 통과하고 SRS §10.2.2 표의 필수 속성이 모두 존재한다.",
             slo="누락 0종"),
        dict(title="경로 태깅 속성이 존재한다",
             given="`top3_rendered` 스키마가 정의된 상태에서",
             when="속성 목록을 검사하면",
             then="`parse_path` 가 필수 속성으로 존재한다. 이 값이 없으면 **REQ-NF-001a와 001b를 분리 측정할 수 없다**."),
        dict(fail=True, title="필수 속성이 빠진 이벤트가 표기된다",
             given="필수 속성 하나가 결측된 이벤트가 주어지고",
             when="검증을 실행하면",
             then="이벤트를 폐기하지 않고 `incomplete: true` 로 표기해 적재한다. `ANA-005` 의 결측률 집계 대상이 된다.",
             slo="결측률 ≤ 1%"),
    ],
    constraints=[
        ("계측 원칙", "**중복 제거는 애플리케이션이 아니라 스키마가 보장한다** — `event_id` 를 기본 키로 두어 DB 제약으로 이중 계수를 차단한다 (SRS §10.2)."),
        ("성능", "계측은 사용자 요청 경로의 지연 원인이 되면 안 된다. 스키마 검증은 수집 엔드포인트(`ANA-002`)에서 수행하고 렌더 경로에 넣지 않는다."),
        ("보안", "**REQ-NF-010** 이벤트 속성에 개인 식별 정보를 담지 않는다. 좌표·연락처를 속성으로 두지 않는다."),
    ],
    dod_extra=["SRS §10.2.2의 이벤트 20종과 스키마가 1:1 일치하는가?",
               "`parse_path` 열거값 3종이 정의되었는가?"],
    external="없음",
)

SPECS["DAT-001"] = dict(
    purpose="색인의 기본 단위를 `Place` 가 아니라 **`Dish` + `Attribute`** 로 고정하고, 근거 표기의 전제인 "
            "`Verification` 분리와 계측의 전제인 `Event` 파티셔닝까지 `schema.prisma` 한 파일에서 확정한다.",
    sdd="31-erd--개체와-관계", seq="해당 없음",
    breakdown=[
        "`Place` — `id` · `name` · `districtCode` · `seats` · `maxParty` · `latitude` · `longitude` · `deletedAt`(논리 삭제). `@@index([districtCode, deletedAt])`",
        "`Dish` — `placeId` · `name` · `canonicalKey` · `price`. **`@@index([canonicalKey])` 필수**",
        "`PriceProfile` — `perPersonLow` · `perPersonHigh` · `sampleSize`. **단일 값 컬럼을 두지 않는다**",
        "`Attribute` — `ownerId` · `scope`(PLACE/DISH) · `key` · `value`. **성분·접근성 필드를 같은 테이블에 사전 확보** (REQ-FUNC-010). `@@index([ownerId, scope, key])`",
        "`Verification` 을 **별도 엔터티로 분리** — `attributeId`(unique) · `status` · `verifiedAt` · `verifiedBy` · `sourceUrl`. `@@index([verifiedAt])` (ADR-002)",
        "`AgentRoom` · `Proposal` · `Reservation` · `Payment` — `Proposal` 에 **가격 필드 없음**, `Payment` 에 **카드 정보 컬럼 없음**, `idempotencyKey` unique",
        "`Event` — `eventId`(PK · 중복 제거 키) · `occurredAt` · `receivedAt` · `properties` · `incomplete`",
        "`Event` **일 단위 파티셔닝 원시 SQL 마이그레이션** 작성 (Prisma 스키마로 표현 불가)",
        "**`canonicalKey` 정규화 사전 및 정규화기** 구현 — '물냉면/평양냉면/냉면'을 동일 키로",
        "시드 스크립트 — 가격 표본 5건 이상/미만 매장, 90일 초과 속성을 각각 포함",
        "단위 테스트 — 논리 삭제 동작, `canonicalKey` 교차 매장 조회, 금지 컬럼 부재",
    ],
    scenarios=[
        dict(title="메뉴명 정규화 키로 여러 매장이 함께 조회된다",
             given="`canonicalKey = \"냉면\"` 을 공유하는 `Dish` 가 서로 다른 세 `Place` 아래 적재되어 있고",
             when="`canonicalKey` 로 조회하면",
             then="세 매장의 `Dish` 가 모두 반환되고 인덱스를 사용한다. `EXPLAIN` 에 Seq Scan이 없어야 한다."),
        dict(title="속성마다 확인 이력이 정확히 하나 붙는다",
             given="`Attribute` 와 `Verification` 이 1:1로 정의된 상태에서",
             when="속성을 조회하면",
             then="`verifiedAt` · `verifiedBy` 가 반드시 함께 조회된다. 확인 이력 없는 속성이 저장될 수 없어야 한다 (ADR-002)."),
        dict(fail=True, title="물리 삭제가 차단된다",
             given="이미 참조된 `Place` 레코드가 있고",
             when="삭제 요청이 들어오면",
             then="행을 지우지 않고 `deletedAt` 만 갱신한다. 삭제 후에도 `Dish` 이력이 조회 가능해야 한다."),
        dict(fail=True, title="가격 표본이 부족한 매장이 식별된다",
             given="`sampleSize < 5` 인 `PriceProfile` 이 존재하고",
             when="해당 매장을 조회하면",
             then="`sampleSize` 만으로 '가격 확인 필요' 판정이 가능해야 한다. `QRY-007` 이 이 값으로 예상가 산출 여부를 정한다 (US-1 AC6)."),
        dict(fail=True, title="금지된 컬럼이 스키마에 존재하지 않는다",
             given="전체 스키마가 적용된 상태에서",
             when="컬럼 목록을 검사하면",
             then="`Proposal` 에 가격 협상 필드 0개, `Payment` 에 카드 정보 컬럼 0개여야 한다.",
             slo="발견 시 `TST-008` 실패 (§6.3-7 · REQ-NF-011)"),
    ],
    constraints=[
        ("성능", "`canonicalKey` 인덱스가 **REQ-NF-002**(색인 조회 p95 ≤ 400ms)의 전제다. 누락 시 목표 달성이 불가능해진다."),
        ("안정성", "마이그레이션은 `prisma migrate deploy` 로만 적용하며 **빌드 단계에 넣지 않는다** (SRS §6.4 · §14.4)."),
        ("보안", "**REQ-NF-010** 개인 식별 정보를 두지 않는다. 회식 참석자의 비고·개인 취향 필드는 **스키마에 존재해서는 안 된다**."),
        ("기술 제약", "**C-TEC-003** · **REQ-TEC-004** DB 접근은 `src/lib/db.ts` 싱글턴 경유만. **REQ-TEC-006** 로컬·운영 드리프트 0."),
        ("되돌림 비용", "**ADR-001 기준 최대** — 색인 단위를 바꾸면 전면 재색인 + REQ-FUNC-002~005 전부 재작업. S0에서 확정하고 이후 변경하지 않는다."),
    ],
    dod_extra=["`supabase start` 로컬과 운영 마이그레이션이 동일한가? (REQ-TEC-006)",
               "SRS §6.2의 전 모델이 실제 `schema.prisma` 와 일치하는가?"],
    external="**DEP-T2** Supabase 프로젝트(운영 · 로컬 CLI) — 미확보 시 마이그레이션 검증 불가",
)

SPECS["DAT-006"] = dict(
    purpose="스키마 위에 실제 값을 채워 넣고, 조회를 `use cache` 로 감싸 Redis 없이 캐시 히트율 70%를 만든다. "
            "값이 비면 스키마가 있어도 Top-3가 나오지 않는다(R2).",
    sdd="42-index-service--색인과-신선도", seq="SD-01 조건 검색 → Top-3 반환",
    breakdown=[
        "색인 적재 파이프라인 — `Place` → `Dish`(정규화 키 부여) → `Attribute` → `Verification` 순으로 적재",
        "적재 시 `canonicalKey` 정규화기(`DAT-001`) 적용 및 미매칭 메뉴명 로그 수집 (사전 확장 재료)",
        "속성 조회 API 설계 — `queryByAttribute(AttributeQuery)` 형태로 `query` 모듈에 노출",
        "`src/lib/cache.ts` — `use cache` 래퍼 작성. `cacheTag` 로 무효화 단위를 `place:{id}` · `district:{code}` 로 분리",
        "`cacheLife` 설정 — 속성·메뉴 조회 TTL 6시간 (SRS §6.1 캐시 TTL)",
        "변경 시 무효화 — 적재·재확인 처리 후 `updateTag` 호출",
        "캐시 히트율 계측 — 히트/미스를 이벤트로 적재해 `ANA-002` 가 집계",
        "커버리지 리포트 — 상권별 필수 필드 5개 충족 매장 수 산출 (목표 300곳 · R2 추적)",
        "단위 테스트 — 캐시 히트 시 DB 호출이 발생하지 않는지, 무효화 후 최신값이 나오는지",
    ],
    scenarios=[
        dict(title="캐시 히트 시 DB를 조회하지 않는다",
             given="속성이 캐시에 적재된 상태에서",
             when="동일 키로 다시 조회하면",
             then="DB 호출 없이 반환되고 응답이 목표 안에 든다.",
             slo="캐시 히트 시 p95 ≤ 120ms (REQ-NF-002)"),
        dict(title="변경 후 최신값이 반영된다",
             given="캐시에 적재된 속성이 재확인으로 갱신되고",
             when="`updateTag` 가 호출된 뒤 조회하면",
             then="갱신된 값이 반환된다. 오래된 값이 계속 나오면 안 된다."),
        dict(fail=True, title="커버리지 미달이 드러난다",
             given="상권의 필수 필드 5개 충족 매장이 300곳 미만이고",
             when="커버리지 리포트를 산출하면",
             then="상권별 부족 수치가 보고되고 `ANA-005` 의 알림 대상이 된다. 부족을 감춘 채 결과를 내보내지 않는다.",
             slo="상권당 < 300곳 시 알림 (R2)"),
    ],
    constraints=[
        ("성능", "**REQ-NF-002** 색인 조회 p95 ≤ 400ms · 캐시 히트 시 ≤ 120ms · 히트율 ≥ 70%."),
        ("기술 제약", "**D-03 · REQ-TEC-015** 외부 캐시 서버를 도입하지 않는다. 캐시는 Next.js `use cache` 와 PostgreSQL로만 구현한다 (ADR-T05)."),
        ("한계", "**LIM-T02** 캐시 무효화가 태그 단위로만 가능하다. 키 단위 세밀 제어가 필요하면 태그 설계를 다시 나눠야 한다."),
        ("되돌림 비용", "중간 — 태그 설계를 바꾸면 무효화 호출 지점을 전부 손봐야 한다."),
    ],
    external="**DEP-T2 · DEP-T5** 상권 3곳 데이터와 가맹 온보딩 인력 — 값이 없으면 파이프라인만 있고 결과가 비어 있다.",
)

SPECS["DAT-008"] = dict(
    purpose="모든 테이블에 RLS를 걸고 내부 조회를 전량 감사 로그로 남긴다. Supabase는 클라이언트가 "
            "DB에 직접 붙을 수 있는 구조라 RLS 없이는 데이터가 사실상 공개다.",
    sdd="33-데이터베이스-물리-설계-요점", seq="해당 없음",
    breakdown=[
        "`supabase/policies/` 에 테이블별 RLS 정책 SQL 작성 — **전 테이블 RLS 활성화**",
        "역할 분리 — 서버 로직은 서비스 롤, 클라이언트 직접 접근은 익명 롤로 제한",
        "익명 롤 정책 — 공개 가능한 읽기만 허용하고 `Payment` · `Event` · `audit_logs` 는 전면 차단",
        "가맹 콘솔 정책 — 자기 매장 데이터만 접근 (`MCH-001` 의 인증 주체와 연결)",
        "`audit_logs` 테이블 생성 — 조회 주체 · 대상 · 시각 · 상관 ID(`INF-008`)",
        "PostgreSQL 트리거 작성 — 내부 조회를 전량 기록",
        "마이그레이션에 정책 포함 — 로컬·운영 동일 적용 (REQ-TEC-006)",
        "단위 테스트 — 익명 롤로 금지 테이블 접근 시 거부되는지 (`TST-008` 이 상시 검증)",
    ],
    scenarios=[
        dict(title="서비스 롤은 정상 접근한다",
             given="RLS가 전 테이블에 활성화된 상태에서",
             when="서버 로직이 서비스 롤로 조회하면",
             then="정책이 허용한 범위에서 정상 반환되고 `audit_logs` 에 기록이 남는다."),
        dict(fail=True, title="익명 롤이 결제·계측 테이블에 접근할 수 없다",
             given="익명 롤 키만 가진 클라이언트가 있고",
             when="`Payment` 또는 `Event` 를 직접 조회하면",
             then="RLS가 거부한다. 한 행도 반환되어서는 안 된다.",
             slo="비인가 반환 0행 (REQ-NF-012)"),
        dict(fail=True, title="다른 매장의 데이터에 접근할 수 없다",
             given="가맹 A로 인증된 세션이 있고",
             when="가맹 B의 프로필·제안을 조회하면",
             then="RLS가 거부한다. 매장 간 데이터 격리가 정책 수준에서 보장되어야 한다."),
    ],
    constraints=[
        ("보안", "**REQ-NF-012** 전 테이블 RLS 활성화 · 내부 조회 전량 감사 로그. 애플리케이션 코드가 아니라 **DB 정책이 최종 방어선**이다."),
        ("기술 제약", "**C-TEC-003** Supabase 기능 범위 안에서 구현한다. 정책은 마이그레이션에 포함해 로컬·운영이 동일해야 한다."),
        ("되돌림 비용", "높음 — 정책을 느슨하게 배포한 뒤 조이면 그 사이의 노출을 되돌릴 수 없다. S0에서 엄격하게 시작한다."),
    ],
    dod_extra=["전 테이블에 RLS가 활성화되었는가? (누락 테이블 0개)"],
    external="**DEP-T2** Supabase 프로젝트",
)

SPECS["DAT-010"] = dict(
    purpose="90일이 지난 속성을 야간에 찾아 경고 상태로 바꾸고 재확인 큐에 넣는다. 이것이 없으면 "
            "'확인 90일 경과' 경고를 띄울 근거 자체가 만들어지지 않는다.",
    sdd="fc-06-속성-신선도-관리-흐름", seq="SD-09 불일치 신고 → 재확인 큐",
    breakdown=[
        "`app/api/cron/freshness/route.ts` 작성 — `CRON_SECRET` 인증 (REQ-TEC-013)",
        "야간 스캔 — `Verification.verifiedAt` 인덱스로 90일 초과 건 조회, `status = STALE` 전환",
        "`ReverifyTask` 큐 적재 — 사유(`STALE` / `USER_REPORT`) · 우선순위 · 큐 시각. `TEC-006` 의 DB 큐 규약을 따른다",
        "90일 초과 비율 산출 — **20% 초과 시 우선순위 상향** 및 알림 (`ANA-005` 연동)",
        "운영자 처리 인터페이스 — `nextTask(우선순위)` 로 다음 항목을 꺼내고 결과를 반영",
        "처리 결과 반영 — 사실 확인 시 `VERIFIED` + `verifiedAt` 갱신, 사실 아님이면 `RETIRED` 후 노출 중단",
        "Cron 실행 결과 응답 — 처리 건수·소요·마지막 실행 시각 (`REL-001` 이 읽음)",
        "단위 테스트 — 89일/91일 경계, 큐 우선순위 상향 조건",
    ],
    scenarios=[
        dict(title="90일 초과 속성이 STALE로 전환된다",
             given="`verifiedAt` 이 91일 전인 속성이 있고",
             when="야간 스캔이 실행되면",
             then="`status = STALE` 로 전환되고 `ReverifyTask` 가 생성된다. 89일 건은 전환되지 않아야 한다.",
             slo="경계 오판 0건"),
        dict(title="초과 비율이 높으면 우선순위가 올라간다",
             given="90일 초과 속성 비율이 25%인 상태에서",
             when="스캔이 완료되면",
             then="재확인 큐 우선순위가 상향되고 알림이 발송된다.",
             slo="임계 20% (REQ-NF-008)"),
        dict(fail=True, title="Cron 실행 실패가 조용히 넘어가지 않는다",
             given="스캔 중 예외가 발생한 상태에서",
             when="실행이 종료되면",
             then="실패가 기록되고 마지막 성공 시각이 갱신되지 않는다. `REL-001` 이 24시간 초과를 감지해 알린다.",
             slo="무증상 중단 0건 (R-T3)"),
    ],
    constraints=[
        ("기술 제약", "**D-05 · REQ-TEC-013** 상시 스케줄러를 두지 않는다. Vercel Cron → Route Handler로만 구현하고 외부 호출을 거부한다."),
        ("신뢰성", "**REQ-NF-008** 90일 초과 시 경고 표기 필수. 스캔은 매일 1회 실행되고 **실행 실패 시 알림**이 발송되어야 한다."),
        ("성능", "야간 배치이므로 사용자 요청 경로와 분리된다. 다만 대량 갱신이 커넥션 풀을 잠식하지 않도록 배치 크기를 제한한다 (R-T2)."),
    ],
    external="없음",
)

SPECS["MCK-001"] = dict(
    purpose="UI 작업이 백엔드 완성을 기다리지 않게 한다. '빈 화면 금지'·'근거 없는 후보 반환 금지'가 "
            "요구하는 **실패·경계 상태**는 실제 데이터로 만들기 어려워 픽스처가 있어야 개발·검증할 수 있다.",
    seq="해당 없음",
    breakdown=[
        "`src/mocks/` 생성 — 픽스처는 `CTR-001` · `CTR-002` 계약 타입을 그대로 만족해야 한다",
        "**Top-3 픽스처** — 정상 3건 / 근거 4항목 중 하나 누락 / 후보 2건 이하 / 후보 0건",
        "**파싱 픽스처** — 캐시 히트 / 결정론 성공 / LLM 폴백 / 파싱 실패(구조화 필터 전환)",
        "**대화방·제안 픽스처** — 제안 0건 / 1건 / 5건 / 마감 경과 / 소환 0곳 미개시",
        "**결제·웹훅 픽스처** — 승인 / 거절 / 중복 수신(멱등) / 환불 / 노쇼",
        "Mock 모드 스위치 — 환경 변수로 활성화하며 **로컬·프리뷰에서만** 동작. 프로덕션에서는 강제 비활성",
        "타입 계약 테스트 — 픽스처가 계약 스키마를 통과하는지 (계약 변경 시 픽스처가 먼저 깨지게)",
        "단위 테스트 — 프로덕션 환경에서 Mock 모드가 켜지지 않는지",
    ],
    scenarios=[
        dict(title="픽스처가 계약 스키마를 만족한다",
             given="`CTR-001` · `CTR-002` 의 스키마가 정의된 상태에서",
             when="전체 픽스처를 검증하면",
             then="모두 통과한다. 계약이 바뀌면 픽스처 테스트가 먼저 실패해 불일치를 조기에 드러낸다.",
             slo="불일치 0건"),
        dict(title="실패·경계 상태를 픽스처로 재현할 수 있다",
             given="Mock 모드가 활성화된 상태에서",
             when="근거 누락·제안 0건·웹훅 중복 시나리오를 요청하면",
             then="해당 상태가 그대로 재현되어 UI가 실패 흐름을 개발·검증할 수 있다."),
        dict(fail=True, title="프로덕션에서 Mock 모드가 켜지지 않는다",
             given="프로덕션 배포 환경에서",
             when="Mock 모드 환경 변수를 설정해도",
             then="스위치가 강제 비활성되고 실제 데이터 경로만 동작한다. 가짜 데이터가 사용자에게 노출되어서는 안 된다.",
             slo="프로덕션 Mock 응답 0건"),
    ],
    constraints=[
        ("기술 제약", "**C-TEC-002** 별도 Mock 서버를 세우지 않는다. 픽스처는 앱 내부 모듈로 두고 환경 변수로만 전환한다."),
        ("보안", "픽스처에 실제 매장·이용자 데이터를 넣지 않는다. 전부 합성 데이터여야 한다."),
        ("품질", "픽스처는 **정상보다 실패·경계를 더 많이** 담는다 — 그쪽이 실제 데이터로 만들기 어려운 쪽이기 때문이다."),
    ],
    dod_extra=["픽스처가 '빈 화면 금지'가 요구하는 실패 상태 전부를 재현하는가?"],
    external="없음",
)

SPECS["QRY-001"] = dict(
    purpose="질의의 70% 이상을 LLM 호출 없이 해석해 응답 시간과 추론 비용을 동시에 잡는다. "
            "이 흡수율이 무너지면 REQ-NF-001a와 REQ-NF-013이 함께 무너진다(ADR-T02).",
    sdd="41-query-service--조건을-해석하는-부품", seq="SD-01 조건 검색 → Top-3 반환",
    breakdown=[
        "조건 카테고리 사전 구축 — 가격·메뉴·상황 속성(1인석·소음·체류)·단체 조건을 항목별로 등재",
        "결정론 파서 구현 — 사전 매칭 + 규칙 기반. 반환 타입은 `CTR-001` 의 `ConditionSet`",
        "정규화 규칙 — 어순 차이·조사 제거·동의어 매핑으로 같은 질의를 같은 키로 수렴",
        "파싱 캐시 구현 — `정규화 질의 → ConditionSet`. `use cache` 계층(`DAT-006`) 위에 얹는다",
        "흡수율 계측 — 캐시 히트 / 결정론 성공 / 미스를 구분해 `parse_path` 로 적재",
        "미매칭 질의 로그 수집 — 사전 확장의 재료 (SRS §7.2)",
        "성능 예산 확인 — 정규화·캐시 조회 ≤ 20ms, 결정론 파싱 ≤ 30ms",
        "단위 테스트 — 사전 등재 표현 적중, 캐시 히트 시 재계산 없음, 예산 초과 감지",
    ],
    scenarios=[
        dict(title="사전에 있는 조건이 LLM 없이 해석된다",
             given="'인당 2만원 이하 평양냉면 혼자' 처럼 사전 등재 표현으로 구성된 질의가 주어지고",
             when="결정론 파서가 실행되면",
             then="LLM 호출 없이 `ConditionSet` 이 반환된다.",
             slo="결정론 파싱 ≤ 30ms · 흡수율 ≥ 70% (REQ-FUNC-004)"),
        dict(title="동일 질의가 캐시로 재사용된다",
             given="이전에 LLM으로 해석된 질의가 캐시에 적재된 상태에서",
             when="어순만 다른 같은 의미의 질의가 들어오면",
             then="정규화 후 캐시 히트로 즉시 반환되고 LLM을 재호출하지 않는다.",
             slo="캐시 히트율 ≥ 60% · 동일 질의 LLM 재호출 0건 (REQ-NF-002b)"),
        dict(fail=True, title="사전에 없는 표현은 해석하지 않고 넘긴다",
             given="사전에 없는 상황어가 포함된 질의가 주어지고",
             when="결정론 파서가 실행되면",
             then="**추측하지 않고** 미해석으로 표시해 LLM 폴백(`QRY-002`)으로 넘긴다. 잘못 해석한 조건을 만들어내면 안 된다.",
             slo="오해석으로 인한 불일치 신고 기여 0건"),
        dict(fail=True, title="흡수율이 목표에 못 미치면 드러난다",
             given="`parse_path` 계측이 동작하는 상태에서",
             when="일간 분포를 집계하면",
             then="LLM 경로 비율이 40%를 넘으면 알림이 발송된다. 비용·지연이 동시에 악화되는 신호다.",
             slo="LLM 호출 비율 일간 ≤ 40% (SRS §10.3)"),
    ],
    constraints=[
        ("성능", "**REQ-NF-001a** 결정론 경로 p95 ≤ 1,000ms. 이 태스크의 예산은 정규화·캐시 20ms + 파싱 30ms."),
        ("비용", "**REQ-NF-013** 세션당 추론 비용 ≤ 12원. 결정론 흡수와 파싱 캐시가 1차 방어선이다."),
        ("기술 제약", "**D-03 · REQ-TEC-015** 파싱 캐시도 외부 캐시 서버 없이 `use cache` 로 구현한다."),
        ("설계 원칙", "**판정하지 않는다**(§6.3-4) — 사전에 없는 표현을 억지로 매핑하지 않고 폴백에 넘긴다."),
    ],
    dod_extra=["Phase 0 실제 질의 로그로 흡수율을 실측했는가? (ASM-T1 검증)"],
    external="없음",
)

SPECS["QRY-002"] = dict(
    purpose="모든 AI 호출을 한 곳으로 모아 타임아웃·재시도·토큰 상한·스키마 검증을 강제한다. "
            "호출 지점이 흩어지면 이 규약이 전부 무너진다(ADR-T10).",
    sdd="41-query-service--조건을-해석하는-부품", seq="SD-01 조건 검색 → Top-3 반환",
    breakdown=[
        "Vercel AI SDK 설치 — `ai` 패키지 및 `@ai-sdk/google` 프로바이더",
        "`src/lib/ai.ts` 단일 진입점 작성 — 모든 AI 호출이 이 모듈을 경유 (REQ-TEC-008)",
        "모델 지정을 **환경 변수로만** — `google(process.env.AI_MODEL_PARSE!)`. 코드에 모델명 상수를 두지 않는다 (D-07 · REQ-TEC-009)",
        "구조화 출력 구현 — `generateText({ model, output: Output.object({ schema }), prompt })`. 스키마는 `CTR-001` 의 `ConditionSet`",
        "타임아웃 — `abortSignal` 로 **2,000ms 상한**. 초과 시 구조화 필터 폴백",
        "재시도 — 최대 1회. 재시도가 REQ-NF-001b 예산을 넘기면 폴백",
        "출력 토큰 상한 — `AI_MAX_OUTPUT_TOKENS` 적용 (비용 1차 방어)",
        "스키마 검증 실패 처리 — **검증되지 않은 응답을 절대 사용하지 않고** 결정론 폴백으로 전환 (REQ-TEC-010)",
        "토큰 사용량 계측 — `ANA-007` 이 집계할 수 있게 적재",
        "단위 테스트 — 타임아웃 동작, 검증 실패 시 폴백, 모델 환경 변수 교체",
    ],
    scenarios=[
        dict(title="구조화 출력이 스키마 검증을 통과한다",
             given="결정론 파서가 해석하지 못한 질의가 주어지고",
             when="Gemini 폴백이 호출되면",
             then="`Output.object` 로 `ConditionSet` 형태가 반환되고 스키마 검증을 통과한다.",
             slo="LLM 경로 p95 ≤ 2,500ms (REQ-NF-001b)"),
        dict(title="모델을 코드 변경 없이 교체할 수 있다",
             given="`AI_MODEL_PARSE` 환경 변수가 설정된 상태에서",
             when="값을 다른 모델 ID로 바꾸고 재배포하면",
             then="**코드가 한 줄도 바뀌지 않고** 동작하며 파싱 결과 스키마가 동일하게 유지된다.",
             slo="코드 변경 0줄 (REQ-TEC-009)"),
        dict(fail=True, title="타임아웃 시 폴백으로 열화한다",
             given="Gemini 응답이 2,000ms를 초과하는 상태에서",
             when="`abortSignal` 이 발동하면",
             then="호출을 중단하고 구조화 필터 폴백으로 전환한다. 빈 화면이 나오지 않아야 한다.",
             slo="빈 화면 노출 0건 (§6.3-6)"),
        dict(fail=True, title="스키마 검증 실패 응답을 사용하지 않는다",
             given="LLM이 스키마에 맞지 않는 응답을 반환한 상태에서",
             when="검증이 실패하면",
             then="해당 응답을 **폐기**하고 결정론 폴백으로 전환한다. 검증되지 않은 값이 하류로 흘러가면 안 된다.",
             slo="미검증 응답 사용 0건 (REQ-TEC-010)"),
    ],
    constraints=[
        ("성능", "**REQ-NF-001b** LLM 경로 p95 ≤ 2,500ms. 타임아웃 2,000ms + 폴백 여유로 구성한다."),
        ("비용", "**REQ-NF-013** 세션당 ≤ 12원. 출력 토큰 상한 · 파싱 캐시 · 결정론 흡수 3중 방어. 일간 18원 초과 시 알림."),
        ("보안", "**REQ-NF-010** 프롬프트에 개인 식별 정보를 넣지 않는다 — 조건 문장만 전달하고 사용자 ID·좌표·연락처를 포함하지 않는다 (`SEC-002` 가 검증)."),
        ("기술 제약", "**C-TEC-005** 자체 AI 서버 없이 Vercel AI SDK로 외부 API를 호출한다. **C-TEC-006** Gemini 기본 · SDK 표준 인터페이스 준수. "
                   "**REQ-TEC-008** 모든 호출은 `src/lib/ai.ts` 경유."),
        ("외부 의존", "**LIM-T06** AI 응답 지연은 우리 코드로 줄일 수 없다. 타임아웃·폴백·캐시가 유일한 방어다."),
    ],
    dod_extra=["환경 변수만 바꿔 다른 모델로 동작하는 것을 실제로 확인했는가? (REQ-TEC-009 검증 시나리오)"],
    external="**DEP-T3** Google Gemini API 키 및 쿼터 — 미확보 시 결정론 경로로만 동작",
)

SPECS["QRY-005"] = dict(
    purpose="2단 파싱을 하나의 Server Action으로 오케스트레이션하고, 어느 경로로 처리됐는지를 "
            "`parse_path` 로 남긴다. 이 태그가 없으면 REQ-NF-001a와 001b를 분리 측정할 수 없다.",
    sdd="41-query-service--조건을-해석하는-부품", seq="SD-01 조건 검색 → Top-3 반환 · SD-02 자연어 파싱 실패 → 구조화 필터 폴백",
    breakdown=[
        "`app/actions/query.ts` 에 `submitQuery` 작성 (`'use server'`)",
        "입력 검증 — `CTR-001` 의 Zod 스키마 적용 (함수 첫 줄)",
        "인가 확인 — `assertActor(...)` 호출 (Server Action은 공개 엔드포인트와 동등)",
        "오케스트레이션 순서 — ① 파싱 캐시 조회 ② 결정론 파서 ③ Gemini 폴백 ④ 실패 시 구조화 필터 UI 전환",
        "LLM 성공 시 파싱 캐시 적재",
        "**`parse_path` 태깅** — `cache` / `deterministic` / `llm` 중 하나를 결과에 부착",
        "계측 적재 — `after()` 로 `query_committed`(`parse_ms` · `parse_status` 포함) 적재. 응답 지연 0 (ADR-T07)",
        "결과 경로 반환 및 캐시 무효화 필요 시 `revalidateTag`",
        "단위 테스트 — 3경로 분기, 인가 미확인 시 거부, `parse_path` 값 정확성",
    ],
    scenarios=[
        dict(title="세 경로가 각각 태깅된다",
             given="캐시 히트 · 결정론 성공 · LLM 폴백 세 상황이 각각 주어지고",
             when="`submitQuery` 가 실행되면",
             then="`parse_path` 가 `cache` / `deterministic` / `llm` 으로 정확히 구분되어 적재된다. 이 값으로 경로별 p95가 분리 측정된다."),
        dict(title="계측이 응답을 지연시키지 않는다",
             given="계측 적재가 `after()` 로 구현된 상태에서",
             when="액션이 실행되면",
             then="이벤트 적재는 응답 이후에 수행되고 사용자 응답 시간에 포함되지 않는다.",
             slo="계측으로 인한 지연 0ms (ADR-T07)"),
        dict(fail=True, title="파싱 실패 시 구조화 필터로 전환한다",
             given="사전에 없는 표현이고 LLM도 스키마 검증에 실패한 상태에서",
             when="액션이 완료되면",
             then="빈 화면 대신 구조화 필터 UI 경로를 반환하고 **해석하지 못한 표현을 그대로 표기**한다.",
             slo="파싱 실패율 ≤ 1.5% · 빈 화면 노출 0건 (US-1 AC5)"),
        dict(fail=True, title="인가되지 않은 호출이 거부된다",
             given="Server Action이 외부에서 직접 호출된 상태에서",
             when="인가 확인이 실패하면",
             then="`{ ok: false }` 로 거부하고 하류 로직을 실행하지 않는다. 스택이 노출되어서는 안 된다."),
    ],
    constraints=[
        ("성능", "**REQ-NF-001a** 결정론 경로 p95 ≤ 1,000ms / **REQ-NF-001b** LLM 경로 p95 ≤ 2,500ms — `parse_path` 로 분리 측정한다."),
        ("안정성", "**REQ-NF-007** 최종 파싱 실패율 ≤ 1.5%, 실패 시 구조화 필터로 열화."),
        ("기술 제약", "**C-TEC-002** Server Action은 POST 전용이며 GET 캐시가 없다(**LIM-T04**). 캐시가 필요한 읽기는 RSC로 둔다."),
        ("계측 필수", "`top3_rendered` 와 `query_committed` 에 `parse_path` 를 반드시 포함한다 — 없으면 성능 요구사항을 판정할 수 없다."),
    ],
    external="없음",
)

SPECS["QRY-007"] = dict(
    purpose="인당 예상가를 **범위로** 제시하고 예산 초과분을 요약으로 밀어낸다. 가격 데이터가 "
            "부실한 매장을 후보에서 빼지 않고 '판정 대상 아님'으로 다루는 것이 이 태스크의 핵심이다.",
    sdd="41-query-service--조건을-해석하는-부품", seq="SD-01 조건 검색 → Top-3 반환",
    breakdown=[
        "`PerPersonEstimator` 구현 — `PriceProfile` 에서 `perPersonLow`/`High` 범위 산출",
        "`isEstimable()` 공개 — `sampleSize < 5` 또는 `verifiedAt` 90일 초과면 **false**",
        "범위 폭 검증 — ±20% 초과 시 표기하지 않고 표본 추가 수집 큐로 넘김",
        "`PricePlanFilter` 구현 — 예산 상한 초과 매장을 기본 결과에서 제외하고 '예산 초과 N곳' 요약 생성",
        "**판정 제외 경로** — `isEstimable()` 이 false면 '가격 확인 필요'로 표기하고 예산 필터 판정 대상에서 뺀다 (후보에서 빼지 않는다)",
        "`app/actions/query.ts` 에 `submitPriceFeedback` 작성 — 실제 결제액 입력 → 편차 기록 → 다음 표기에 반영",
        "오차 계측 — `price_feedback` 이벤트 적재 (`ANA-002` 가 ±10% 내 비율 집계)",
        "단위 테스트 — 표본 5건 경계, 범위 폭 ±20% 경계, 예산 초과 오탐률",
    ],
    scenarios=[
        dict(title="인당 예상가가 범위로 표기된다",
             given="이용자가 인당 예산 상한을 입력한 상태에서",
             when="후보 목록을 요청하면",
             then="모든 후보 카드에 인당 예상가와 오차 범위가 표시된다.",
             slo="표기율 100% · 범위 폭 ≤ ±20% (US-1 AC1)"),
        dict(title="예산 초과 매장이 요약으로 밀린다",
             given="예산 상한이 2만원이고 초과 매장이 존재하면",
             when="필터가 적용되면",
             then="해당 매장은 기본 결과에서 제외되고 '예산 초과 N곳'으로만 요약된다.",
             slo="오탐률 ≤ 3% (US-1 AC2)"),
        dict(fail=True, title="가격 데이터가 부실하면 판정하지 않는다",
             given="`sampleSize < 5` 이거나 가격이 90일 초과 미갱신인 매장이 후보에 포함되고",
             when="예산 필터가 실행되면",
             then="예상가 대신 **'가격 확인 필요'** 로 표기하고 **판정 대상에서 제외**한다. 후보에서 빼면 커버리지 부족이 '결과 없음'으로 보인다.",
             slo="예상가 오표기 0건 · 제외 사유 표기율 100% (US-1 AC6)"),
        dict(fail=True, title="범위 폭이 기준을 넘으면 표기하지 않는다",
             given="추정 범위 폭이 ±20%를 넘는 상태에서",
             when="표기를 시도하면",
             then="표기하지 않고 표본 추가 수집 큐로 넘긴다. 신뢰할 수 없는 범위를 내보내면 안 된다."),
    ],
    constraints=[
        ("성능", "**REQ-NF-001a** 예산 안에서 동작해야 한다. 추정은 색인 조회 결과 위의 순수 계산으로 두고 추가 왕복을 만들지 않는다."),
        ("품질", "**보조 5** 예상가 오차 ±10% 내 비율 80%. `submitPriceFeedback` 의 편차 기록이 이 지표를 만든다."),
        ("설계 원칙", "**단일 값 금지** — `PriceProfile` 은 범위만 저장한다(SRS §6.2). 평균 하나만 보여 주면 C2의 신뢰가 형성되지 않는다."),
    ],
    external="없음",
)

SPECS["QRY-009"] = dict(
    purpose="메뉴명 하나로 그 메뉴를 하는 매장을 찾아준다. 3단 폴백으로 어느 단계에서도 "
            "'0건입니다'로 끝나지 않게 한다.",
    sdd="41-query-service--조건을-해석하는-부품", seq="SD-03 메뉴명 질의 → 유사 메뉴 → 반경 확대",
    breakdown=[
        "`DishQueryResolver` 구현 — 입력 메뉴명을 `canonicalKey` 로 정규화 후 색인 조회",
        "1단계 — 해당 `canonicalKey` 존재 시 매장 목록 반환 (업종 목록이 아니라 **매장** 목록)",
        "2단계 — 색인에 없고 유사 메뉴가 3건 이상이면 대체하고 **'유사 메뉴로 대체했다'를 명시**",
        "3단계 — 반경 1km 내 0건이면 **3km로 1회 자동 확대**하고 확대 사실 표기",
        "4단계 — 확대 후에도 0건이면 인접 카테고리 결과를 사유와 함께 반환",
        "폴백 단계를 `top3_rendered.substitute_used` · `radius_expanded` 속성으로 적재",
        "정답률 계측 — 메뉴 질의 정답 반환률 산출 근거 적재",
        "단위 테스트 — 4단계 분기 전부, 유사 메뉴 3건 경계, 반경 확대 1회 제한",
    ],
    scenarios=[
        dict(title="메뉴명 하나로 매장이 반환된다",
             given="이용자가 반경 1km 안에 있고 색인에 해당 메뉴가 있으면",
             when="검색창에 메뉴명만 입력하면",
             then="업종 목록이 아니라 그 메뉴를 취급하는 매장이 반환된다.",
             slo="정답 반환률 ≥ 92% · 첫 결과 p95 ≤ 1,000ms (US-2 AC1)"),
        dict(fail=True, title="색인에 없으면 유사 메뉴로 대체한다",
             given="메뉴 색인에 해당 메뉴가 없고 유사 메뉴가 3건 이상 존재하면",
             when="검색이 실행되면",
             then="'찾는 메뉴가 없어 유사 메뉴로 대체했다'를 명시하고 반환한다.",
             slo="빈 결과 반환률 ≤ 2% (US-2 AC2)"),
        dict(fail=True, title="반경 내 0건이면 자동 확대한다",
             given="반경 1km 안에 해당 메뉴도 유사 메뉴도 없고",
             when="검색이 실행되면",
             then="반경을 3km로 **1회** 자동 확대하고 확대 사실을 표기한다. 그래도 0건이면 인접 카테고리를 사유와 함께 반환한다.",
             slo="결과 0건 종료 0회 · 반경 확대 표기율 100% (US-2 AC4)"),
    ],
    constraints=[
        ("성능", "**REQ-NF-001a** 첫 결과 p95 ≤ 1,000ms. 메뉴명 질의는 LLM이 불필요하므로 **항상 결정론 경로**여야 한다."),
        ("안정성", "**REQ-NF-007** 빈 결과 반환률 ≤ 2%. 폴백 단계마다 대체 사실을 표기한다."),
        ("설계 원칙", "**빈 화면 금지**(§6.3-6) — 어느 단계에서도 '결과 없음'으로 종료하지 않는다."),
    ],
    external="없음",
)

SPECS["EVD-001"] = dict(
    purpose="후보마다 선정 이유·근거 속성·확인 일자·확인 주체 4항목을 조립하고 검증한다. "
            "이 4항목이 이 제품이 경쟁사와 갈리는 유일한 축이다.",
    sdd="43-evidence-service--ranking-service--근거와-순서", seq="SD-04 근거 표기와 90일 경고",
    breakdown=[
        "`EvidenceComposer` — 후보의 근거 속성으로 선정 이유 1줄 생성. 템플릿 우선, LLM은 보조 (비용 방어)",
        "`VerificationChecker.isComplete()` — 4항목(이유·속성·확인 일자·확인 주체) 전부 존재 여부 판정",
        "90일 경과 경고 — `verifiedAt` 초과 시 '확인 90일 경과'를 함께 표시. 노출은 계속하되 경고를 붙인다",
        "**판정형 문구 금지 필터** — 생성 문장에서 적합/부적합을 단정하는 표현을 제거 (§6.3-4)",
        "LLM 사용 시 `QRY-002` 의 `lib/ai.ts` 경유 및 프롬프트에 판정 금지 규칙 명시",
        "4항목 미충족 후보는 `null` 반환 — `RNK-001` 이 배제할 수 있게",
        "`evidence_complete` 이벤트 적재 — `has_*` 4개 플래그와 `stale_warning` 포함",
        "단위 테스트 — 4항목 조합 전수, 90일 경계, 판정형 어휘 제거",
    ],
    scenarios=[
        dict(title="근거 4항목이 모두 표시된다",
             given="Top-3가 반환된 상태에서",
             when="각 후보 카드를 보면",
             then="선정 이유 1줄 + 근거 속성 + 확인 일자 + 확인 주체가 모두 표시된다.",
             slo="4개 항목 표기율 100% (US-3 AC1)"),
        dict(fail=True, title="90일 초과 속성에 경고가 붙는다",
             given="어떤 속성이 90일 이상 갱신되지 않았고",
             when="그 속성이 후보 카드에 노출되면",
             then="'확인 90일 경과' 경고를 함께 표시한다. 경고 없이 노출되어서는 안 된다.",
             slo="경고 누락률 0% (US-3 AC2)"),
        dict(fail=True, title="판정형 문구가 제거된다",
             given="LLM이 '혼밥하기 좋습니다' 같은 판정형 문장을 생성한 상태에서",
             when="필터가 실행되면",
             then="판정 표현이 제거되고 **사실 속성만 남는다**. 기준이 사람마다 다른 조건을 시스템이 단정하지 않는다.",
             slo="판정형 문구 0건 (§6.3-4)"),
        dict(fail=True, title="4항목 미충족 후보는 근거를 만들지 않는다",
             given="확인 주체가 없는 속성만 가진 후보가 주어지고",
             when="근거 조립이 실행되면",
             then="`null` 을 반환해 `RNK-001` 이 후보에서 배제하게 한다. 불완전한 근거를 채워 넣어 내보내면 안 된다."),
    ],
    constraints=[
        ("성능", "**REQ-NF-001a** 근거 조립·랭킹 예산 ≤ 300ms. 후보별 조립을 **병렬**로 수행한다 (SD-01)."),
        ("비용", "**REQ-NF-013** 근거 문장은 템플릿 우선이고 LLM은 보조다. LLM 남용은 세션당 12원 예산을 깬다."),
        ("설계 원칙", "**판정하지 않는다**(§6.3-4) — 판단 재료만 제공한다. C4가 원한 것은 판정이 아니라 재료다.",),
        ("근거 구조", "**ADR-002** `Verification` 이 별도 엔터티라 확인 일자·주체를 속성마다 조회할 수 있다."),
    ],
    external="없음",
)

SPECS["EVD-004"] = dict(
    purpose="결정을 남에게 설명할 수 있는 형태로 내보낸다. 근거 4항목이 갖춰진 후보만 카드가 되며, "
            "이것이 C1의 Job('정한 이유를 눈에 보이게')을 해결하는 지점이다.",
    sdd="43-evidence-service--ranking-service--근거와-순서", seq="SD-05 공유 카드 생성",
    breakdown=[
        "`app/api/share-cards/[id]/route.ts` — GET, 공개·캐시 가능 (`CTR-002` 계약)",
        "`next/og` 로 OG 이미지 생성 — `UX-010` 의 비주얼 규격 적용",
        "**`assertFourItems()` 선행 검증** — 근거 4항목 누락 시 이미지를 만들지 않고 **400 반환**",
        "딥링크 생성 — 카드에서 원래 후보로 돌아올 수 있게",
        "캐시 헤더 설정 — 생성 p95 ≤ 3,000ms 달성 수단",
        "`share_card_created` 이벤트 적재 (`render_ms` · `channel`)",
        "단위 테스트 — 4항목 누락 시 400, 캐시 헤더 존재, 렌더 시간 예산",
    ],
    scenarios=[
        dict(title="근거를 담은 카드가 생성된다",
             given="이용자가 후보를 하나 선택하고",
             when="공유를 요청하면",
             then="선정 이유와 근거를 담은 카드가 이미지 또는 링크로 생성된다.",
             slo="생성 p95 ≤ 3,000ms · 공유 카드 생성률 ≥ 60% (US-3 AC3)"),
        dict(fail=True, title="근거 4항목이 없으면 카드를 만들지 않는다",
             given="근거 4항목 중 하나가 누락된 후보 id가 주어지고",
             when="공유 카드를 요청하면",
             then="이미지를 생성하지 않고 **400**을 반환한다. 빈 이미지나 플레이스홀더를 반환해서는 안 된다.",
             slo="근거 없는 카드 0건 (§6.3-2)"),
    ],
    constraints=[
        ("성능", "**REQ-FUNC-005** 생성 p95 ≤ 3,000ms. GET 캐시가 가능한 유일한 경로이므로 캐시 헤더를 반드시 설정한다."),
        ("기술 제약", "**C-TEC-002** 캐시 가능한 GET이므로 Server Action이 아니라 **Route Handler**로 구현한다(LIM-T04)."),
        ("설계 원칙", "카드는 **근거를 담는 그릇**이지 홍보물이 아니다. 판정형 문구가 카드에 들어가지 않아야 한다."),
    ],
    external="없음",
)

SPECS["EVD-005"] = dict(
    purpose="'조건이 달랐다'는 신고를 60초 안에 재확인 큐로 연결한다. 이 회로가 없으면 "
            "근거가 한 번 틀린 뒤에도 계속 같은 값으로 노출된다.",
    sdd="43-evidence-service--ranking-service--근거와-순서", seq="SD-09 불일치 신고 → 재확인 큐",
    breakdown=[
        "`app/actions/evidence.ts` 에 `reportMismatch` 작성 (`'use server'`)",
        "입력 검증 — `placeId` · `attributeKey` (`CTR-001` 스키마)",
        "`Verification.status = NEEDS_REVERIFY` 즉시 전환",
        "`ReverifyTask` 적재 — 사유 `USER_REPORT`, 우선순위는 사용자 신고가 배치보다 높게 (`DAT-010` 큐 규약)",
        "`mismatch_reported` 이벤트 적재 (`attribute_key` · `reverify_queued_at`)",
        "신고 후 해당 속성의 후보 카드 노출 정책 확인 — 경고 병기 또는 제외 판단",
        "단위 테스트 — 전환 지연, 중복 신고 처리, 큐 우선순위",
    ],
    scenarios=[
        dict(title="신고가 재확인 큐로 연결된다",
             given="방문이 완료된 상태에서",
             when="이용자가 '조건이 달랐다'를 신고하면",
             then="해당 속성의 확인 상태가 '재확인 필요'로 즉시 전환되고 큐에 적재된다.",
             slo="반영 지연 ≤ 60s (US-3 AC4)"),
        dict(fail=True, title="같은 속성을 반복 신고해도 큐가 폭주하지 않는다",
             given="이미 `NEEDS_REVERIFY` 인 속성이 있고",
             when="다른 이용자가 같은 속성을 신고하면",
             then="새 태스크를 중복 생성하지 않고 신고 횟수만 누적한다. 큐가 같은 항목으로 채워지면 안 된다."),
    ],
    constraints=[
        ("품질", "**보조 6** 방문 후 조건 불일치 신고율 ≤ 10%. 이 값이 높으면 근거 품질 문제이지 신고 기능 문제가 아니다."),
        ("성능", "전환은 60초 내. Server Action에서 동기 처리하고 계측만 `after()` 로 미룬다."),
        ("운영", "사용자 신고는 배치보다 **우선순위가 높다** — 실제 방문에서 확인된 오류이기 때문이다."),
    ],
    external="없음",
)

SPECS["RNK-001"] = dict(
    purpose="근거가 없는 후보를 배제하고 정확히 3개를 고른다. 근거 미충족 배제는 선택적 기능이 아니라 "
            "`select()` 의 일부이며, 밖에서 끌 수 있으면 규칙이 규칙이 아니게 된다.",
    sdd="43-evidence-service--ranking-service--근거와-순서", seq="SD-01 조건 검색 → Top-3 반환",
    breakdown=[
        "`TopThreeSelector.select()` 구현 — 내부에서 `excludeWithoutEvidence()` 를 **private으로** 호출",
        "후보 수 **3개 고정** — 페이지네이션 파라미터를 받지 않는다 (ADR-003)",
        "근거 충족 후보가 3개 미만이면 **있는 만큼 반환 + 커버리지 부족 안내**. 근거 없는 후보로 자리를 채우지 않는다",
        "근거 충족 후보가 0개면 폴백 경로(`QRY-009` 반경 확대·인접 카테고리)로 넘김",
        "`ComparisonAxisBuilder` — 비교 축 생성(인당가·대표메뉴·상황속성·확인일자)",
        "정렬 기준 — 조건 적합도. **광고·노출 순서 관련 필드를 정렬 키에 두지 않는다** (ADR-004)",
        "`top3_rendered` 이벤트 적재 (`candidate_count` · `latency_ms` · `parse_path` 전달)",
        "단위 테스트 — 근거 미충족 배제, 후보 0·1·2·3·5개 분기, 정렬 키에 가격·광고 필드 부재",
    ],
    scenarios=[
        dict(title="정확히 3개가 근거와 함께 반환된다",
             given="근거 4항목을 갖춘 후보가 5개 있고",
             when="Top-3 선정이 실행되면",
             then="정확히 3개가 반환되고 비교 축이 함께 제공된다. 페이지네이션 수단이 노출되지 않아야 한다.",
             slo="후보 수 3개 고정 (ADR-003)"),
        dict(fail=True, title="근거 없는 후보가 배제된다",
             given="근거 4항목 중 하나가 없는 후보가 포함된 집합이 주어지고",
             when="선정이 실행되면",
             then="해당 후보는 결과에서 제외된다. 배제는 `select()` 내부 동작이라 **플래그로 끌 수 없어야** 한다.",
             slo="근거 없는 후보 반환 0건 (§6.3-2)"),
        dict(fail=True, title="후보가 부족하면 채우지 않고 알린다",
             given="근거 충족 후보가 2개뿐인 상태에서",
             when="선정이 실행되면",
             then="2개만 반환하고 커버리지 부족을 안내한다. 근거 없는 후보로 3칸을 메우면 그 순간 경쟁사 화면과 같아진다."),
    ],
    constraints=[
        ("성능", "**REQ-NF-001a** 근거 조립·랭킹 합산 ≤ 300ms."),
        ("설계 원칙", "**후보 수 3개 고정**(ADR-003) — 되돌릴 수 있는 결정이지만 v0.1에서는 고정한다. **광고 미도입**(ADR-004) — 정렬 키에 노출 순서 관련 필드가 없다."),
        ("품질", "**보조 3** Top-3 내 선택률 45%. 후보를 늘리는 것이 아니라 근거를 붙이는 것으로 이 수치를 올린다."),
    ],
    external="없음",
)

SPECS["RNK-003"] = dict(
    purpose="Top-3를 화면에 스트리밍하고, 파싱이 실패했을 때 구조화 필터로 갈아탄다. "
            "LLM 경로에서도 500ms 안에 스켈레톤을 띄워 '빈 화면 금지'를 화면 수준에서 지킨다.",
    sdd="43-evidence-service--ranking-service--근거와-순서", seq="SD-01 조건 검색 → Top-3 반환 · SD-02 자연어 파싱 실패 → 구조화 필터 폴백",
    breakdown=[
        "`app/(search)/results/page.tsx` RSC 작성 — DB 직접 조회, API 왕복 없음",
        "Suspense 경계 배치 — **500ms 내 스켈레톤 렌더** (`UX-003` 규격)",
        "후보 카드 렌더 — `UX-006` 의 근거 4항목·90일 경고·예상가 범위·비교 축 반영",
        "구조화 필터 폴백 화면 — `UX-004` 규격. 해석하지 못한 표현을 그대로 표기",
        "빈·오류 상태 렌더 — `UX-004` 의 정의 적용. 어느 경우에도 빈 화면을 만들지 않는다",
        "`top3_rendered` 이벤트를 `after()` 로 적재 — `latency_ms` · `parse_path` 포함",
        "성능 — 클라이언트 번들 최소화, `next/image` · `next/font` 적용 (REQ-NF-004)",
        "단위·통합 테스트 — 스켈레톤 렌더 시각, 폴백 전환, 근거 4항목 표기",
    ],
    scenarios=[
        dict(title="결정론 경로가 1초 안에 렌더된다",
             given="결정론 파서가 조건을 해석한 상태에서",
             when="결과 페이지가 요청되면",
             then="Top-3가 근거와 함께 스트리밍 렌더된다.",
             slo="p95 ≤ 1,000ms (REQ-NF-001a)"),
        dict(title="LLM 경로에서도 즉시 스켈레톤이 뜬다",
             given="LLM 폴백이 진행 중인 상태에서",
             when="결과 페이지가 요청되면",
             then="500ms 내 스켈레톤이 렌더되고 조건 확정 후 결과가 채워진다.",
             slo="스켈레톤 ≤ 500ms · 전체 p95 ≤ 2,500ms (REQ-NF-001b)"),
        dict(fail=True, title="파싱 실패 시 구조화 필터 화면으로 전환된다",
             given="파싱이 최종 실패한 상태에서",
             when="결과 페이지가 렌더되면",
             then="빈 화면 대신 구조화 필터 UI가 렌더되고 해석하지 못한 표현이 표기된다.",
             slo="빈 화면 노출 0건 (US-1 AC5)"),
        dict(fail=True, title="결과가 0건이어도 화면이 비지 않는다",
             given="폴백까지 거쳐도 후보가 0건인 상태에서",
             when="페이지가 렌더되면",
             then="사유와 다음 행동(조건 완화·반경 확대)을 담은 상태 화면이 렌더된다."),
    ],
    constraints=[
        ("성능", "**REQ-NF-001a** ≤ 1,000ms · **REQ-NF-001b** ≤ 2,500ms · **REQ-NF-004** LCP ≤ 2.5s (4G)."),
        ("기술 제약", "**C-TEC-001 · 002** RSC에서 DB를 직접 조회한다. 내부 REST를 만들어 자기 자신을 `fetch` 하지 않는다(D-02)."),
        ("계층 분리", "이 태스크는 유형 `UI` 다 — **기능 구현(BE)과 분리**해 담당·리뷰 관점을 나눈다(원칙 P6)."),
        ("설계 원칙", "**빈 화면 금지**(§6.3-6)를 화면 수준에서 최종 보증하는 지점이다."),
    ],
    external="없음",
)

SPECS["RSV-001"] = dict(
    purpose="선택한 대상의 인원·메뉴·시간을 예약으로 그대로 승계해 **재입력을 0으로** 만든다. "
            "조건을 다시 묻는 순간 결정이 실행으로 이어지지 않는다.",
    sdd="45-reservation--payment-service", seq="SD-07 제안 선택 → 예약 승계 → 선결제",
    breakdown=[
        "`app/actions/reservation.ts` 에 `selectProposal` 작성",
        "`ProposalCarryOver` — 인원·메뉴 구성·시간을 `Reservation` 초안으로 승계",
        "`assertNoReentry()` — 승계 후 사용자가 다시 입력해야 하는 필드가 0개인지 검증",
        "`OrderAmountCalculator` — 주문량 기준 금액 산출. 순수 함수로 두어 테스트 가능하게",
        "선결제 적용 범위 판정 — **4인 이상 단체 제안에만** (§6.3-8)",
        "`Reservation.state = CARRIED_OVER → AWAITING_PAYMENT` 전이",
        "단위 테스트 — 승계 필드 누락률, 금액 산출 오류율, 4인 경계",
    ],
    scenarios=[
        dict(title="조건이 자동 승계된다",
             given="이용자가 대상 하나를 선택하고",
             when="예약 화면으로 넘어가면",
             then="인원·메뉴 구성·시간이 자동 승계된다.",
             slo="재입력 필드 0개 · 승계 누락률 ≤ 0.5% (US-4 AC1)"),
        dict(title="주문량 기준으로 금액이 산출된다",
             given="메뉴 구성이 포함된 선택이 주어지고",
             when="금액 산출이 실행되면",
             then="주문량 기준 금액이 계산된다.",
             slo="금액 산출 오류율 ≤ 0.3% (US-4 AC2)"),
        dict(fail=True, title="승계에 실패하면 결제로 넘어가지 않는다",
             given="필수 승계 항목이 누락된 상태에서",
             when="`assertNoReentry()` 가 실패하면",
             then="예약 초안을 만들지 않고 사유를 반환한다. 재입력을 요구하는 화면으로 넘기지 않는다."),
        dict(fail=True, title="4인 미만은 선결제 대상이 아니다",
             given="3인 예약이 주어지고",
             when="선결제 적용을 판정하면",
             then="선결제 대상에서 제외된다. 소규모에까지 선결제를 걸면 전환율이 깎인다(R4).",
             slo="§6.3-8 위반 0건"),
    ],
    constraints=[
        ("품질", "**US-4 AC1** 재입력 필드 0개 · 승계 누락률 ≤ 0.5%. **AC2** 금액 산출 오류율 ≤ 0.3%."),
        ("설계 원칙", "**선결제는 4인 이상 단체 제안만**(§6.3-8) — R4(선결제가 전환을 깎음)의 완화 장치다."),
        ("기술 제약", "**C-TEC-002** 변경 작업이므로 Server Action으로 구현한다."),
    ],
    external="없음",
)

SPECS["RSV-003"] = dict(
    purpose="결제를 PG에 위탁하고 결과를 웹훅으로 받는다. **중복 수신에도 상태가 변하지 않는 멱등성**이 "
            "이 태스크의 핵심이며, 서버리스에서 웹훅은 반드시 중복 도착한다고 가정해야 한다.",
    sdd="45-reservation--payment-service", seq="SD-07 제안 선택 → 예약 승계 → 선결제 · SD-08 취소·환불 / 노쇼 판정·정산",
    breakdown=[
        "`app/actions/reservation.ts` 에 `requestPayment` 작성 — PG 결제 세션 생성",
        "`PgDelegationClient` — 승인 요청. **카드 정보를 수신·보관하지 않는다** (PCI 부담을 PG로)",
        "`app/api/webhooks/pg/route.ts` — POST. **서명 검증을 본문 파싱보다 먼저** 수행 (`CTR-002` 계약)",
        "**멱등 처리** — `Payment.idempotencyKey` unique 제약으로 중복 수신 시 최초 결과를 그대로 반환",
        "상태 전이 — `AWAITING_PAYMENT → CONFIRMED`(승인) / `FAILED`(거절)",
        "매장 확정 통보 — 승인 후 30초 내",
        "암호화 정책 적용 — 저장 AES-256 · 전송 TLS 1.3 (REQ-NF-011)",
        "단위·통합 테스트 — 서명 불일치 거부, 중복 웹훅 멱등, 통보 지연",
    ],
    scenarios=[
        dict(title="승인 후 매장에 확정이 통보된다",
             given="결제 승인이 완료되고",
             when="웹훅이 도착하면",
             then="예약이 `CONFIRMED` 로 전이되고 매장에 확정이 통보된다.",
             slo="통보 지연 ≤ 30s (US-4 AC2)"),
        dict(fail=True, title="중복 웹훅에도 상태가 변하지 않는다",
             given="동일한 멱등 키의 웹훅이 이미 처리된 상태에서",
             when="같은 키로 다시 도착하면",
             then="재처리하지 않고 최초 결과를 반환한다. `Payment` 상태가 바뀌어서는 안 된다.",
             slo="중복 처리 0건 (SRS §9.2)"),
        dict(fail=True, title="서명 없는 웹훅이 본문 파싱 전에 거부된다",
             given="서명 헤더가 없거나 불일치하는 요청이 도착하고",
             when="핸들러가 실행되면",
             then="본문을 파싱하기 전에 거부한다. 결제 API 오류율에 5xx로 집계되지 않아야 한다.",
             slo="결제 API 5xx ≤ 0.1% (REQ-NF-006)"),
        dict(fail=True, title="카드 정보가 저장되지 않는다",
             given="결제 흐름 전체가 실행된 뒤",
             when="`Payment` 테이블을 검사하면",
             then="카드 정보 컬럼이 존재하지 않고 `pgTransactionId` 만 보관된다.",
             slo="카드 정보 저장 0건 (REQ-NF-011)"),
    ],
    constraints=[
        ("보안", "**REQ-NF-011** PCI-DSS 준수 PG 위탁 · 카드 정보 비보관 · 저장 AES-256 · 전송 TLS 1.3 · 웹훅 서명 검증 필수."),
        ("안정성", "**REQ-NF-006** 결제 API 5xx ≤ 0.1%. 서명·검증 실패는 4xx다."),
        ("기술 제약", "**C-TEC-002** 결제 요청은 Server Action, 결과 수신은 Route Handler 웹훅 — 외부에서 들어오기 때문이다(SRS §15-9)."),
        ("멱등성", "서버리스에서 웹훅 중복은 예외가 아니라 기본 가정이다. DB unique 제약으로 보장하고 애플리케이션 로직에 의존하지 않는다."),
    ],
    external="**DEP-T4** PG 계약 — 서명 알고리즘·멱등 키 필드명 확정 필요. 미확정 시 착수 불가",
)

SPECS["RSV-005"] = dict(
    purpose="취소는 환불로, 노쇼는 매장 정산으로 끝낸다. **돈이 가는 방향이 반대**인 두 흐름을 "
            "한 곳에서 다뤄 판정 기준이 어긋나지 않게 한다.",
    sdd="323-reservation--payment--예약과-결제", seq="SD-08 취소·환불 / 노쇼 판정·정산",
    breakdown=[
        "`app/actions/reservation.ts` 에 `cancelReservation` 작성",
        "취소 가능 시점 판정 — **방문 2시간 전**까지",
        "전액 환불 — `PgDelegationClient.refundFull()`. 24시간 내 처리",
        "매장 즉시 통보 — 취소 접수 시점",
        "`app/api/cron/noshow/route.ts` — 매 10분. `CRON_SECRET` 인증",
        "**노쇼 판정 순서** — ① 방문 확인 존재? ② 취소 이력? ③ **매장 측 수용 불가 사유?** ④ 없으면 노쇼",
        "노쇼 시 선결제액 매장 정산 · `noshow_flagged` 이벤트 적재",
        "주간 노쇼율 8% 초과 시 알림 및 `AGR`·`MCH` 신규 노출 중단 신호 (`ANA-005` 연동)",
        "단위 테스트 — 2시간 경계, 매장 귀책 분기, 오판정률",
    ],
    scenarios=[
        dict(title="2시간 전 취소가 전액 환불된다",
             given="이용자가 예약 2시간 전까지 취소하고",
             when="취소가 접수되면",
             then="선결제 금액을 전액 환불하고 매장에 즉시 알린다.",
             slo="환불 처리 ≤ 24h · 환불 실패율 ≤ 0.5% (US-4 AC3)"),
        dict(fail=True, title="방문 확인이 없으면 노쇼로 기록된다",
             given="예약 시각이 지나고 방문 확인이 없는 상태에서",
             when="노쇼 판정 Cron이 실행되면",
             then="노쇼로 기록하고 선결제 금액을 매장에 정산한다.",
             slo="선택 제안 노쇼율 ≤ 5% · 오판정률 ≤ 1% (US-4 AC4)"),
        dict(fail=True, title="매장 귀책은 노쇼가 아니다",
             given="매장 측 수용 불가 사유가 기록된 상태에서",
             when="노쇼 판정이 실행되면",
             then="노쇼로 기록하지 않고 이용자에게 환불한다. 매장이 문을 닫았는데 이용자를 노쇼로 기록하면 오판정률 1%를 지킬 수 없다."),
        dict(fail=True, title="노쇼율이 임계를 넘으면 제안이 중단된다",
             given="주간 노쇼율이 8%를 넘은 상태에서",
             when="집계가 완료되면",
             then="알림이 발송되고 에이전트 제안의 **신규 노출이 중단**된다. 가맹점은 노쇼 한 번으로 이탈한다.",
             slo="임계 8% (SRS §10.3)"),
    ],
    constraints=[
        ("품질", "**US-4 AC3** 환불 ≤ 24h · 실패율 ≤ 0.5%. **AC4** 노쇼율 ≤ 5% · 오판정률 ≤ 1%."),
        ("기술 제약", "**D-05 · REQ-TEC-013** 노쇼 판정은 Vercel Cron → Route Handler. 상시 스케줄러를 두지 않는다."),
        ("설계 원칙", "**매장 귀책 분기를 노쇼 판정 앞에 둔다** — 오판정률 목표를 지키는 유일한 방법이다(SDD FC-05)."),
        ("게이트", "노쇼율 8% 초과는 **게이트 2 미달** 신호이며 F7·F8을 v0.2로 연기하는 판단 근거가 된다."),
    ],
    external="**DEP-T4** PG 환불 API",
)

SPECS["MCH-001"] = dict(
    purpose="가맹 콘솔의 인증과 MFA를 세운다. 콘솔은 매장의 영업 정보와 정산이 걸린 화면이라 "
            "RLS(`DAT-008`)와 짝을 이루는 인증 주체가 먼저 있어야 한다.",
    sdd="44-agent-room-service--merchant-console-service--공급-측", seq="해당 없음",
    breakdown=[
        "Supabase Auth 연동 — 가맹 사용자 스키마 및 매장 매핑",
        "**MFA(TOTP) 적용** — 콘솔 로그인에 2단계 인증 필수 (REQ-NF-012)",
        "세션 검증 헬퍼 — `proxy.ts`(`INF-008`)의 인증 훅과 연결",
        "RLS 정책 연결 — 인증 주체가 자기 매장 데이터만 접근하도록 `DAT-008` 정책과 결합",
        "감사 로그 연동 — 콘솔 조회를 `audit_logs` 에 기록",
        "복구 흐름 — MFA 기기 분실 시 절차 정의",
        "단위 테스트 — MFA 미완료 세션 차단, 타 매장 데이터 접근 거부",
    ],
    scenarios=[
        dict(title="MFA를 통과해야 콘솔에 접근한다",
             given="가맹 계정이 등록된 상태에서",
             when="비밀번호만으로 로그인을 시도하면",
             then="2단계 인증을 요구하고, 완료 전까지 콘솔 데이터가 반환되지 않는다.",
             slo="MFA 미적용 접근 0건 (REQ-NF-012)"),
        dict(fail=True, title="다른 매장 데이터에 접근할 수 없다",
             given="가맹 A로 인증된 세션이 있고",
             when="가맹 B의 프로필·제안을 조회하면",
             then="RLS가 거부한다. 애플리케이션 코드가 아니라 **DB 정책이 최종 방어선**이다.",
             slo="비인가 반환 0행"),
    ],
    constraints=[
        ("보안", "**REQ-NF-012** 가맹 콘솔 2FA · 내부 조회 전량 감사 로그. Supabase Auth MFA를 사용한다."),
        ("기술 제약", "**C-TEC-003** 인증 정책이 Supabase 기능 범위에 제한된다(SRS §15-8)."),
        ("의존", "`DAT-008` 의 RLS 정책과 짝을 이룬다 — 인증만 있고 정책이 없으면 격리가 성립하지 않는다."),
    ],
    external="**DEP-T2** Supabase Auth 활성화",
)

SPECS["MCH-002"] = dict(
    purpose="가맹 콘솔을 검색 화면과 **레이아웃 수준에서 분리**한다. 같은 앱 안에 있으므로 "
            "라우트 그룹으로 나누지 않으면 두 사용자 경험이 섞인다.",
    sdd="12-컴포넌트-다이어그램--시스템-내부-구조", seq="해당 없음",
    breakdown=[
        "`app/(merchant)/` 라우트 그룹 생성",
        "`app/(merchant)/layout.tsx` — 검색 화면과 독립된 전용 레이아웃 (`UX-013` 규격)",
        "인증 게이트 — `MCH-001` 의 세션 검증을 레이아웃 진입에서 확인",
        "**설정 화면 3개 이하** 구조 설계 — 사장이 직접 조작하므로 화면 수가 곧 이탈률",
        "네비게이션 — 검색 화면으로의 전환 경로를 두지 않는다 (역할이 다름)",
        "shadcn/ui 컴포넌트만 사용 (`INF-002` 인벤토리)",
        "단위 테스트 — 미인증 진입 차단, 설정 화면 수 상한",
    ],
    scenarios=[
        dict(title="콘솔이 독립 레이아웃으로 렌더된다",
             given="인증된 가맹 세션이 있고",
             when="콘솔에 진입하면",
             then="검색 화면과 다른 전용 레이아웃이 렌더되고 설정 화면이 3개를 넘지 않는다.",
             slo="설정 화면 ≤ 3개 (US-5 AC1)"),
        dict(fail=True, title="미인증 진입이 차단된다",
             given="세션이 없는 상태에서",
             when="`/(merchant)` 경로에 진입하면",
             then="레이아웃 렌더 전에 차단되고 콘솔 데이터가 응답에 포함되지 않는다."),
    ],
    constraints=[
        ("사용자 특성", "**P5는 조작 숙련도가 낮다**(SRS §8) — 화면 수 상한이 곧 이탈률 방어다."),
        ("기술 제약", "**C-TEC-004 · D-08** shadcn/ui 컴포넌트를 자체 구현하지 않는다."),
        ("계층 분리", "이 태스크는 유형 `UI` 다 — 기능 구현(BE)과 분리해 추적한다(원칙 P6)."),
    ],
    external="없음",
)

SPECS["MCH-003"] = dict(
    purpose="매장이 '내 말로' 자기를 소개할 자료구조와, 조건 밖 요청에 소환되지 않게 하는 매칭기를 만든다. "
            "수용 조건이 조회 가능한 개체여야 부적합 소환을 막을 수 있다.",
    sdd="31-erd--개체와-관계", seq="해당 없음",
    breakdown=[
        "`MerchantProfile` 모델 — `mood`(분위기) · `strengths`(강점) · `services`(제공 서비스) · `updatedAt`",
        "`AcceptanceRule` 모델 — `minParty` · `maxParty` · `timeWindow` · `roomRequired`",
        "마이그레이션 작성 및 RLS 정책 추가 (`DAT-008` 규약)",
        "`AcceptanceRuleMatcher.matches(rule, conditionSet)` — 조건 밖이면 false",
        "매칭기를 `AGR-001` 의 소환 대상 선정에 노출",
        "**가격·할인 필드를 두지 않는다** — 콘솔은 자기 소개를 적는 곳이지 흥정하는 곳이 아니다",
        "단위 테스트 — 인원·시간·룸 조건 경계, 조건 밖 매칭 거부",
    ],
    scenarios=[
        dict(title="수용 조건을 항목별로 등록할 수 있다",
             given="사장이 콘솔에 접속한 상태에서",
             when="분위기·강점·서비스·수용 조건을 입력하면",
             then="항목별로 저장된다.",
             slo="필수 항목 ≤ 5개 (US-5 AC1)"),
        dict(fail=True, title="수용 조건 밖 요청은 매칭되지 않는다",
             given="최대 인원 8명으로 등록한 매장이 있고",
             when="12명 조건 요청이 들어오면",
             then="매칭기가 false를 반환해 **소환 자체가 발생하지 않는다**.",
             slo="부적합 소환률 ≤ 5% (US-5 AC2)"),
        dict(fail=True, title="가격 관련 필드가 스키마에 없다",
             given="프로필·수용 조건 스키마가 적용된 상태에서",
             when="컬럼 목록을 검사하면",
             then="할인폭·가격 협상 필드가 0개다. 경쟁 축은 가격이 아니라 **조건 적합도**다(§6.3-7)."),
    ],
    constraints=[
        ("설계 원칙", "**할인폭을 넣는 칸이 아니다** — 콘솔은 '우리 가게는 이런 자리다'를 적는 곳이다(SRS §8 P5)."),
        ("보안", "**REQ-NF-012** RLS로 자기 매장만 접근. 프로필도 예외가 아니다."),
        ("사용자 특성", "필수 항목 5개 이하 — 온보딩 이탈을 막기 위한 상한이다(R2 완화 장치)."),
    ],
    external="없음",
)

SPECS["MCH-004"] = dict(
    purpose="프로필을 저장하되 **등록된 사실 속성으로 뒷받침되지 않는 문구는 저장하지 않는다.** "
            "이 가드가 없으면 제안 카드가 곧 광고가 되고 N1이 지적한 문제가 그대로 재현된다.",
    sdd="44-agent-room-service--merchant-console-service--공급-측", seq="해당 없음",
    breakdown=[
        "`app/actions/merchant.ts` 에 `saveMerchantProfile` 작성",
        "입력 검증 — `CTR-001` 스키마. 인가 확인(자기 매장인지)을 첫 줄에서",
        "`EvidenceGuard.validate()` — 강점 문구가 등록된 `Attribute` 로 뒷받침되는지 검사",
        "미뒷받침 문구 처리 — **저장하지 않고** 근거가 될 속성 등록을 안내",
        "1회 클릭 재편집 — 기존 항목을 불러와 수정 가능하게 (US-5 AC3)",
        "저장 후 캐시 무효화 — `updateTag('place:{id}')`",
        "단위 테스트 — 미뒷받침 문구 거부, 재편집 로드, 타 매장 저장 시도 거부",
    ],
    scenarios=[
        dict(title="프로필이 저장되고 재편집된다",
             given="프로필 작성이 완료된 상태에서",
             when="사장이 내용을 갱신하려 하면",
             then="기존 항목을 1회 클릭으로 불러와 수정할 수 있다.",
             slo="프로필 갱신율 ≥ 70% (US-5 AC3)"),
        dict(fail=True, title="근거 없는 강점 문구가 저장되지 않는다",
             given="사장이 등록된 사실 속성으로 뒷받침되지 않는 표현을 입력하고",
             when="저장을 시도하면",
             then="해당 문구를 **저장하지 않고** 근거가 될 속성 등록을 안내한다.",
             slo="근거 없는 제안 문구 0건 (US-5 AC4)"),
        dict(fail=True, title="타 매장 프로필을 저장할 수 없다",
             given="가맹 A 세션으로",
             when="가맹 B의 프로필 저장을 시도하면",
             then="인가 확인에서 거부되고 RLS가 2차로 막는다."),
    ],
    constraints=[
        ("설계 원칙", "**근거 없는 문구 0건**(§6.3-2) — 제안이 광고로 읽히지 않게 하는 유일한 장치다."),
        ("보안", "**SRS §6.1 규약 ②** Server Action은 공개 엔드포인트와 동등 — 인가를 첫 줄에서 확인한다."),
        ("성능", "저장 후 `updateTag` 로 캐시를 무효화한다. 오래된 프로필이 소환 매칭에 쓰이면 안 된다."),
    ],
    external="없음",
)

SPECS["AGR-001"] = dict(
    purpose="조건에 맞는 매장 에이전트를 3–5곳 불러 하나의 대화방을 만든다. 적합 매장이 0곳이면 "
            "대화방을 아예 만들지 않는 것이 '빈 제안 화면 0건'의 첫 방어선이다.",
    sdd="44-agent-room-service--merchant-console-service--공급-측", seq="SD-06 에이전트 소환 → 제안 수집 → 180초 마감",
    breakdown=[
        "`app/actions/agent-room.ts` 에 `createAgentRoom` 작성",
        "입력 검증 — 카테고리·지역·조건 json. **조건 2개 이상 필수** (`CTR-001` 스키마)",
        "`AgentSummoner.summon()` — `MCH-003` 의 매칭기로 적합 매장 조회",
        "`limitCount()` — **3~5곳으로 제한**. 초과 시 적합도 상위만",
        "적합 매장 0곳이면 **즉시 미개시 응답** — 대화방을 생성하지 않는다 (`ok: true` 로 표현)",
        "`AgentRoom` 생성 — `expiresAt = now + 180s` **서버 시각 기준**",
        "`Summon` 레코드 생성 — 소환 수신·열람·무시를 세기 위한 실체 (보조 11 분모)",
        "소환 알림 발송 및 `agent_summoned` 이벤트 적재",
        "단위 테스트 — 0곳/2곳/3곳/8곳 분기, 조건 1개 거부, 만료 시각 서버 기준",
    ],
    scenarios=[
        dict(title="3~5곳이 소환되어 대화방이 만들어진다",
             given="이용자가 카테고리와 지역을 말하고 적합 매장이 8곳 있으면",
             when="소환이 실행되면",
             then="적합도 상위 3~5곳만 소환되어 하나의 대화방이 생성된다.",
             slo="소환 수 3–5곳 · 대화방 생성 p95 ≤ 2,000ms (US-6 AC1)"),
        dict(fail=True, title="적합 매장이 0곳이면 대화방을 만들지 않는다",
             given="수용 조건을 만족하는 매장이 0곳인 상태에서",
             when="소환이 실행되면",
             then="**즉시 미개시 응답**을 반환하고 대화방을 생성하지 않는다. 참여 매장 없는 빈 대화방을 만들면 '참여 매장이 없는 서비스'로 읽힌다."),
        dict(fail=True, title="조건이 1개면 소환하지 않는다",
             given="조건이 1개만 주어진 상태에서",
             when="소환을 시도하면",
             then="입력 검증에서 거부한다. 조건이 부족하면 부적합 소환이 늘어난다.",
             slo="조건 2개 이상 필수 (SRS §6.1)"),
    ],
    constraints=[
        ("성능", "**US-6 AC1** 대화방 생성 p95 ≤ 2,000ms."),
        ("설계 원칙", "마감은 **서버 `expiresAt` 기준**이며 클라이언트 타이머를 신뢰하지 않는다(ADR-T06)."),
        ("품질", "**보조 11** 가맹점 월 제안 참여율 60% — `Summon` 레코드가 이 지표의 분모다."),
        ("리스크", "**R1** 매장의 제안 참여가 목표에 미달하면 이 기능 전체가 성립하지 않는다. 게이트 1의 가맹 LOI 30곳이 선행 확인 장치다."),
    ],
    external="**DEP-T5** 가맹 온보딩 — 적합 매장 모수가 없으면 소환 자체가 불가능",
)

SPECS["AGR-002"] = dict(
    purpose="서버리스에는 180초 동안 켜져 있는 프로세스가 없다. 대화방 상태를 DB에 두고 "
            "**Supabase Realtime이 알림을 나르게** 해 폴링 없이 제안 도착을 전한다.",
    sdd="322-agentroom--대화방-수명", seq="SD-06 에이전트 소환 → 제안 수집 → 180초 마감",
    breakdown=[
        "`src/lib/realtime.ts` — Supabase Realtime 클라이언트 (익명 키 + RLS 전제)",
        "대화방 채널 구독 — `room:{roomId}` 채널",
        "`Proposal` 변경 브로드캐스트 수신 → 제안 도착 푸시",
        "마감 브로드캐스트 수신 → 결과 화면 전환",
        "**클라이언트 카운트다운은 서버가 내려준 `expiresAt` 기준**으로만 계산",
        "재연결 처리 — 연결이 끊겼다 붙으면 현재 상태를 다시 조회해 동기화",
        "구독 해제 — 대화방 이탈·마감 시 정리",
        "단위 테스트 — 재연결 후 상태 일치, 타 대화방 채널 구독 거부(RLS)",
    ],
    scenarios=[
        dict(title="제안이 폴링 없이 도착한다",
             given="이용자가 대화방 채널을 구독한 상태에서",
             when="매장이 제안을 제출하면",
             then="Realtime 브로드캐스트로 즉시 전달된다. 클라이언트 폴링이 없어야 한다.",
             slo="폴링 요청 0건"),
        dict(fail=True, title="연결이 끊겨도 상태가 어긋나지 않는다",
             given="구독 중 네트워크가 끊겼다 복구된 상태에서",
             when="재연결되면",
             then="현재 대화방 상태를 다시 조회해 동기화한다. 끊긴 동안 도착한 제안이 누락되어서는 안 된다."),
        dict(fail=True, title="다른 대화방을 구독할 수 없다",
             given="자신이 참여하지 않은 `roomId` 가 주어지고",
             when="해당 채널 구독을 시도하면",
             then="RLS가 거부한다. 타인의 제안이 노출되어서는 안 된다."),
    ],
    constraints=[
        ("기술 제약", "**LIM-T01** 장시간 실행 프로세스를 둘 수 없다. **ADR-T06** DB 상태 + Realtime + lazy close 조합으로 해결한다."),
        ("보안", "**REQ-NF-012** Realtime도 RLS 적용 대상이다. 익명 키로 임의 채널을 구독할 수 없어야 한다."),
        ("계층 분리", "이 태스크는 유형 `UI` 다 — 클라이언트 구독 코드이므로 기능 구현(BE)과 분리한다(원칙 P6)."),
        ("의존", "실시간 전달이 **Supabase Realtime 가용성에 종속**된다(SRS §15-6). 장애 시 마감은 lazy close로 여전히 확정된다."),
    ],
    external="**DEP-T2** Supabase Realtime 활성화",
)

SPECS["AGR-003"] = dict(
    purpose="제안을 받아 **가격이 아니라 조건 적합도로** 정렬한다. 가격 협상 기능을 0건으로 두는 것이 "
            "이 제품이 광고·입찰 모델과 갈리는 지점이다.",
    sdd="44-agent-room-service--merchant-console-service--공급-측", seq="SD-06 에이전트 소환 → 제안 수집 → 180초 마감",
    breakdown=[
        "`app/actions/agent-room.ts` 에 `submitProposal` 작성",
        "입력 검증 — `roomId` · headline · highlights · services (`CTR-001` 스키마)",
        "`EvidenceGuard` 재사용(`MCH-004`) — 등록 속성으로 뒷받침되지 않는 문구는 **400 반환**",
        "마감 시각 확인 — `expiresAt` 경과 후 제출은 거부",
        "`Proposal` 저장 → Realtime 브로드캐스트 (`AGR-002` 가 수신)",
        "`FitnessRanker.rank()` — **조건 적합도 1순위**. 가격 필드가 스키마에 없어 정렬 키로 쓸 수 없다",
        "비교 축과 근거를 함께 표시할 수 있는 형태로 반환",
        "불이행 신고 처리 — `Proposal.state = VIOLATED` → `Summon.fitnessScore` 하향",
        "`proposal_received` · `agent_proposed` 이벤트 적재",
        "단위 테스트 — 근거 없는 문구 거부, 마감 후 제출 거부, 정렬 키에 가격 부재",
    ],
    scenarios=[
        dict(title="제안이 적합도 순으로 정렬된다",
             given="여러 제안이 수집된 상태에서",
             when="정리해 보여 주면",
             then="가격이 아니라 **조건 적합도를 1순위**로 정렬하고 비교 축과 근거를 함께 표시한다.",
             slo="근거 표기율 100% · 가격 협상 기능 0건 (US-6 AC3)"),
        dict(fail=True, title="근거 없는 제안 문구가 거부된다",
             given="등록 속성으로 뒷받침되지 않는 headline이 주어지고",
             when="제출하면",
             then="400을 반환하고 저장하지 않는다.",
             slo="근거 없는 제안 문구 0건"),
        dict(fail=True, title="마감 후 제출이 거부된다",
             given="`expiresAt` 이 지난 대화방에",
             when="제안을 제출하면",
             then="거부한다. 마감 판정은 서버 시각 기준이므로 클라이언트 시계 조작으로 우회할 수 없어야 한다."),
        dict(fail=True, title="불이행 신고가 소환 가중치를 낮춘다",
             given="선택된 제안이 현장에서 이행되지 않아 신고된 상태에서",
             when="신고가 처리되면",
             then="불이행이 기록되고 해당 매장의 소환 가중치가 하향된다.",
             slo="제안 이행률 ≥ 95% · 신고 처리 ≤ 48h (US-6 AC5)"),
    ],
    constraints=[
        ("설계 원칙", "**가격 협상 기능 0건**(§6.3-7) — `Proposal` 에 가격 필드가 없으므로 실수로 정렬 키에 넣을 수 없다."),
        ("품질", "**보조 9** 제안 이행률 ≥ 95%. **§6.3-10** 불이행 시 소환 가중치 하향."),
        ("성능", "**US-6 AC2** 첫 제안 p50 ≤ 60s · 180초 내 제안 1건 이상 도착률 85%."),
    ],
    external="없음",
)

SPECS["AGR-005"] = dict(
    purpose="180초 마감을 **서버 시각 기준 lazy close**로 확정한다. Cron은 아무도 조회하지 않은 방을 "
            "치우는 보조 수단이라, 마감 정확도가 Cron 주기에 종속되지 않는다.",
    sdd="322-agentroom--대화방-수명", seq="SD-06 에이전트 소환 → 제안 수집 → 180초 마감",
    breakdown=[
        "**lazy close** — 대화방 조회 시 `expiresAt` 경과가 확인되면 그 자리에서 상태를 확정",
        "상태 전이 — 유효 제안 ≥ 1건이면 `CLOSED_FILLED`, 0건이면 `CLOSED_EMPTY`",
        "`app/api/cron/close-rooms/route.ts` — 매 1분. 아무도 조회하지 않은 만료 방을 정리 (보조 수단)",
        "동시성 처리 — lazy close와 Cron이 같은 방을 동시에 닫아도 상태가 한 번만 전이되게 (조건부 갱신)",
        "마감 브로드캐스트 — `AGR-002` 가 수신해 화면 전환",
        "Cron 응답 — 처리 건수·소요·마지막 실행 시각 (`REL-001` 이 읽음)",
        "단위 테스트 — 경과 경계, 동시 마감 중복 전이 방지, 제안 0건/1건 분기",
    ],
    scenarios=[
        dict(title="조회 시점에 마감이 확정된다",
             given="`expiresAt` 이 지난 대화방을",
             when="이용자가 조회하면",
             then="그 자리에서 상태가 확정된다. **Cron 주기를 기다리지 않는다**.",
             slo="마감 지연 ≤ 조회 시점"),
        dict(title="아무도 안 본 방도 정리된다",
             given="만료됐지만 아무도 조회하지 않은 대화방이 있고",
             when="Cron이 실행되면",
             then="상태가 확정되고 브로드캐스트된다."),
        dict(fail=True, title="동시 마감이 중복 전이를 만들지 않는다",
             given="lazy close와 Cron이 같은 방을 동시에 처리하고",
             when="둘 다 상태 전이를 시도하면",
             then="조건부 갱신으로 **한 번만** 전이된다. 정산·알림이 중복 실행되어서는 안 된다."),
        dict(fail=True, title="Cron 실행 실패가 드러난다",
             given="Cron 실행이 중단된 상태에서",
             when="마지막 성공 시각이 24시간을 넘으면",
             then="`REL-001` 이 알림을 발송한다. 마감이 조용히 멈추면 안 된다.",
             slo="무증상 중단 0건 (R-T3)"),
    ],
    constraints=[
        ("기술 제약", "**LIM-T01** 장시간 프로세스 불가. **D-05** Cron은 Vercel Cron → Route Handler로만."),
        ("설계 원칙", "**마감 정확도가 Cron 주기에 종속되지 않는다**(ADR-T06) — lazy close가 본체, Cron이 보조."),
        ("동시성", "서버리스는 같은 작업이 병렬 실행될 수 있다. 상태 전이는 반드시 조건부 갱신으로 한다."),
    ],
    external="없음",
)

SPECS["AGR-006"] = dict(
    purpose="유효 제안이 0건일 때 빈 화면 대신 **제안 없는 Top-3로 되돌린다.** 빈 제안 화면을 보여 주면 "
            "'참여 매장이 없는 서비스'로 읽히고, 그 인상은 되돌리기 어렵다.",
    sdd="fc-04-제안-마감-처리", seq="SD-06 에이전트 소환 → 제안 수집 → 180초 마감",
    breakdown=[
        "마감 상태가 `CLOSED_EMPTY` 일 때의 화면 정의 (`UX-004` 빈·오류 상태 규격 준용)",
        "**제안 없는 Top-3로 복귀** — `RNK-003` 의 결과를 재사용",
        "사실 안내 — '180초 내 제안이 도착하지 않았다'를 명시. 원인을 숨기지 않는다",
        "다음 행동 제시 — 조건 완화·시간대 변경 등",
        "복귀 경로 계측 — 어느 경로로 되돌아갔는지 적재",
        "단위 테스트 — `CLOSED_EMPTY` 시 빈 화면이 렌더되지 않는지",
    ],
    scenarios=[
        dict(fail=True, title="제안 0건이면 Top-3로 되돌린다",
             given="180초가 지나고 유효 제안이 0건인 상태에서",
             when="결과 화면이 렌더되면",
             then="빈 화면 대신 **제안 없는 Top-3**로 되돌리고 그 사실을 알린다.",
             slo="빈 제안 화면 노출 0건 (US-6 AC4)"),
        dict(fail=True, title="소환 미개시도 같은 화면으로 흡수된다",
             given="적합 매장 0곳으로 대화방이 생성되지 않은 상태에서",
             when="이용자가 결과를 보면",
             then="동일한 복귀 화면이 렌더된다. 두 경우가 사용자에게 다르게 보일 이유가 없다."),
    ],
    constraints=[
        ("설계 원칙", "**빈 화면 금지**(§6.3-6). 빈 제안 화면은 참여 매장 부재로 읽히므로 반드시 대체 결과를 보여 준다."),
        ("계층 분리", "이 태스크는 유형 `UI` 다 — 화면 흐름이므로 마감 판정(`AGR-005`, BE)과 분리한다(원칙 P6)."),
    ],
    external="없음",
)

SPECS["ANA-002"] = dict(
    purpose="사용자 행동을 숫자로 바꾸는 파이프라인을 세운다. 계측 경로가 사용자 요청 경로와 "
            "분리되어 있어야 계측이 실패해도 사용자가 영향을 받지 않는다.",
    sdd="46-analytics-service--계측", seq="SD-10 계측 이벤트 수집 → 집계 → 알림",
    breakdown=[
        "`app/api/events/route.ts` — POST. `sendBeacon` 배치 페이로드 수신 (`CTR-002` 계약)",
        "**중복 제거** — `Event.eventId` 기본 키 제약으로 이중 계수 차단. 애플리케이션 로직에 의존하지 않는다",
        "`incomplete` 플래그 — 필수 속성 결측 시 폐기하지 않고 표기해 적재",
        "서버 이벤트 적재 — `after()` 헬퍼 작성. 응답 이후 실행되어 지연 0 (ADR-T07)",
        "세션 스티칭 — 익명 디바이스 ID → 로그인 사용자 ID 소급 병합",
        "`app/api/cron/aggregate/route.ts` — 매일 03:00 KST. 지표 12종 집계",
        "지연 도착 처리 — **48시간 허용, D+2 확정**. 확정 전 값은 `provisional` 표기",
        "지표 마트 뷰 생성 — 일간·주간·월간",
        "단위 테스트 — 중복 `event_id` 폐기, `after()` 가 응답을 지연시키지 않음, D+2 확정",
    ],
    scenarios=[
        dict(title="이벤트가 유실 없이 적재된다",
             given="클라이언트가 `sendBeacon` 으로 배치를 보내고",
             when="수집 엔드포인트가 처리하면",
             then="이벤트가 `occurredAt` 파티션에 적재되고 세션이 스티칭된다."),
        dict(title="계측이 응답을 지연시키지 않는다",
             given="서버 이벤트가 `after()` 로 적재되는 상태에서",
             when="Server Action이 실행되면",
             then="적재는 응답 이후에 수행되고 사용자 응답 시간에 포함되지 않는다.",
             slo="계측으로 인한 지연 0ms (ADR-T07)"),
        dict(fail=True, title="중복 event_id가 이중 계수되지 않는다",
             given="클라이언트 재시도로 같은 `event_id` 가 두 번 도착하고",
             when="수집이 처리하면",
             then="DB 기본 키 제약으로 두 번째가 폐기된다. 지표가 부풀지 않아야 한다.",
             slo="이중 계수 0건"),
        dict(fail=True, title="계측 실패가 사용자 요청을 막지 않는다",
             given="이벤트 적재가 실패하는 상태에서",
             when="사용자 요청이 처리되면",
             then="요청은 정상 완료된다. 계측은 비동기·유실 허용 경로다 (SRS §7 계측 파이프라인)."),
    ],
    constraints=[
        ("성능", "**REQ-NF-001a** 계측이 응답 지연 원인이 되면 안 된다. 서버 적재는 `after()`, 클라이언트는 `sendBeacon`."),
        ("기술 제약", "**D-04 · REQ-TEC-014** 외부 메시지 브로커를 두지 않는다. 대가로 대량 유입 시 DB 쓰기 부하가 생긴다(SRS §15-10)."),
        ("정합성", "**48시간 지연 도착 허용 · D+2 확정.** 확정 전 값은 `provisional` 로 표기해 성급한 판단을 막는다."),
        ("보안", "**REQ-NF-010** 이벤트 속성에 개인 식별 정보를 담지 않는다."),
    ],
    external="없음",
)

SPECS["ANA-005"] = dict(
    purpose="지표가 틀리는 원인이 제품인지 계측인지 구분한다. 이 구분이 없으면 계측 결함을 보고 "
            "제품을 고치는 잘못된 결정을 내린다.",
    sdd="46-analytics-service--계측", seq="SD-10 계측 이벤트 수집 → 집계 → 알림",
    breakdown=[
        "누락률 점검 — 서버 측 기대 발생 수(예: `/v1/query` 호출 수) 대비 수신 이벤트 수",
        "결측률 점검 — `incomplete` 플래그 비율",
        "세션 스티칭 정확도 — 로그인 전후 병합 세션의 중복 계수 검사",
        "지표 재현성 — 동일 기간 재집계 시 값 일치 여부",
        "`isReliable()` 판정 — 임계 초과 시 지표를 `unreliable` 로 표기",
        "`MetricAlertDispatcher` — SRS §10.3의 관측 항목별 임계·채널·대응 구현",
        "**게이트 연동** — `unreliable` 지표는 릴리스 게이트 판정에 사용하지 않는다",
        "단위 테스트 — 각 임계 경계, `unreliable` 전파, 알림 채널 라우팅",
    ],
    scenarios=[
        dict(title="품질을 통과한 지표만 게이트에 쓰인다",
             given="계측 품질 점검이 전부 통과한 상태에서",
             when="지표를 집계하면",
             then="`isReliable() = true` 로 표기되고 게이트 판정에 사용된다."),
        dict(title="임계 초과 시 알림이 발송된다",
             given="관측 항목이 임계를 넘은 상태에서",
             when="집계가 완료되면",
             then="정의된 채널로 알림이 발송되고 대응 절차가 함께 안내된다.",
             slo="SRS §10.3 임계표 그대로"),
        dict(fail=True, title="계측 결함 지표가 게이트를 통과하지 못한다",
             given="이벤트 누락률이 2%를 넘은 상태에서",
             when="지표를 판정하면",
             then="`unreliable` 로 표기되고 **릴리스 게이트 판정에서 배제**된다. 잘못된 데이터로 통과·미달을 판정하지 않는다.",
             slo="누락률 > 2% · 결측률 > 1% · 스티칭 중복 > 0.5%"),
    ],
    constraints=[
        ("품질 원칙", "**계측 결함과 제품 실패를 구분한다** — 구분하지 못하면 잘못된 릴리스 결정을 한다(SRS §7)."),
        ("관측", "**REQ-NF-015** §10.3의 관측 항목 전부에 수집 방식·임계·채널·대응이 정의되어야 한다."),
        ("게이트", "`unreliable` 지표는 게이트 0·1·2 판정에 사용하지 않는다(SRS §10.4)."),
    ],
    external="없음",
)

SPECS["ANA-007"] = dict(
    purpose="AI 추론 비용과 단위 경제를 숫자로 만든다. 비용이 보이지 않으면 LLM 호출 비율이 조용히 "
            "올라가 REQ-NF-013이 무너진 뒤에야 발견된다.",
    sdd="46-analytics-service--계측", seq="SD-10 계측 이벤트 수집 → 집계 → 알림",
    breakdown=[
        "토큰 사용량 집계 — `QRY-002` 가 적재한 사용량을 세션 단위로 환산",
        "세션당 추론 비용 산출 — 일간 집계. 임계 18원 초과 시 알림",
        "**LLM 호출 비율 감시** — `parse_path` 분포에서 `llm` 비율 산출. 일간 40% 초과 시 알림",
        "파싱 캐시 히트율 감시 — 일간 50% 미만 시 알림",
        "단위 경제 월간 리포트 — 성사 1건당 수수료 대 처리 비용 배수",
        "제안 품질 심사 인력 상한 추적 — 가맹점 150곳당 1 FTE",
        "단위 테스트 — 비용 환산 정확성, 임계 알림 발화",
    ],
    scenarios=[
        dict(title="세션당 추론 비용이 집계된다",
             given="토큰 사용량이 적재된 상태에서",
             when="일간 집계가 실행되면",
             then="세션당 추론 비용이 산출되고 목표와 대비된다.",
             slo="세션당 ≤ 12원 (REQ-NF-013)"),
        dict(fail=True, title="LLM 호출 비율 상승이 조기에 드러난다",
             given="`parse_path` 의 `llm` 비율이 45%인 상태에서",
             when="일간 집계가 완료되면",
             then="알림이 발송된다. 비용과 지연이 **동시에** 악화되는 선행 신호다.",
             slo="LLM 호출 비율 일간 ≤ 40%"),
        dict(fail=True, title="단위 경제가 무너지면 보고된다",
             given="성사 1건당 수수료가 처리 비용의 3배 미만인 상태에서",
             when="월간 리포트가 산출되면",
             then="미달이 명시된다. 사업 모델 성립 여부에 관한 신호다.",
             slo="수수료 > 처리 비용 × 3 (REQ-NF-014)"),
    ],
    constraints=[
        ("비용", "**REQ-NF-013** 세션당 ≤ 12원 · 일간 > 18원 시 알림. **REQ-NF-014** 성사당 수수료 > 처리 비용 × 3."),
        ("선행 지표", "LLM 호출 비율은 비용·지연의 **선행 지표**다. 비용이 오른 뒤가 아니라 비율이 오를 때 대응한다."),
        ("운영", "제안 품질 심사는 가맹점 150곳당 1 FTE 상한. 초과 시 신규 온보딩 속도를 조절한다(LIM-09)."),
    ],
    external="없음",
)

SPECS["SEC-001"] = dict(
    purpose="참석자 출발지 등 목적을 다한 개인정보를 30일 안에 지운다. 파기가 자동화되지 않으면 "
            "'목적 외 사용 금지' 약속이 문서에만 남는다.",
    seq="해당 없음",
    breakdown=[
        "`app/api/cron/purge/route.ts` — 매일 05:00 KST. `CRON_SECRET` 인증 (집계 이후 실행)",
        "파기 대상 식별 — 세션 종료 후 30일 경과한 참석자 출발지 등",
        "파기 실행 — **물리 삭제**. 개인정보는 논리 삭제 대상이 아니다",
        "파기 로그 — 무엇을 몇 건 지웠는지 기록 (개인정보 자체는 로그에 남기지 않는다)",
        "스키마 점검 — 회식 참석자의 비고·개인 취향 필드가 **존재하지 않는지** 확인 (`TST-008` 과 교차)",
        "Cron 응답 — 처리 건수·소요·마지막 실행 시각",
        "단위 테스트 — 29일/31일 경계, 파기 후 조회 불가, 무단 호출 거부",
    ],
    scenarios=[
        dict(title="30일 경과 데이터가 파기된다",
             given="세션 종료 후 31일이 지난 참석자 출발지 데이터가 있고",
             when="파기 Cron이 실행되면",
             then="해당 레코드가 물리 삭제되고 이후 조회되지 않는다. 29일 건은 남아 있어야 한다.",
             slo="경계 오판 0건 (REQ-NF-010)"),
        dict(fail=True, title="외부에서 파기를 실행할 수 없다",
             given="`CRON_SECRET` 없이",
             when="파기 엔드포인트를 호출하면",
             then="인증 거부로 응답하고 **파기 로직이 실행되지 않는다**. 임의 실행은 데이터 손실 사고다.",
             slo="무단 실행 0건 (REQ-TEC-013)"),
        dict(fail=True, title="Cron 실행 실패가 드러난다",
             given="파기 Cron이 중단된 상태에서",
             when="마지막 성공 시각이 24시간을 넘으면",
             then="알림이 발송된다. 파기가 조용히 멈추면 개인정보가 계속 남는다.",
             slo="무증상 중단 0건 (R-T3)"),
    ],
    constraints=[
        ("보안", "**REQ-NF-010** 참석자 출발지는 세션 종료 후 30일 내 파기 · 목적 외 사용 금지. 개인 제약 정보는 **본인 단말에만** 저장한다."),
        ("기술 제약", "**D-05 · REQ-TEC-013** Vercel Cron → Route Handler. 외부 호출 거부."),
        ("실행 순서", "집계(03:00) 이후 파기(05:00) — 파기 전 지표 집계가 끝나 있어야 한다."),
    ],
    external="없음",
)

SPECS["SEC-002"] = dict(
    purpose="프롬프트를 통해 개인정보가 외부 AI로 나가는 경로를 막는다. C-TEC-005가 만든 "
            "**새로운 유출 면**이므로 별도 검증 장치가 필요하다.",
    seq="SD-01 조건 검색 → Top-3 반환",
    breakdown=[
        "프롬프트 조립 지점 식별 — `QRY-002` 의 `lib/ai.ts` 단일 진입점만",
        "허용 필드 화이트리스트 정의 — **조건 문장만** 전달. 사용자 ID·좌표·연락처·개인 제약은 금지",
        "런타임 가드 — 프롬프트 조립 시 금지 패턴 검출하면 호출을 중단",
        "정적 검사 — `verify-constraints` 에 규칙 추가 (프롬프트에 사용자 객체 직접 전달 금지)",
        "테스트 픽스처 — 개인정보가 섞인 입력으로 가드가 발동하는지",
        "감사 — 프롬프트 샘플링 로그에 개인정보가 없는지 정기 확인 (로그 자체에도 남기지 않는다)",
        "단위 테스트 — 금지 필드 전달 시 호출 차단",
    ],
    scenarios=[
        dict(title="조건 문장만 프롬프트에 담긴다",
             given="사용자 세션과 위치 정보가 있는 요청에서",
             when="LLM 파싱이 호출되면",
             then="프롬프트에는 조건 문장만 포함되고 사용자 ID·좌표·연락처가 들어가지 않는다.",
             slo="개인정보 포함 프롬프트 0건 (REQ-NF-010)"),
        dict(fail=True, title="개인정보가 섞이면 호출이 중단된다",
             given="프롬프트 조립 중 금지 패턴이 검출되고",
             when="가드가 발동하면",
             then="**AI 호출을 하지 않고** 결정론 폴백으로 전환한다. 유출 후 되돌릴 방법이 없기 때문이다."),
        dict(fail=True, title="정적 검사가 위험한 코드를 막는다",
             given="사용자 객체를 프롬프트에 직접 전달하는 코드가 추가되고",
             when="빌드를 실행하면",
             then="`verify-constraints` 가 이를 지목하고 빌드가 실패한다."),
    ],
    constraints=[
        ("보안", "**REQ-NF-010** 개인 제약 정보는 본인 단말에만. 프롬프트는 외부 전송이므로 **가장 엄격한 기준**을 적용한다."),
        ("기술 제약", "**REQ-TEC-008** 모든 AI 호출이 `lib/ai.ts` 를 경유하므로 가드를 한 곳에만 두면 된다."),
        ("되돌림", "유출은 되돌릴 수 없다 — 검출 시 호출을 **중단**하고 폴백한다. 경고 후 진행하지 않는다."),
    ],
    external="없음",
)

SPECS["REL-001"] = dict(
    purpose="경로별 응답 시간·Cron 실패·커넥션 풀을 감시하고 롤백 절차를 세운다. 전담 SRE가 없는 "
            "구조(SRS §2)라 감시가 자동이 아니면 아무도 보지 않는다.",
    seq="해당 없음",
    breakdown=[
        "Vercel Observability 연동 — `parse_path` 별 p95 분리 관측 (`QRY-005` 태깅 활용)",
        "결정론 경로 임계 — 10분 > 1,500ms 시 Slack",
        "LLM 경로 임계 — 10분 > 3,000ms 시 Slack (더 가벼운 모델로 환경 변수 교체가 대응)",
        "**Cron 실행 실패 알림** — 각 배치가 마지막 성공 시각을 기록하고 24시간 초과 시 PagerDuty (R-T3)",
        "**Supavisor 풀 사용률 감시** — 80% 초과 시 Slack (R-T2)",
        "롤백 절차 문서화 — Vercel 즉시 롤백 경로와 판단 기준",
        "기능 플래그 — 모듈 단위 비활성화 수단 (R-T4 완화)",
        "단위 테스트 — 임계 경계 발화, 마지막 실행 시각 추적",
    ],
    scenarios=[
        dict(title="경로별 p95가 분리 관측된다",
             given="`parse_path` 태깅이 적재되는 상태에서",
             when="관측 대시보드를 보면",
             then="결정론 경로와 LLM 경로의 p95가 각각 표시된다. 합산 값만 보면 REQ-NF-001a 달성 여부를 알 수 없다."),
        dict(fail=True, title="Cron이 멈추면 24시간 안에 드러난다",
             given="어느 Cron의 마지막 성공이 25시간 전인 상태에서",
             when="감시가 실행되면",
             then="PagerDuty 알림이 발송된다. 신선도·마감·집계·파기가 조용히 멈추는 것이 이 프로젝트의 대표 실패 모드다.",
             slo="무증상 중단 0건 (R-T3)"),
        dict(fail=True, title="커넥션 풀 고갈 전에 경고한다",
             given="Supavisor 풀 사용률이 85%인 상태에서",
             when="감시가 실행되면",
             then="알림이 발송된다. 고갈되면 5xx가 급증한다(R-T2).",
             slo="임계 80%"),
    ],
    constraints=[
        ("성능", "**REQ-NF-001a/001b** 경로별 임계. **REQ-NF-009** RTO ≤ 30분 — 롤백 절차가 이 목표의 수단이다."),
        ("조직", "**전담 SRE가 없다**(SRS §2) — 관측·알림은 풀스택 리드가 겸하므로 자동 알림이 유일한 감지 수단이다."),
        ("한계", "**LIM-T05** 플랫폼 장애는 자체 복구가 불가능하다. 상태 페이지 구독과 사용자 공지가 대응의 전부다."),
    ],
    external="없음",
)

SPECS["REL-004"] = dict(
    purpose="Supabase PITR을 켜 RPO 5분을 확보한다. 설정 하나지만 켜지 않으면 데이터 손실 시 "
            "복구할 지점 자체가 없다.",
    seq="해당 없음",
    breakdown=[
        "Supabase 프로젝트에서 **PITR 활성화** (플랜 요건 확인)",
        "복구 지점 간격 확인 — RPO ≤ 5분 충족 여부",
        "복구 절차 문서화 — 어떤 상황에서 누가 어떻게 복구하는지",
        "복원 훈련 1회 — 스테이징에서 특정 시점 복구를 실제로 수행",
        "RTO 측정 — 훈련에서 실제 소요 시간을 기록 (목표 ≤ 30분)",
    ],
    scenarios=[
        dict(title="PITR이 활성화되어 있다",
             given="Supabase 프로젝트 설정에서",
             when="백업 정책을 확인하면",
             then="PITR이 켜져 있고 복구 지점 간격이 목표를 만족한다.",
             slo="RPO ≤ 5분 (REQ-NF-009)"),
        dict(fail=True, title="복원 훈련으로 RTO를 실측한다",
             given="스테이징 환경에서",
             when="특정 시점 복구를 수행하면",
             then="완료까지 소요 시간이 기록된다. 목표를 넘으면 절차를 단축해야 한다.",
             slo="RTO ≤ 30분"),
    ],
    constraints=[
        ("신뢰성", "**REQ-NF-009** RTO ≤ 30분 · RPO ≤ 5분."),
        ("기술 제약", "**C-TEC-003** Supabase 기능 범위에서 해결한다. 자체 백업 인프라를 만들지 않는다."),
        ("비용", "PITR은 플랜에 따라 비용이 발생한다 — 예산 항목으로 인지해야 한다."),
    ],
    external="**DEP-T2** Supabase 플랜이 PITR을 지원해야 함",
)

SPECS["REL-006"] = dict(
    purpose="가용성을 실제로 측정해 SRS §15.1이 남긴 미해소 항목 — '99.5% 목표를 두 플랫폼 SLA 곱으로 "
            "재산정할지' — 에 판단 근거를 제공한다.",
    seq="해당 없음",
    breakdown=[
        "헬스체크 엔드포인트 정의 — 앱·DB 각각의 생존 확인",
        "Vercel 상태와 Supabase 상태를 각각 수집",
        "**합성 가용성 산출** — 두 플랫폼 가용성의 곱으로 월 가용성 계산",
        "다운타임 집계 — 월 다운타임 분 단위 (목표 ≤ 3시간 39분)",
        "상태 페이지 구독 — 두 플랫폼의 장애 공지를 수신해 사건과 대조",
        "월간 리포트 — 실측 가용성과 목표 99.5%의 격차를 명시",
        "**판단 근거 제출** — 실측치가 목표에 못 미치면 §15.1 결정(목표 재산정 vs 멀티 클라우드)을 위한 자료로 제출",
        "단위 테스트 — 합성 계산 정확성, 다운타임 집계",
    ],
    scenarios=[
        dict(title="월 가용성이 합성으로 산출된다",
             given="두 플랫폼의 가동 데이터가 수집된 상태에서",
             when="월간 집계가 실행되면",
             then="각 플랫폼 가용성과 그 곱으로 계산한 합성 가용성이 산출된다.",
             slo="목표 ≥ 99.5% · 월 다운타임 ≤ 3시간 39분 (REQ-NF-005)"),
        dict(fail=True, title="목표 미달이 판단 근거로 보고된다",
             given="합성 가용성이 99.2%로 목표에 못 미친 상태에서",
             when="월간 리포트가 산출되면",
             then="격차와 원인(어느 플랫폼의 다운타임인지)이 명시되고 §15.1의 결정 안건으로 올라간다. **감추지 않는다**."),
        dict(fail=True, title="자체 복구가 불가능함을 전제로 대응한다",
             given="플랫폼 장애가 발생한 상태에서",
             when="대응을 실행하면",
             then="상태 페이지 확인과 사용자 공지가 수행된다. **LIM-T05** 에 따라 자체 복구 수단은 없다."),
    ],
    constraints=[
        ("신뢰성", "**REQ-NF-005** 월 가용성 ≥ 99.5%. 두 플랫폼 가용성이 **곱으로** 작용하므로 각 구성요소는 ≥ 99.9%를 전제로 한다."),
        ("한계", "**LIM-T05** 플랫폼 장애는 자체 복구 불가. 이 태스크는 복구가 아니라 **측정과 판단 근거 제공**이 역할이다."),
        ("미해소", "**SRS §15.1** — 99.5%를 계약상 보증하려면 멀티 클라우드가 필요하고 이는 C-TEC-007과 충돌한다. 실측이 이 결정의 입력이다."),
    ],
    external="없음",
)

SPECS["REL-007"] = dict(
    purpose="오류율을 집계하되 **폴백 신호를 오류에서 제외한다.** 파싱 실패·결과 0건·제안 0건은 "
            "설계된 열화 경로인데 이를 5xx로 세면 정상 동작이 장애로 보고된다.",
    seq="해당 없음",
    breakdown=[
        "Vercel Runtime Logs 연동 — 5분 윈도 집계",
        "상태 코드 분류 — `CTR-002` 의 에러 코드 → HTTP 매핑표를 집계 기준으로 사용",
        "**폴백 신호 제외** — 파싱 실패·결과 0건·제안 0건은 오류율 분자에서 뺀다 (§6.3-6)",
        "결제 API 별도 집계 — `/api/webhooks/pg` 및 결제 Server Action은 더 엄격한 임계",
        "임계 알림 — 5분 5xx > 0.3%(일반) / > 0.1%(결제) 시 PagerDuty",
        "4xx 별도 추적 — 인증·검증 실패는 오류율이 아니라 **품질 신호**로 본다",
        "단위 테스트 — 폴백 신호가 분자에 포함되지 않는지, 결제 임계 분리",
    ],
    scenarios=[
        dict(title="오류율이 임계 안에서 유지된다",
             given="정상 운영 상태에서",
             when="5분 윈도 집계가 실행되면",
             then="5xx 오류율이 임계 이하로 산출된다.",
             slo="일반 ≤ 0.3% · 결제 ≤ 0.1% (REQ-NF-006)"),
        dict(fail=True, title="폴백 신호가 오류로 집계되지 않는다",
             given="파싱 실패·결과 0건·제안 0건이 다수 발생한 상태에서",
             when="오류율을 집계하면",
             then="이들은 분자에서 제외된다. 설계된 열화 경로를 장애로 보고하면 잘못된 대응을 부른다.",
             slo="폴백 신호의 5xx 집계 0건 (§6.3-6)"),
        dict(fail=True, title="결제 오류가 별도 임계로 감시된다",
             given="결제 API 5xx가 0.15%인 상태에서",
             when="집계가 완료되면",
             then="일반 임계(0.3%)는 넘지 않았어도 **결제 임계(0.1%)를 넘어 알림**이 발송된다."),
    ],
    constraints=[
        ("안정성", "**REQ-NF-006** 5xx ≤ 0.3% · 결제 API ≤ 0.1%. 인증·검증 실패는 4xx이며 오류율이 아니다."),
        ("설계 원칙", "**폴백은 오류가 아니다** — `CTR-002` 의 에러 코드 체계가 이 구분의 근거다."),
        ("의존", "집계 기준이 `CTR-002` 의 HTTP 매핑표다. 매핑이 바뀌면 이 집계도 함께 바뀌어야 한다."),
    ],
    external="없음",
)

SPECS["TST-001"] = dict(
    purpose="US-1~US-3의 인수 기준을 실행 가능한 테스트로 옮긴다. AC가 문서에만 있으면 "
            "'근거 없는 후보 반환 금지' 같은 규칙이 언제 깨졌는지 알 수 없다.",
    seq="SD-01 조건 검색 → Top-3 반환 · SD-02 파싱 실패 폴백 · SD-03 메뉴명 폴백 · SD-04 근거 표기",
    breakdown=[
        "테스트 러너·실행 환경 구성 (`MCK-001` 픽스처 사용, 실제 DB 없이 동작)",
        "**US-1 예산·조건 필터** — AC1 표기율 · AC2 예산 초과 요약 · AC3 조건 카테고리 · AC4 편차 반영 · **AC5 파싱 실패 폴백** · **AC6 가격 확인 필요**",
        "**US-2 메뉴명 질의** — AC1 정답 반환률 · **AC2 유사 메뉴 대체** · AC3 필수 입력 0개 · **AC4 반경 확대**",
        "**US-3 근거 표기** — AC1 4항목 · **AC2 90일 경고** · AC3 공유 카드 · **AC4 불일치 신고**",
        "임계치 단언 — 각 AC의 수치를 테스트에서 그대로 검증",
        "실패 흐름 우선 배치 — 정상보다 실패 케이스를 먼저 작성",
        "CI 대신 **빌드 단계** 편입 (C-TEC-007)",
    ],
    scenarios=[
        dict(title="US-1~3의 정상 AC가 전부 통과한다",
             given="픽스처가 준비된 상태에서",
             when="테스트를 실행하면",
             then="세 스토리의 정상 흐름 AC가 전부 통과한다.",
             slo="정상 AC 통과율 100%"),
        dict(fail=True, title="실패 흐름 AC가 규칙 위반을 잡아낸다",
             given="근거 4항목 중 하나가 없는 후보를 반환하도록 코드를 바꾸고",
             when="테스트를 실행하면",
             then="US-3 AC1 테스트가 실패한다. 규칙이 깨진 순간 빌드가 막혀야 한다.",
             slo="실패 흐름 AC 8건 이상 커버"),
    ],
    constraints=[
        ("품질", "US-1~3의 AC 14건을 전부 커버한다. **실패 흐름 6건**(AC5·AC6·AC2·AC4·AC2·AC4)이 반드시 포함되어야 한다."),
        ("기술 제약", "**C-TEC-007 · D-06** 외부 CI를 두지 않으므로 테스트는 빌드 명령에 편입한다."),
        ("독립성", "`MCK-001` 픽스처로 실제 DB·LLM 없이 동작해야 한다 — 그래야 백엔드 완성 전에도 돌릴 수 있다."),
    ],
    external="없음",
)

SPECS["TST-004"] = dict(
    purpose="US-4의 결제 AC와 **웹훅 멱등성**을 검증한다. 서버리스에서 웹훅 중복은 예외가 아니라 "
            "기본 가정이므로 이 테스트가 없으면 이중 결제·이중 정산이 운영에서 처음 발견된다.",
    seq="SD-07 제안 선택 → 예약 승계 → 선결제 · SD-08 취소·환불 / 노쇼 판정·정산",
    breakdown=[
        "**US-4** — AC1 조건 승계(재입력 0개) · AC2 주문량 금액 산출·통보 · **AC3 취소·환불** · **AC4 노쇼 판정**",
        "**멱등성 테스트** — 동일 멱등 키 웹훅을 2회·3회 전송해도 `Payment` 상태가 변하지 않는지",
        "서명 검증 테스트 — 서명 없음·불일치·만료 3종이 전부 거부되는지",
        "거부 응답이 4xx인지 확인 — 5xx로 새면 결제 오류율 예산(0.1%)이 오염된다",
        "매장 귀책 분기 테스트 — 노쇼 오판정이 발생하지 않는지",
        "픽스처 사용 — `MCK-001` 의 결제·웹훅 픽스처 (승인/거절/중복/환불)",
    ],
    scenarios=[
        dict(title="US-4의 정상 AC가 통과한다",
             given="결제 픽스처가 준비된 상태에서",
             when="테스트를 실행하면",
             then="조건 승계와 금액 산출·통보 AC가 통과한다.",
             slo="재입력 0개 · 통보 ≤ 30s"),
        dict(fail=True, title="중복 웹훅이 상태를 바꾸지 못한다",
             given="동일 멱등 키 웹훅을 3회 전송하고",
             when="테스트가 상태를 검사하면",
             then="`Payment` 상태가 최초 처리 결과 그대로여야 한다. 이중 정산이 발생하면 실패한다.",
             slo="중복 처리 0건"),
        dict(fail=True, title="서명 실패가 4xx로 처리된다",
             given="서명이 불일치하는 웹훅을 전송하고",
             when="응답을 검사하면",
             then="4xx이며 5xx가 아니다. 5xx면 결제 오류율 예산이 잘못 소모된다.",
             slo="결제 5xx ≤ 0.1%"),
    ],
    constraints=[
        ("품질", "US-4의 AC 4건 전부 커버. **실패 흐름 2건**(AC3 취소·AC4 노쇼) 포함."),
        ("보안", "**REQ-NF-011** 서명 검증·카드 정보 비보관을 테스트로 고정한다."),
        ("멱등성", "중복 웹훅 테스트는 **선택이 아니라 필수**다 — 서버리스에서 중복은 반드시 발생한다."),
    ],
    external="**DEP-T4** PG 계약 확정 후 서명 알고리즘을 픽스처에 반영해야 한다",
)

SPECS["TST-005"] = dict(
    purpose="US-5·US-6의 공급 측 AC를 검증한다. 근거 없는 제안 문구와 빈 제안 화면은 "
            "이 제품의 신뢰를 무너뜨리는 두 실패 모드라 테스트로 고정한다.",
    seq="SD-06 에이전트 소환 → 제안 수집 → 180초 마감",
    breakdown=[
        "**US-5 매장 프로필** — AC1 항목별 등록(화면 ≤3·필수 ≤5) · **AC2 수용 조건 밖 미소환** · AC3 1회 클릭 재편집 · **AC4 근거 없는 문구 거부**",
        "**US-6 소환·제안** — AC1 3~5곳 소환 · AC2 180초 내 도착률 · AC3 적합도 정렬·가격 협상 0건 · **AC4 제안 0건 폴백** · **AC5 불이행 가중치**",
        "정렬 키 검증 — 가격 관련 필드가 정렬에 쓰이지 않는지 (스키마에 없음을 확인)",
        "마감 판정 테스트 — 서버 시각 기준, 동시 마감 중복 전이 없음",
        "픽스처 사용 — `MCK-001` 의 대화방·제안 픽스처 (0건/1건/5건/마감 경과)",
    ],
    scenarios=[
        dict(title="US-5·6의 정상 AC가 통과한다",
             given="대화방 픽스처가 준비된 상태에서",
             when="테스트를 실행하면",
             then="프로필 등록과 소환·정렬 AC가 통과한다.",
             slo="소환 3–5곳 · 근거 표기율 100%"),
        dict(fail=True, title="근거 없는 제안이 거부된다",
             given="등록 속성으로 뒷받침되지 않는 headline을 제출하고",
             when="테스트가 응답을 검사하면",
             then="400이 반환되고 저장되지 않는다.",
             slo="근거 없는 제안 문구 0건"),
        dict(fail=True, title="제안 0건에 빈 화면이 나오지 않는다",
             given="180초가 지나고 유효 제안이 0건인 픽스처에서",
             when="결과를 검사하면",
             then="제안 없는 Top-3로 복귀하고 사실이 안내된다.",
             slo="빈 제안 화면 노출 0건"),
    ],
    constraints=[
        ("품질", "US-5·6의 AC 9건 전부 커버. **실패 흐름 4건** 포함."),
        ("설계 원칙", "가격 협상 기능 0건(§6.3-7)과 빈 제안 화면 0건(§6.3-6)을 테스트로 고정한다."),
    ],
    external="없음",
)

SPECS["TST-007"] = dict(
    purpose="제약 게이트가 실제로 작동하는지 **의도적 위반으로** 확인한다. 게이트를 만들어 두고 "
            "작동을 검증하지 않으면 조용히 무력화된 채로 남는다.",
    seq="해당 없음",
    breakdown=[
        "위반 픽스처 작성 — REQ-TEC-001~015 각각을 의도적으로 어기는 최소 코드 조각",
        "  · 모듈 내부 직접 임포트 (REQ-TEC-002) · 원시 DB 커넥션 (REQ-TEC-004)",
        "  · 커넥션 환경 변수 누락 (REQ-TEC-005) · AI 직접 호출 (REQ-TEC-008)",
        "  · 모델명 하드코딩 (REQ-TEC-009) · 외부 CI 파일 추가 (REQ-TEC-011)",
        "  · Redis 의존성 추가 (REQ-TEC-015) · 메시지 브로커 추가 (REQ-TEC-014)",
        "각 위반에 대해 **빌드가 실패하는지** 검증하는 테스트 작성",
        "게이트가 통과시키면 테스트가 실패하도록 구성 (역방향 단언)",
        "정기 실행 — 게이트 수정 시마다 회귀 확인",
    ],
    scenarios=[
        dict(title="정상 코드는 게이트를 통과한다",
             given="제약을 지킨 코드 상태에서",
             when="빌드를 실행하면",
             then="게이트가 통과하고 배포가 진행된다."),
        dict(fail=True, title="각 위반이 빌드를 막는다",
             given="REQ-TEC 위반 픽스처를 하나씩 적용하고",
             when="빌드를 실행하면",
             then="**전부 빌드가 실패**한다. 하나라도 통과하면 이 테스트가 실패해 게이트의 구멍을 알린다.",
             slo="검증 대상 위반 전건 차단 (REQ-TEC-012)"),
    ],
    constraints=[
        ("기술 제약", "**C-TEC-007 · ADR-T08** 게이트가 빌드에 있으므로 이 테스트도 빌드 맥락에서 실행된다."),
        ("검증 원칙", "게이트는 **만드는 것보다 작동을 확인하는 것**이 중요하다. 무력화된 게이트는 없는 것보다 나쁘다."),
    ],
    external="없음",
)

SPECS["TST-008"] = dict(
    purpose="스키마와 정책이 규약대로인지 정적으로 검사한다. RLS 누락·금지 컬럼 존재·이벤트 계약 "
            "불일치는 런타임에 드러나기 전에 잡아야 한다.",
    seq="해당 없음",
    breakdown=[
        "**RLS 정책 테스트** — 전 테이블 RLS 활성화 여부, 익명 롤로 금지 테이블 접근 시 거부",
        "매장 간 격리 테스트 — 가맹 A 세션으로 가맹 B 데이터 접근 시 0행",
        "**이벤트 계약 테스트** — `CTR-005` 의 20종 스키마와 실제 적재 이벤트가 일치하는지",
        "**결제 컬럼 부재 검사** — `Payment` 에 카드 정보 컬럼이 없는지 (REQ-NF-011)",
        "`Proposal` 가격 필드 부재 검사 (§6.3-7)",
        "개인정보 필드 부재 검사 — 회식 참석자 비고·개인 취향 (REQ-NF-010)",
        "빌드 단계 편입 — 위반 시 배포 차단 (REQ-TEC-012)",
    ],
    scenarios=[
        dict(title="스키마와 정책이 규약을 만족한다",
             given="마이그레이션이 적용된 상태에서",
             when="검사를 실행하면",
             then="RLS 전 테이블 활성화·금지 컬럼 0개·이벤트 계약 일치가 확인된다."),
        dict(fail=True, title="RLS가 빠진 테이블을 잡아낸다",
             given="RLS가 비활성화된 테이블이 추가되고",
             when="검사를 실행하면",
             then="해당 테이블명을 지목하고 빌드가 실패한다.",
             slo="RLS 누락 테이블 0개 (REQ-NF-012)"),
        dict(fail=True, title="금지 컬럼이 추가되면 차단된다",
             given="`Payment` 에 카드 번호 컬럼을 추가하고",
             when="검사를 실행하면",
             then="빌드가 실패한다. 카드 정보 비보관은 타협 대상이 아니다.",
             slo="카드 정보 컬럼 0개 (REQ-NF-011)"),
        dict(fail=True, title="이벤트 계약 불일치를 잡아낸다",
             given="이벤트에 필수 속성이 빠진 채 적재되고",
             when="계약 테스트를 실행하면",
             then="불일치가 보고된다. 지표 집계가 조용히 틀어지는 것을 막는다."),
    ],
    constraints=[
        ("보안", "**REQ-NF-011 · 012 · REQ-NF-010** — 셋 다 스키마·정책 수준에서 검사한다. 코드 리뷰에 의존하지 않는다."),
        ("기술 제약", "**REQ-TEC-012** 빌드 단계 편입 — 위반 시 배포 차단."),
        ("검사 원칙", "정적으로 잡을 수 있는 것을 런타임에 맡기지 않는다."),
    ],
    external="없음",
)

SPECS["TST-010"] = dict(
    purpose="게이트 2의 통과 조건인 300 RPS 부하 테스트를 만든다. SRS가 게이트 조건으로 걸어 둔 "
            "검증인데 이를 수행할 수단이 없으면 게이트를 판정할 수 없다.",
    seq="SD-01 조건 검색 → Top-3 반환",
    breakdown=[
        "부하 테스트 스크립트 작성 — 300 RPS 목표 (Phase 1 기준)",
        "**경로별 분리 측정** — `parse_path` 로 결정론 경로와 LLM 경로의 p95를 나눠 측정",
        "시나리오 구성 — 결정론 70% / LLM 30% 비율로 실제 분포를 모사",
        "Supavisor 풀 사용률 동시 관측 — 80% 초과 여부 (R-T2)",
        "**LCP 측정** — 4G 조건에서 모바일 초기 렌더 (REQ-NF-004)",
        "결과 리포트 — 목표 대비 실측, 병목 구간 지목 (성능 예산 §4.2.1의 어느 구간인지)",
        "게이트 판정 자료 제출 — 게이트 2 통과 여부의 근거",
    ],
    scenarios=[
        dict(title="300 RPS에서 경로별 목표를 만족한다",
             given="Phase 1 규모 데이터가 적재된 상태에서",
             when="300 RPS 부하를 가하면",
             then="결정론 경로와 LLM 경로의 p95가 각각 목표 안에 든다.",
             slo="결정론 ≤ 1,000ms · LLM ≤ 2,500ms · 풀 사용률 ≤ 80% (REQ-NF-001a·001b·003)"),
        dict(title="모바일 초기 렌더가 목표를 만족한다",
             given="4G 회선 조건에서",
             when="첫 화면을 측정하면",
             then="LCP가 목표 안에 든다.",
             slo="LCP ≤ 2.5s (REQ-NF-004)"),
        dict(fail=True, title="목표 미달 시 병목 구간을 지목한다",
             given="p95가 목표를 초과한 상태에서",
             when="리포트를 산출하면",
             then="성능 예산(§4.2.1)의 어느 구간이 초과했는지 명시된다. '느리다'가 아니라 **어디가 느린지**를 알려야 한다."),
    ],
    constraints=[
        ("성능", "**REQ-NF-001a/001b · REQ-NF-003 · REQ-NF-004** 전부 이 태스크가 검증한다."),
        ("게이트", "**SRS §10.4 게이트 2** 통과 조건이 '300 RPS 부하 테스트 통과'다. 이 태스크 없이는 게이트를 판정할 수 없다."),
        ("한계", "3,000 RPS(Phase 2)는 Supabase 컴퓨트 상향이 전제다(SRS §15-3). 이 테스트는 Phase 1 목표만 검증한다."),
    ],
    external="없음",
)

# ── Part B · UI/UX 디자인 ─────────────────────────────────────────
SPECS["UX-001"] = dict(
    purpose="디자인 토큰과 컴포넌트 인벤토리를 확정해 AI가 일관된 디자인 코드를 생성하도록 강제한다. "
            "이 목록이 없으면 shadcn/ui에 있는 것을 계속 다시 만들게 된다.",
    seq="해당 없음",
    breakdown=[
        "디자인 토큰 정의 — 색·타이포·간격·반경·그림자. Tailwind 테마로 등록 가능한 형태",
        "다크/라이트 대응 정책 결정 (하나만 지원할지, 둘 다인지)",
        "**shadcn/ui 컴포넌트 인벤토리 확정** — 이 프로젝트에서 쓸 목록과 **자체 제작 금지 목록**",
        "상태 표현 규약 — 로딩·빈·오류·경고 4종의 시각 언어 통일",
        "근거 표기 시각 언어 — 확인 일자·확인 주체·경고를 어떻게 보여 줄지의 기본형",
        "접근 가능한 대비 확인 — 텍스트 대비 기준 충족",
        "핸드오프 산출물 — 개발이 `INF-002` 에서 그대로 등록할 수 있는 형태로",
    ],
    scenarios=[
        dict(title="토큰이 Tailwind 테마로 등록 가능하다",
             given="토큰 정의가 완료된 상태에서",
             when="개발이 테마로 옮기면",
             then="추가 해석 없이 그대로 등록된다. 값이 모호해 재문의가 발생하지 않아야 한다."),
        dict(fail=True, title="인벤토리에 없는 컴포넌트 요구가 걸러진다",
             given="화면 설계에서 새 컴포넌트가 필요해지고",
             when="인벤토리를 확인하면",
             then="shadcn/ui에 대응물이 있으면 **그것을 쓰도록 결정**된다. 자체 제작은 대응물이 없을 때만 승인된다.",
             slo="중복 구현 0건 (D-08 · REQ-TEC-007)"),
    ],
    constraints=[
        ("기술 제약", "**C-TEC-004** Tailwind + shadcn/ui. **D-08** 존재하는 컴포넌트를 다시 만들지 않는다."),
        ("성능", "**REQ-NF-004** LCP ≤ 2.5s — 웹폰트 수·이미지 정책이 이 목표에 직결된다."),
        ("범위", "이 태스크는 **디자인 산출물**이다. 코드 등록은 `INF-002`(Part A)가 한다 — 두 관점을 분리한다."),
    ],
    external="없음",
)

SPECS["UX-003"] = dict(
    purpose="조건을 부담 없이 입력하게 하고, 결과를 기다리는 동안 빈 화면을 보여 주지 않는다. "
            "C3는 점심 오십 분 안에 결정해야 하므로 입력 단계가 늘면 이탈한다.",
    seq="SD-01 조건 검색 → Top-3 반환",
    breakdown=[
        "조건 입력 화면 — 자연어 1줄 입력과 구조화 조건 입력을 한 화면에서",
        "**필수 입력 필드 0개** — 아무것도 입력하지 않아도 검색이 가능해야 한다 (US-2 AC3)",
        "입력 단계 1회 이하 — 조건을 여러 화면에 나눠 받지 않는다",
        "**로딩 스켈레톤** — 500ms 내 렌더되는 형태. 결과 카드의 골격을 미리 보여 준다",
        "모바일 초기 렌더 가이드 — 이미지·폰트 정책, 첫 화면에 필요한 자산 최소화 (REQ-NF-004)",
        "진입 경로별 차이 정의 — 네이버 탭 임베드와 독립 웹의 헤더 처리",
        "핸드오프 — `RNK-003` 이 그대로 구현할 수 있는 규격으로",
    ],
    scenarios=[
        dict(title="필수 입력 없이 검색이 가능하다",
             given="조건 입력 화면이 노출된 상태에서",
             when="아무 조건도 입력하지 않고 검색하면",
             then="결과가 반환된다. 필수 입력을 요구하면 C3가 이탈한다.",
             slo="필수 입력 필드 0개 · 입력 단계 ≤ 1회 (US-2 AC3)"),
        dict(fail=True, title="대기 중 빈 화면이 없다",
             given="LLM 폴백으로 응답이 지연되는 상태에서",
             when="결과를 기다리면",
             then="500ms 내 스켈레톤이 렌더된다. 흰 화면을 보여 주지 않는다.",
             slo="스켈레톤 ≤ 500ms (REQ-NF-001b)"),
    ],
    constraints=[
        ("성능", "**REQ-NF-004** LCP ≤ 2.5s · **REQ-NF-001b** 스켈레톤 ≤ 500ms."),
        ("사용자 특성", "**C3는 시간 제약이 강하다**(SRS §8) — 입력 부담이 곧 이탈이다."),
        ("기술 제약", "**C-TEC-004 · D-08** shadcn/ui 인벤토리 안에서 구성한다."),
    ],
    external="없음",
)

SPECS["UX-004"] = dict(
    purpose="파싱이 실패했을 때 갈아탈 화면과, 결과가 없을 때 보여 줄 상태를 정의한다. "
            "'빈 화면 금지'는 구현 규칙이기 전에 **화면이 존재해야** 지킬 수 있다.",
    seq="SD-02 자연어 파싱 실패 → 구조화 필터 폴백",
    breakdown=[
        "구조화 필터 폴백 화면 — 자연어 파싱 실패 시 전환되는 필터 UI",
        "**해석하지 못한 표현을 그대로 표기** — 무엇을 못 알아들었는지 사용자가 알 수 있게",
        "빈 상태 정의 — 결과 0건 · 후보 부족(1~2건) · 커버리지 부족 각각의 화면",
        "오류 상태 정의 — 일시 오류 · 외부 시스템 실패. 사용자가 할 수 있는 다음 행동을 함께 제시",
        "제안 0건 복귀 화면 규격 — `AGR-006` 이 사용",
        "상태별 문구 톤 — 시스템 탓을 사용자에게 돌리지 않는다",
        "핸드오프 — `RNK-003` · `AGR-006` 이 구현할 규격",
    ],
    scenarios=[
        dict(fail=True, title="파싱 실패가 필터 화면으로 이어진다",
             given="자연어 조건 해석이 실패한 상태에서",
             when="화면이 전환되면",
             then="구조화 필터 UI가 렌더되고 **해석하지 못한 표현이 그대로 표기**된다.",
             slo="빈 화면 노출 0건 (US-1 AC5)"),
        dict(fail=True, title="결과 0건에도 다음 행동이 있다",
             given="폴백까지 거쳐도 후보가 0건인 상태에서",
             when="빈 상태가 렌더되면",
             then="사유와 함께 조건 완화·반경 확대 같은 다음 행동이 제시된다. 막다른 화면을 만들지 않는다."),
    ],
    constraints=[
        ("설계 원칙", "**빈 화면 금지**(§6.3-6) — 이 태스크가 그 규칙의 화면 근거를 만든다."),
        ("품질", "**REQ-NF-007** 파싱 실패율 ≤ 1.5% · 빈 결과 반환률 ≤ 2%. 낮아도 0은 아니므로 화면이 반드시 필요하다."),
        ("톤", "실패를 사용자 탓으로 돌리는 문구를 쓰지 않는다."),
    ],
    external="없음",
)

SPECS["UX-006"] = dict(
    purpose="후보 카드에 근거 4항목·90일 경고·예상가 범위·비교 축을 담는다. 이 카드가 "
            "경쟁사 목록 화면과 이 제품이 갈리는 유일한 지점이다.",
    seq="SD-04 근거 표기와 90일 경고",
    breakdown=[
        "**근거 4항목 레이아웃** — 선정 이유 1줄 · 근거 속성 · 확인 일자 · 확인 주체. 넷이 모두 보이는 구조",
        "**90일 경과 경고** 표시 규칙 — 눈에 띄되 후보를 부정적으로 보이게 하지 않는 톤",
        "**인당 예상가 범위** 표기 — 단일 값이 아니라 범위로. '가격 확인 필요' 상태도 함께 정의",
        "**비교 축 레이아웃** — 세 후보를 같은 축으로 비교할 수 있게",
        "판정형 표현 배제 — '좋습니다' 같은 단정 문구를 시각 언어에서도 쓰지 않는다",
        "카드 확장 상호작용 — 근거 문장·확인 일자를 펼쳐 보는 동작 (`card_expanded` 계측 대상)",
        "핸드오프 — `RNK-003` 구현 규격",
    ],
    scenarios=[
        dict(title="근거 4항목이 한 카드에 모두 보인다",
             given="후보 카드 디자인이 완료된 상태에서",
             when="카드를 확인하면",
             then="선정 이유·근거 속성·확인 일자·확인 주체가 모두 표시된다.",
             slo="4개 항목 표기율 100% (US-3 AC1)"),
        dict(title="예상가가 범위로 표기된다",
             given="가격 데이터가 충분한 후보에서",
             when="카드를 보면",
             then="인당 예상가가 범위로 표시된다.",
             slo="범위 폭 ≤ ±20% (US-1 AC1)"),
        dict(fail=True, title="90일 경과 후보에 경고가 붙는다",
             given="속성이 90일 이상 갱신되지 않은 후보에서",
             when="카드를 보면",
             then="'확인 90일 경과' 경고가 함께 표시된다. 경고 없이 노출되면 안 된다.",
             slo="경고 누락률 0% (US-3 AC2)"),
        dict(fail=True, title="가격 데이터가 부실하면 다르게 표기된다",
             given="표본이 부족하거나 오래된 가격 데이터의 후보에서",
             when="카드를 보면",
             then="예상가 대신 **'가격 확인 필요'** 가 표기된다. 없는 정확도를 있는 것처럼 보이게 하지 않는다."),
    ],
    constraints=[
        ("설계 원칙", "**근거 없는 후보는 카드가 될 수 없다**(§6.3-2). **판정하지 않는다**(§6.3-4) — 시각 언어도 단정하지 않는다."),
        ("품질", "**보조 4** 근거 표기율 100% · **보조 3** Top-3 내 선택률 45%."),
        ("기술 제약", "**C-TEC-004 · D-08** shadcn/ui 인벤토리 안에서 구성한다."),
    ],
    external="없음",
)

SPECS["UX-010"] = dict(
    purpose="결정을 남에게 보여 줄 수 있는 카드로 만들고, 방문 후 사실을 되돌려 받을 입구를 만든다. "
            "공유는 C1의 Job이고, 방문 후 입력은 근거 품질을 유지하는 회로다.",
    seq="SD-05 공유 카드 생성 · SD-09 불일치 신고 → 재확인 큐",
    breakdown=[
        "공유 카드 비주얼 — OG 이미지 규격(크기·안전 영역·폰트 크기)",
        "카드에 담을 것 — 선정 이유 1줄과 근거. **판정형 문구를 넣지 않는다**",
        "브랜딩 최소화 — 광고물로 보이지 않게",
        "방문 후 결제액 입력 폼 — 부담 없이 한 번에 끝나는 구조",
        "불일치 신고 폼 — 어떤 속성이 달랐는지 지목할 수 있게 (`EVD-005` 가 속성 키를 받는다)",
        "신고 후 피드백 — '재확인 요청됨'을 사용자에게 알림",
        "핸드오프 — `EVD-004`(카드) · `RNK-003`(폼) 구현 규격",
    ],
    scenarios=[
        dict(title="공유 카드가 근거를 담는다",
             given="후보를 선택한 상태에서",
             when="공유 카드를 생성하면",
             then="선정 이유와 근거가 담긴 이미지가 만들어진다.",
             slo="공유 카드 생성률 ≥ 60% (US-3 AC3)"),
        dict(title="방문 후 입력이 한 번에 끝난다",
             given="방문·결제가 완료된 상태에서",
             when="결제액 입력 폼을 열면",
             then="한 화면에서 입력이 완료된다. 단계가 늘면 아무도 입력하지 않는다.",
             slo="±10% 내 비율 ≥ 80% (US-1 AC4)"),
        dict(fail=True, title="불일치를 속성 단위로 신고할 수 있다",
             given="방문 후 조건이 달랐던 상황에서",
             when="신고 폼을 열면",
             then="어떤 속성이 달랐는지 지목할 수 있고, 신고 후 '재확인 요청됨'이 안내된다.",
             slo="반영 지연 ≤ 60s (US-3 AC4)"),
    ],
    constraints=[
        ("설계 원칙", "공유 카드는 **근거를 담는 그릇**이지 홍보물이 아니다. 판정형 문구·과장 표현을 넣지 않는다."),
        ("품질", "**보조 5** 예상가 오차 ±10% 내 80% — 방문 후 입력이 이 지표의 유일한 재료다."),
        ("참여율", "입력·신고 폼은 단계가 늘수록 참여가 급감한다. 한 화면·한 동작을 원칙으로 한다."),
    ],
    external="없음",
)

SPECS["UX-013"] = dict(
    purpose="사장이 직접 조작하는 화면을 만든다. **화면 수가 곧 이탈률**이므로 설정 화면 3개·필수 항목 "
            "5개라는 상한이 디자인의 출발점이다.",
    seq="해당 없음",
    breakdown=[
        "콘솔 전체 구조 설계 — **설정 화면 ≤ 3개**",
        "프로필 입력 화면 — 분위기·강점·서비스·수용 조건. **필수 항목 ≤ 5개**",
        "근거 없는 문구 안내 UI — 저장이 거부됐을 때 **무엇을 등록해야 하는지** 알려 주는 화면 (US-5 AC4)",
        "1회 클릭 재편집 — 기존 항목을 불러오는 동선 (US-5 AC3)",
        "소환 알림 수신 화면 — 조건과 남은 시간이 한눈에",
        "제안 작성 화면 — 강점을 적는 곳이지 **할인폭을 적는 곳이 아님**을 시각적으로 명확히",
        "검색 화면과 다른 레이아웃 언어 — 역할이 다름을 시각으로 구분",
        "핸드오프 — `MCH-002` 구현 규격",
    ],
    scenarios=[
        dict(title="설정이 3개 화면 안에서 끝난다",
             given="콘솔 구조가 설계된 상태에서",
             when="화면 수를 세면",
             then="설정 화면이 3개를 넘지 않고 필수 항목이 5개를 넘지 않는다.",
             slo="설정 화면 ≤ 3개 · 필수 항목 ≤ 5개 (US-5 AC1)"),
        dict(fail=True, title="근거 없는 문구가 거부될 때 다음 행동을 안내한다",
             given="사장이 뒷받침되지 않는 강점 문구를 입력하고",
             when="저장이 거부되면",
             then="**어떤 속성을 등록해야 하는지** 구체적으로 안내한다. 거부만 하고 끝내면 사장이 이탈한다."),
    ],
    constraints=[
        ("사용자 특성", "**P5는 조작 숙련도가 낮다**(SRS §8) — 화면 수 상한이 이탈 방어의 핵심이다."),
        ("설계 원칙", "**할인폭을 넣는 칸이 아니다** — 경쟁 축은 가격이 아니라 조건 적합도(§6.3-7)."),
        ("리스크", "**R1** 매장 참여 미달이 이 제품의 최대 리스크다. 콘솔 UX가 참여율을 좌우한다."),
    ],
    external="없음",
)

SPECS["UX-014"] = dict(
    purpose="180초 카운트다운과 제안 비교 화면을 설계한다. 대기 중 이탈을 막으면서 "
            "**가격이 아니라 적합도로** 비교하게 만드는 것이 이 화면의 과제다.",
    seq="SD-06 에이전트 소환 → 제안 수집 → 180초 마감",
    breakdown=[
        "카운트다운 표시 — **서버가 내려준 마감 시각 기준**임을 전제로 설계",
        "대기 중 화면 — 몇 곳이 소환됐는지, 몇 건이 도착했는지 실시간 표시 (대기 이탈 방어)",
        "제안 도착 애니메이션 — 도착 순서가 아니라 **적합도 순 정렬**임을 오해 없게",
        "제안 비교 레이아웃 — 분위기·강점·서비스를 같은 축으로",
        "**근거 표기** — 각 제안이 어떤 등록 속성에 근거하는지 (§6.3-2)",
        "가격 비교 UI를 만들지 않는다 — 흥정으로 읽힐 여지를 시각에서 제거",
        "제안 0건 복귀 화면 — `UX-004` 규격 재사용",
        "핸드오프 — `AGR-003` · `AGR-006` 구현 규격",
    ],
    scenarios=[
        dict(title="대기 중 상태가 보인다",
             given="대화방이 생성되고 카운트다운이 시작된 상태에서",
             when="화면을 보면",
             then="소환된 매장 수와 도착 제안 수, 남은 시간이 표시된다.",
             slo="대기 중 이탈 ≤ 15%"),
        dict(title="제안이 적합도 순으로 비교된다",
             given="제안이 여러 건 도착한 상태에서",
             when="비교 화면을 보면",
             then="적합도 순으로 정렬되고 각 제안의 근거가 함께 표시된다.",
             slo="근거 표기율 100% · 가격 비교 UI 0개 (US-6 AC3)"),
        dict(fail=True, title="제안 0건에도 화면이 비지 않는다",
             given="180초가 지나고 제안이 0건인 상태에서",
             when="화면이 전환되면",
             then="제안 없는 Top-3 복귀 화면이 렌더된다.",
             slo="빈 제안 화면 노출 0건 (US-6 AC4)"),
    ],
    constraints=[
        ("설계 원칙", "**경쟁 축은 가격이 아니라 적합도**(§6.3-7) — 가격 비교 UI를 만들지 않는다."),
        ("성능", "**US-6 AC2** 첫 제안 p50 ≤ 60s · 180초 내 도착률 85%. 대기 화면이 이탈률을 좌우한다."),
        ("기술 제약", "카운트다운은 **서버 마감 시각 기준**이다(ADR-T06) — 클라이언트 시계를 신뢰하지 않는 설계여야 한다."),
    ],
    external="없음",
)

SPECS["UX-015"] = dict(
    purpose="선택한 제안 그대로 예약·결제가 끝나게 한다. **재입력 필드 0개**가 이 화면의 성패이며, "
            "조건을 다시 묻는 순간 결정이 실행으로 이어지지 않는다.",
    seq="SD-07 제안 선택 → 예약 승계 → 선결제",
    breakdown=[
        "예약 확인 화면 — 승계된 인원·메뉴 구성·시간을 **읽기 전용**으로 표시",
        "**재입력 필드 0개** — 사용자가 다시 채워야 하는 칸이 없어야 한다 (US-4 AC1)",
        "금액 표시 — 주문량 기준 산출 내역을 투명하게",
        "선결제 안내 — **4인 이상 단체에만 적용**됨을 명확히. 왜 선결제인지 한 줄 설명",
        "취소·환불 정책 표시 — 2시간 전 취소 시 전액 환불",
        "결제 실패 상태 — 재시도 경로 제공",
        "핸드오프 — `RSV-003` 구현 규격",
    ],
    scenarios=[
        dict(title="승계된 조건이 그대로 보인다",
             given="제안을 선택한 상태에서",
             when="예약 화면으로 넘어가면",
             then="인원·메뉴·시간이 읽기 전용으로 표시되고 입력할 칸이 없다.",
             slo="재입력 필드 0개 (US-4 AC1)"),
        dict(title="취소·환불 정책이 결제 전에 보인다",
             given="결제 화면에서",
             when="선결제 안내를 보면",
             then="2시간 전 취소 시 전액 환불이 명시된다. 결제 후에 알게 되면 신뢰가 깨진다."),
        dict(fail=True, title="결제 실패 시 되돌아갈 길이 있다",
             given="PG 승인이 거절된 상태에서",
             when="화면이 렌더되면",
             then="사유와 재시도 경로가 제시된다. 예약은 미확정 상태로 유지된다."),
    ],
    constraints=[
        ("품질", "**US-4 AC1** 재입력 필드 0개 · 승계 누락률 ≤ 0.5%."),
        ("설계 원칙", "**선결제는 4인 이상 단체만**(§6.3-8) — 소규모까지 걸면 전환율이 깎인다(R4)."),
        ("신뢰", "취소·환불 정책을 **결제 전에** 보여 준다. 경쟁 서비스가 '0원 예약'을 내세운 시장이다."),
    ],
    external="없음",
)
