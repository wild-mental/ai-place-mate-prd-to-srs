# ai-place-mate-prd-to-srs

**AI 장소 큐레이션 PRD v0.1** 을 **SRS 문서**로 변환하는 작업 저장소입니다.

---

## 1. 작업 목표

이 저장소의 목표는 **PRD를 예시 SRS 문서와 동일한 포맷의 SRS로 변환**하는 것입니다.

### 1.1 하는 일

> **기준 템플릿은 `[SRS 문서] AD-Core-Platform (한글).md` 하나입니다.**

`ai-place-prd-v0.1.html` 에 담긴 내용을, 예시 SRS 문서가 이미 쓰고 있는 섹션 구성·표 스키마·ID 체계·서술 톤으로 옮겨 적습니다. 도메인만 다를 뿐(광고 플랫폼 → 장소 큐레이션), 문서의 **뼈대는 예시 문서를 그대로 따릅니다.**

### 1.2 하지 않는 일

**ISO/IEC/IEEE 29148:2018 표준을 전부 만족하는 풀 스펙 SRS를 쓰는 것이 목표가 아닙니다.**

표준이 요구하는 전체 섹션(운영 개념, 생명주기 요구사항, 검증 계획 전문, 요구사항 속성 완전 집합 등)을 빠짐없이 채우는 방향으로 문서를 부풀리지 않습니다. 예시 SRS가 다루지 않은 영역은, 아래 3항의 확장 규칙에 해당하지 않는 한 **쓰지 않습니다.**

### 1.3 확장 규칙 — 예시 문서 범위를 넘는 PRD 내용의 처리

PRD에는 예시 SRS의 7개 섹션 어디에도 자연스럽게 들어가지 않는 내용이 있습니다. 이런 내용은 버리지 않되, 임의의 형식으로 덧붙이지도 않습니다.

**해당 부분에 한해서만 ISO/IEC/IEEE 29148:2018 의 표준 포맷 구성을 따라 섹션을 확장해 작성합니다.**

즉 세 갈래로 처리합니다.

| 상황 | 처리 |
| --- | --- |
| 예시 SRS에 대응 섹션이 있음 | 예시 포맷 그대로 채움 |
| 예시 SRS에 대응 섹션이 없음 | **그 부분만** 29148 표준 구성에 따라 섹션 신설 |
| PRD에 없는데 예시 SRS에는 있는 항목 | 억지로 만들지 않고 미정으로 표기 |

확장으로 신설한 섹션은 어느 표준 조항을 근거로 추가했는지 문서 안에 밝힙니다. 확장은 **최소 단위**로 합니다 — 한 문단을 넣기 위해 표준 전체 챕터를 끌어오지 않습니다.

---

## 2. 파일 구성

| 파일 | 역할 |
| --- | --- |
| `ai-place-prd-v0.1.html` | **변환 입력물(원본).** AI 장소 큐레이션 PRD v0.1 (15장 덱, mermaid 다이어그램 포함) |
| `ai-place-prd-v0.1.md` | 위 HTML을 내용 손실 없이 옮긴 Markdown (v0.1 초안 · 무손실 검증 기준선) |
| `ai-place-prd-v1_0.md` | **SRS 변환 입력물.** 완성도 6기준을 적용해 개정한 PRD v1.0 — 가설 경고 제거, 측정 창구·실패 AC·공수·ADR 보강 |
| `tools/` | HTML→MD 변환기와 무손실 검증 스크립트 |
| `[SRS 문서] AD-Core-Platform (한글).md` | **포맷 기준.** 다른 도메인의 완성된 한글 SRS. 이 문서의 골격을 따름 |
| `29148-2018-ISOIECIEEE.pdf` | **확장 근거.** 1.3의 확장이 필요할 때만 참조 (저작권 문제로 저장소에 포함하지 않음 · 로컬 전용) |
| `docs/[SRS 문서] AI-Place-Mate (한글).md` | **산출물 ①.** PRD v1.0을 예시 SRS 양식으로 변환한 SRS (1–7장 양식 준수 · 8–14장 29148 확장 · 다이어그램 9개 배치) |
| `docs/[설계 문서] AI-Place-Mate (한글).md` | **산출물 ②.** SRS에서 도출한 기술 설계 문서 (SDD) — 다이어그램 32개 |
| `docs/[SRS 문서] AI-Place-Mate (기술제약 반영판).md` | **산출물 ③.** 기술 제약 C-TEC-001~007을 적용한 병렬 SRS (Next.js 단일 스택) |
| `docs/[태스크 리스트] AI-Place-Mate.md` | **산출물 ④.** 반영판 SRS에서 도출한 실행 태스크 97건 (Epic 13 · 의존성 · 복잡도) |
| `docs/[분석] 태스크 추출 방법론 적합성 평가.md` | **산출물 ⑤.** 4단계 추출 방법론 기준 갭 분석 및 풀버전 작성 순서 |
| `.github/ISSUE_TEMPLATE/feature-task.md` | GitHub Project 용 TASK 템플릿 (`tools/make_task_template.py` 생성) |

---

## 3. 포맷 기준 — 예시 SRS의 골격

변환 결과물이 따라야 할 구성입니다.

```
1. 서론          1.1 목적 / 1.2 범위 / 1.3 정의·약어·축약어
2. 이해관계자     역할 · 담당 · 책임 표
3. 시스템 맥락 및 인터페이스   클라이언트 / 내부 서비스 / 외부 시스템
4. 구체적 요구사항  4.1 기능 요구사항 (REQ-FUNC-xxx)
                 4.2 비기능 요구사항 (REQ-NF-xxx)
5. 추적성 매트릭스  요구사항 ID ↔ 모듈 ↔ 구현 클래스 ↔ 테스트 케이스 ID
6. 부록          6.1 API 엔드포인트 / 6.2 데이터 모델 / 6.3 비즈니스 규칙 / 6.4 DB 스키마
7. 향후 개선 사항
```

요구사항 표의 열 스키마도 예시 문서를 그대로 씁니다.

`ID | 제목 | 출처 | 우선순위 | 유형 | 검증 방식 | 인수 기준 | 상태 | 담당자`

---

## 4. 입력 PRD 개요

- **제품:** AI 장소 큐레이션 — "조건을 말하면 근거가 붙은 세 곳이 온다"
- **북극성 KPI:** WEBD(주간 근거 기반 결정 완료 수) · 12주 목표 5,000건/주
- **기능 범위:** F1–F9 · Must 6 / Should 1 / Could 2
- **단계:** Phase 1 클로즈드 베타(수요 측) → Phase 2 오픈 베타(에이전트 제안)
- **유저 스토리:** US-1 ~ US-6, 각 AC마다 정량 임계치 보유
- **실험 설계:** E1 ~ E7

### 변환 시 유의점

PRD 기준선 대부분이 **가설(H)** 상태입니다. 모의 인터뷰 11건, 실제 로그 0건. PRD 스스로 "정확한 목표값 제시가 아니라 무엇을 측정하면 틀렸음을 알 수 있는지를 못 박는 것"이 목적이라고 밝히고 있습니다.

따라서 SRS의 **인수 기준** 칸에 이 수치들을 옮길 때는 확정 사양이 아니라 **Phase 0 이후 재설정 대상**임이 드러나게 표기합니다. (표기 방식 확정 필요)

---

## 5. 진행 상태

- [x] 입력물·기준 문서·표준 원문 확보
- [x] 작업 목표 정의 (본 문서)
- [x] PRD HTML → Markdown 무손실 변환
- [x] PRD v1.0 개정 (완성도 6기준 적용)
- [x] PRD → 예시 SRS 섹션 매핑 (SRS §14.3)
- [x] 예시 범위 초과 항목 식별 및 29148 확장 장 확정 (8–14장)
- [x] SRS 본문 작성
- [x] 추적성 매트릭스 작성
- [x] SRS §10.2를 KPI 계측 계획으로 교체
- [x] 기술 설계 문서(SDD) 작성 및 SRS에 다이어그램 배치
- [x] 기술 제약 반영판 SRS 작성 (Next.js + Supabase + Gemini + Vercel)
- [x] 실행 태스크 리스트 도출 (97건 · 커버리지 누락 0)

---

## 6. PRD Markdown 변환

원본 PRD가 슬라이드 덱 HTML이라 그대로는 대조 작업이 어렵습니다. `tools/html_to_md.py` 로 **내용 손실 없이** `ai-place-prd-v0.1.md` 를 생성해 두었습니다.

### 변환 규칙

| 원본 요소 | Markdown |
| --- | --- |
| 슬라이드 `<section>` 15장 | `---` 구분 + `<!-- 슬라이드 NN / 15 -->` 주석 |
| `<table>` 10개 | GFM 표 (셀 내 줄바꿈은 `<br>` 유지) |
| `<pre class="mermaid">` 4개 | ` ```mermaid ` 코드 펜스 · **줄바꿈 원문 보존** |
| `.story` / `.ac` | `#### US-n` + AC 불릿. Given/When/Then 볼드 |
| `.thr` 임계치 배지 | 인라인 코드 `` `p95 ≤ 1,000ms` `` |
| `.card` / `.risk` / `.warn` | 인용구 블록 |
| `<dl>/<dt>/<dd>` | `- **항목** — 값` |
| `<a href>` | `[텍스트](URL)` — 원본 링크 주소까지 보존 |

### 무손실 검증

`tools/verify_lossless.py` 가 HTML 슬라이드 본문과 생성된 MD의 **토큰 시퀀스를 diff** 합니다.

```
$ python3 tools/verify_lossless.py
HTML 토큰 6173 / MD 토큰 6352
HTML에만 있는 토큰(손실) 0개
MD에만 있는 토큰(추가)   179개
```

**손실 0개.** 추가된 179 토큰은 전부 설명됩니다.

- 38 — 변환기가 붙인 문서 머리말
- 11 — 표 셀 줄바꿈 `<br>`
- 130 — 링크 URL 6개. HTML 본문 텍스트에는 없고 `href` 속성에만 있던 값을 Markdown 본문에 노출한 것

즉 **원본에 있던 글자는 하나도 빠지지 않았고, 늘어난 것은 출처 URL과 구조 표시뿐**입니다.

---

## 7. SRS 변환 결과

`docs/[SRS 문서] AI-Place-Mate (한글).md` — 782줄 · 표 36개 · REQ-FUNC 10건 · REQ-NF 15건.

### 1–7장 — 예시 양식 준수

예시 SRS의 7개 장 구성과 요구사항 표 9열 스키마(`ID | 제목 | 출처 | 우선순위 | 유형 | 검증 방식 | 인수 기준 | 상태 | 담당자`)를 그대로 따랐습니다.

### 8–14장 — 29148 근거 확장

PRD가 이미 확정했으나 예시 양식에 대응 절이 없는 내용만, 표준 조항을 명시해 확장했습니다.

| 장 | 근거 조항 | PRD 출처 |
| --- | --- | --- |
| 8. 사용자 특성 | §9.6.6 User characteristics | 페르소나 |
| 9. 수용 기준 명세 | §9.6.10 d) Specified requirements | US-1 ~ US-6 |
| 10. 검증 및 확인 계획 | §9.6.19 Verification | KPI · 계측 계획 · 릴리스 게이트 |
| 11. 제약 사항 | §9.6.7 Limitations | 리스크 · 정책 제약 |
| 12. 가정 및 의존성 | §9.6.8 Assumptions and dependencies | 가정 · 외부 의존성 |
| 13. 설계 제약 (ADR) | §9.6.16 Design constraints | ADR-001 ~ 005 |
| 14. 요구사항 배분 | §9.6.9 Apportioning of requirements | 공수 · 슬라이스 · 연기 항목 |

확장은 최소 단위로 했고, PRD에 없는 내용은 신설하지 않았습니다.

---

## 8. 설계 문서 (SDD)

`docs/[설계 문서] AI-Place-Mate (한글).md` — 1,443줄 · **다이어그램 32개**. SRS의 요구사항을 어떻게 구현할지를 그림으로 정리했고, 새 요구사항은 만들지 않았습니다.

| 종류 | 개수 | 내용 |
| --- | --- | --- |
| 컨텍스트 · 컴포넌트 | 2 | 시스템 경계, 8개 서비스 호출 관계 |
| 유스케이스 | 1 (+명세 15건) | 행위자 5종 · UC-01 ~ UC-15 |
| ERD | 1 | SRS §6.2를 5개 개체로 확장 |
| 상태 다이어그램 | 4 | Verification · AgentRoom · Reservation·Payment · Proposal |
| 클래스 다이어그램 (CLD) | 6 | 서비스별 · SRS 추적성 매트릭스의 클래스명과 일치 |
| 시퀀스 다이어그램 | 10 | SD-01 ~ SD-10 |
| 플로차트 | 6 | FC-01 ~ FC-06 판단 분기 |
| 데이터 흐름 · 성능 예산 | 2 | 계측 파이프라인, 1,000ms 구간 배분 |

**검증** — 41개 다이어그램(SDD 32 + SRS 9) 전부를 mermaid 파서로 렌더해 통과했습니다.

### SRS에 배치한 다이어그램 9개

전체 32개를 SRS에 넣으면 명세서가 아니라 설계서가 되므로, **요구사항을 이해하는 데 직접 필요한 것만** 배치했습니다.

| SRS 위치 | 다이어그램 |
| --- | --- |
| §0 문서 구성 안내 | 그림 읽는 법 표 (배경지식 없는 독자용) |
| §3.1 · §3.3 | 컨텍스트 · 컴포넌트 |
| §4.0 | 유스케이스 |
| §6.2 | ERD (기존) + Verification 상태 다이어그램 |
| §9.0 | 핵심 흐름 시퀀스 (1,000ms 예산 분해) |
| §10.2 | 계측 파이프라인 데이터 흐름 |
| §10.4 | 릴리스 게이트 (기존) |
| §14.1 | 요구사항 의존성 그래프 |

각 다이어그램에는 **"이 그림이 말하는 것"** 한 줄과 읽는 법을 붙였습니다.

---

## 9. 기술 제약 반영판 SRS

`docs/[SRS 문서] AI-Place-Mate (기술제약 반영판).md` — 1,076줄 · 표 37개 · 다이어그램 6개.

기술 중립판(SRS-AIPLACE-MVP-001)이 **무엇을 만들 것인가**를 정의한다면, 이 문서는 **주어진 스택으로 어떻게 성립시킬 것인가**를 정의합니다. 원본을 대체하지 않는 **병렬 문서**입니다.

### 적용한 제약

| ID | 제약 |
| --- | --- |
| C-TEC-001 · 002 | Next.js App Router 단일 풀스택 · Server Actions / Route Handlers |
| C-TEC-003 | Prisma + Supabase (로컬·운영) |
| C-TEC-004 | Tailwind CSS + shadcn/ui |
| C-TEC-005 · 006 | Vercel AI SDK + Google Gemini (환경 변수로 모델 교체) |
| C-TEC-007 | Vercel 단일화 · Git Push 배포 |

### 제약이 깨뜨린 요구사항 4건과 해소

제약을 정확히 적용하면 기술 중립판의 요구사항 일부가 **그대로는 성립하지 않습니다.** 숨기지 않고 조정 근거와 함께 §15 대장에 전량 기록했습니다.

| 요구사항 | 중립판 | 반영판 | 사유 |
| --- | --- | --- | --- |
| REQ-NF-001 응답 시간 | p95 ≤ 1,000ms 단일 | **경로 분리** — 결정론 ≤ 1,000ms / LLM ≤ 2,500ms | LLM 왕복이 파싱 예산 150ms를 구조적으로 초과 |
| REQ-NF-003 처리량 | 3,000 RPS | Phase 1 300 → Phase 2 3,000 (컴퓨트 상향 전제) | Supavisor 풀·Supabase 플랜 종속 |
| REQ-NF-002 캐시 | 별도 캐시 계층 | `use cache` + **파싱 캐시 신설** | Redis 금지 · LLM 비용 방어가 추가로 필요 |
| CI 계약 테스트 | CI에서 배포 차단 | **Vercel 빌드 단계**에서 차단 | 외부 CI 금지 · 차단 효과는 동일 보존 |

### 신설 요소

- **REQ-TEC-001 ~ 015** — 제약을 검증 가능한 요구사항으로 전환. 위반 시 빌드 실패
- **2단 파싱** — 결정론 파서(≥70% 흡수) → Gemini 폴백 → 파싱 캐시. 지연·비용을 동시에 방어
- **ADR-T01 ~ T10** — 제약을 만족시키기 위한 기술 결정 기록
- **§15 제약 충돌 해소 대장** — 충돌 12건 + **해소되지 않은 2건**(플랫폼 장애 대응 · 3,000 RPS 시점)

---

## 10. 태스크 리스트

`docs/[태스크 리스트] AI-Place-Mate.md` — 369줄 · **태스크 97건** · Epic 13개.

기술제약 반영판 SRS를 실행 가능한 Feature 단위로 분해했습니다. 반영판을 기준으로 삼은 이유는, 그 문서만이 구현 단위(Server Action · Route Handler · RSC · Cron)를 확정하고 있어 태스크로 쪼갤 수 있기 때문입니다.

### 관점 분리

| Part | 관점 | 태스크 수 |
| --- | --- | --- |
| **Part A** | 백엔드 · 프론트엔드 개발 및 인프라 구성 (12 Epic) | 81 |
| **Part B** | UI/UX 디자인 | 16 |

### 검증

| 항목 | 결과 |
| --- | --- |
| 고유 ID | 97 / 97 (중복 0) |
| 미정의 선행 태스크 | 0 |
| 순환 의존성 | 0 |
| SRS 섹션 미기재 | 0 |
| 요구사항 커버리지 | REQ-FUNC 10 · REQ-NF 17 · REQ-TEC 15 **전건 담당 태스크 보유** |
| 스프린트 배치 | 97 / 97 (누락 0) |

복잡도 분포는 H 22 · M 62 · L 13 입니다.

### 임의 추가 방지

SRS에 없는 기능은 넣지 않았고, **의도적으로 제외한 항목은 부록 D에 근거와 함께 명시**했습니다 (연기 대상 6건 · 미사용 2건 · 미해소 결정 대기 2건).

---

## 부기 · 표준 원문 취급

`29148-2018-ISOIECIEEE.pdf` 는 유료 배포되는 저작권 보호 문서라 공개 저장소에 올리지 않습니다. `.gitignore` 로 제외되어 있으며 로컬 작업 디렉터리에는 그대로 남아 있어 참조에 지장이 없습니다.
